# FocusPanel 0.10.99

这一版重新整理状态中心的信息层级，首屏不再是一条塞满所有功能的长页面。

- 首屏只保留任务栏状态、六个常用系统动作、主音量和显示亮度。
- 网络与无线、应用音量、媒体与电池改为三个默认收起的详情区。
- 每个详情标题直接显示网络名称或音频会话摘要，不展开也能获得必要状态。
- 折叠控件使用统一 Fluent 表面、圆角、悬停、键盘焦点和 44px 点击区。
- 展开状态仅在当前运行会话内保留，不写入数据库，也不会让升级用户突然看到长页面。

升级前仍会恢复原生任务栏，现有 SQLite 数据、桌面文件与用户设置保持不变。
