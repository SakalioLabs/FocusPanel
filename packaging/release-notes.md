# FocusPanel 0.10.42

- `FocusPanel-win-Setup.exe` 继续在安装前显示目录输入与浏览入口；打包阶段会无安装执行目录向导探针，GitHub 发布后再逐字节核对 SHA-256，避免公开资产被原生默认安装器顶替。可直接选择 D/E 盘，后续一键更新沿用该目录。
- 自动整理完成或失败时，即使自定义提示窗口因主题、显示变化或窗口关闭竞态无法创建，也只记录日志并采用安全结果，不再二次抛错导致 Panel 退出；桌面监视的后台批处理增加最终异常观察边界。
- 时间月历支持方向键按日/周移动、`PageUp` / `PageDown` 跨月、`Ctrl+Home` 回到今天；焦点跟随选中日期，点击今天或相邻月份日期时网格立即刷新。
