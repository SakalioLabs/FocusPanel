# FocusPanel 0.11.41

- 统一应用栏右键可直接打开真实程序位置或开始菜单快捷方式位置。
- Panel 开始和统一搜索中的应用结果新增启动、管理员启动、打开位置及固定管理菜单。
- 运行应用优先使用窗口追踪器确认的 EXE；未运行项目使用完整启动路径，不按显示名称猜测。
- 商店应用、相对命令、缺失文件和 Explorer 拒绝均明确降级，不误开无关目录。

升级前仍会恢复原生任务栏；现有 SQLite 数据、桌面文件路径、自定义分区和用户设置不会被删除。
