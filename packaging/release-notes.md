# FocusPanel 0.11.30

- 网络、应用音量、媒体与电池提升为状态中心首屏 Panel 主入口。
- 三组详情互斥展开，再次点击当前入口即可关闭并显示真实选中态。
- 快捷设置、通知中心和输入法移入明确的 Windows 辅助入口，不再冒充 Panel 主能力。
- 打开状态中心后的键盘焦点直接落到 Panel 自有后台与窗口管理。

升级前仍会恢复原生任务栏；现有 SQLite 数据、桌面文件路径、自定义分区和用户设置不会被删除。
