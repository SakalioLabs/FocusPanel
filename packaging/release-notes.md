# FocusPanel 0.11.73

- 多窗口应用图标普通点击直接固定 DWM 实时缩略卡，不再先展开完整工作区。
- 再次点击同一图标或按 Esc 只收起缩略卡；悬停预览可原地转为固定态。
- 缩略卡继续提供切换、窗口状态、排列与正常关闭，窗口过多时可进入完整列表。
- 单窗口点击语义保持不变；DWM 不可用时安全回退到文字窗口总览。

升级前仍会恢复原生任务栏；现有 SQLite 数据、桌面文件路径、自定义分区、自定义图标和用户设置不会被删除。
