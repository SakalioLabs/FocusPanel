# FocusPanel 0.10.87

这一版让九槽位快速应用键真正可见、可信。

- 成功注册的前九个应用槽位会在图标左上角显示数字角标。
- 快捷键关闭、任务栏未接管或组合键冲突时不显示误导角标。
- 启动/切换与新实例组合逐项判断，提示只承诺实际可用动作。
- 应用刷新、重排和运行状态变化后仍沿用当前真实注册状态。
- 原有固定顺序、运行窗口分组与点击语义保持不变。

升级前仍会恢复原生任务栏，现有 SQLite 数据、桌面文件与用户设置保持不变。
