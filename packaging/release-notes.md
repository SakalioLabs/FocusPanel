# FocusPanel 0.11.51

- Panel 通知历史跨重启保留，最多保存 50 条，不写入业务数据库。
- 快速变化在后台合并保存，退出和更新重启前等待最后一次写入排空。
- 损坏或异常过大的历史文件会归档并安全重建，读写错误直接显示在通知详情。
- 重启前的消息仍可阅读；旧进程动作明确失效，不显示无法兑现的按钮。

升级前仍会恢复原生任务栏；现有 SQLite 数据、桌面文件路径、自定义分区和用户设置不会被删除。
