# FocusPanel 0.10.60

- 统一搜索现在会匹配已有未完成待办；输入标题或所属项目名称即可直接打开对应项目和任务详情。
- 每条待办结果右侧提供“完成”按钮，无需离开当前应用或进入任务工作区；成功后结果立即消失并显示可点击 Toast。
- 任务索引只在搜索打开时通过共享 `TaskService` 后台刷新，空查询不会展示任务列表；完成、读取失败、任务被删除和退出中的在途操作都有安全反馈。
