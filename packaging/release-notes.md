# FocusPanel 0.11.44

- 附近 Wi-Fi 第一次连接可直接在 Panel 输入密码，不再跳转 Windows 网络设置。
- 支持开放网络及 WPA/WPA2/WPA3 Personal；企业网络明确显示组织登录边界。
- 密码不写入 FocusPanel 数据库、设置或日志，临时配置缓冲区在使用后清零，由 Windows 加密保存系统网络配置。
- 密码错误或连接超时会删除本次失败配置，下一次可直接重新输入，不会卡在错误凭据上。

升级前仍会恢复原生任务栏；现有 SQLite 数据、桌面文件路径、自定义分区和用户设置不会被删除。
