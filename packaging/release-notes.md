# FocusPanel 0.11.74

- 紧凑任务栏末端新增独立“桌面”按钮，单击显示桌面，再次点击恢复窗口。
- 操作直接复用 Panel 的 Shell 自动化边界，不发送 Win+D，也不唤出原生任务栏。
- 按钮保留 44px 点击区域、键盘和读屏支持，并沿用统一圆角与字体令牌。
- 时间按钮原有修饰键和菜单入口继续保留，现有应用、窗口和用户数据不变。

升级前仍会恢复原生任务栏；现有 SQLite 数据、桌面文件路径、自定义分区、自定义图标和用户设置不会被删除。
