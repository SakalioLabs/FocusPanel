# FocusPanel 0.11.87

- 全部窗口可按实际拥有窗口的显示器筛选，并显示每块屏幕的窗口数量。
- 搜索结果、状态中心窗口列表、应用提示和实时缩略卡直接标出窗口所在屏幕。
- 窗口拖到另一屏后自动更新；显示器断开或筛选失效时安全回到全部屏幕。
- 屏幕编号与窗口移动菜单完全一致，不轮询窗口，也不调用原生任务栏。

升级前仍会恢复原生任务栏；现有 SQLite 数据、桌面文件路径、自定义分区、自定义图标和用户设置不会被删除。
