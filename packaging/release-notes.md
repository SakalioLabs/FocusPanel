# FocusPanel 0.10.11

- 收纳文件的存在检查、原属性读取和 Hidden/System 写入不再阻塞 WPF 界面线程。
- Explorer 属性通知与文件属性操作统一进入可测试的后台 I/O 边界。
- 公共桌面的属性应用和失败回滚使用同一管理员授权路径。
- 数据库继续先记录收纳事务；失败会恢复原属性或保留“需要恢复”状态。
- 不移动、不改名用户文件，也不改变现有分区和桌面收纳数据。
