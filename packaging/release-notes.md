# FocusPanel 0.11.38

- 应用图标支持 `↑ / ↓` 逐项浏览，`Home / End` 直达首尾。
- `PageUp / PageDown` 按当前真实可见的图标数量翻页。
- 焦点项自动滚进可视区，并避开上下悬浮滚动按钮。
- 固定项 `Alt+↑ / Alt+↓` 排序、Enter 操作和右键菜单保持原语义。

升级前仍会恢复原生任务栏；现有 SQLite 数据、桌面文件路径、自定义分区和用户设置不会被删除。
