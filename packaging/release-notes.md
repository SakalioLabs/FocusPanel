# FocusPanel 0.11.37

- 删除所有会从原生任务栏弹出“显示隐藏图标”的入口，统一进入 Panel 自有后台与窗口总览。
- 收纳后的 `.lnk` 继续显示快捷方式指定的 `.ico`，同时支持网址快捷方式和自定义文件夹图标。
- 图标视图按真实可用宽度稳定排成多列，不再偶发一行一个或出现忽大忽小的间距。
- 图标使用固定桌面式网格节奏，最后一行与其他行保持同一列基准。

升级前仍会恢复原生任务栏；现有 SQLite 数据、桌面文件路径、自定义分区和用户设置不会被删除。
