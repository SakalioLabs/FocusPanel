# FocusPanel 0.11.71

- 紧凑任务栏只显示用户固定应用和拥有可见窗口的运行应用，纯后台宿主不再占用任务栏滚动长度。
- 后台应用没有被删除，仍完整保留在 Panel 自有“后台”抽屉，可搜索、固定和管理。
- 键盘导航、活动项自动露出、拖拽与九槽位全局快捷键统一跟随紧凑任务栏的真实顺序。
- 在紧凑栏空白处右键即可切换右上、右中、右下，菜单会勾选并保存当前固定位置。

升级前仍会恢复原生任务栏；现有 SQLite 数据、桌面文件路径、自定义分区、自定义图标和用户设置不会被删除。
