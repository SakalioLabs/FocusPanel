# FocusPanel 0.10.7

- 任务查询、增删改和全局字段读写全部移出 WPF 界面线程。
- 每次数据库操作使用独立短生命周期上下文，不再长期共享 EF 跟踪状态。
- 所有任务操作继续通过同一个后台闸门严格串行，失败后仍可恢复。
- 全局字段和图片目录的旧设置备份不再同步阻塞界面。
- 更新完整保留任务标量，且不会把父子导航图误附加到新上下文。
- 不改变现有任务、看板、自定义字段、Inbox 保护或数据库结构。
