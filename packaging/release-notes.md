# FocusPanel 0.11.69

- 全局窗口总览首次打开时直接预选最近使用的上一个窗口，按 Enter 即可切回。
- 保持总览打开时重复按窗口快捷键，会继续沿 MRU 顺序循环并首尾环绕。
- MRU 只记录当前会话真实窗口句柄，不保存标题、不轮询进程，也不写入数据库。
- 窗口关闭、列表缩短或选中项失效时安全回到仍存在的候选；空列表继续聚焦搜索框。

升级前仍会恢复原生任务栏；现有 SQLite 数据、桌面文件路径、自定义分区、自定义图标和用户设置不会被删除。
