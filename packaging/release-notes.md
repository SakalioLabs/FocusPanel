# FocusPanel 0.9.95

- 任务栏守护器要求连续两次同类异常才安全恢复，单次瞬态可见或 AppBar 波动不会误停替代模式。
- 正常采样会清除待确认状态；确认期间完全只读，不重新隐藏任务栏、不改写 AppBar 或工作区。
- 接管时记录主屏任务栏句柄，Explorer 创建新 Shell_TrayWnd 后会准确报告宿主变化。
- 恢复或重新接管会让正在后台读取的旧守护结果立即失效，不会污染新会话。
- 紧急恢复仍绕过确认立即执行，持续失效后仍只恢复一次，不形成闪烁循环。
- Release 构建 0 错误、0 警告，全量 551 项测试和界面冒烟通过。
