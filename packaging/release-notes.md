# FocusPanel 0.10.93

这一版把原任务栏右下角的“显示桌面”能力带回紧凑栏，同时避免增加按钮堆积。

- Shift+点击或 Shift+Enter 时间入口可一步切换显示桌面。
- 鼠标中键点击时间入口提供相同的一击操作。
- 时间右键菜单顶部新增“显示桌面 · Win+D”。
- 普通点击继续打开月历与今日任务，不改变既有操作。
- 失败时仍进入状态中心说明原因，不伪装执行成功。

升级前仍会恢复原生任务栏，现有 SQLite 数据、桌面文件与用户设置保持不变。
