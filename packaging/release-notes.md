# FocusPanel 0.9.35

- “随 Windows 启动”改用稳健的注册表边界，可创建缺失的 Run 键并正确引用带空格路径。
- 路径不可用、权限不足或系统策略拒绝时显示具体原因，重新读取真实状态并回滚复选框，不再假装启用成功。
- 取消开机启动不依赖可执行文件路径，启动项不存在时也可幂等完成。
- 保留圆角键盘焦点、更新徽标、应用溢出导航、任务栏安全恢复和 GitHub Releases 唯一更新源。
- 不迁移数据库，不改变现有任务、桌面收纳、固定应用和设置数据。
