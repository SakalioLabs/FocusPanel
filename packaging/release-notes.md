# FocusPanel 0.11.63

- 搜索“声音输出、声音设备、输出设备、音频输出、音量混合器”只进入 Panel 自有音频详情。
- 删除 `Win+Ctrl+V` 合成按键、SoundOutput Shell 动作及系统状态服务入口。
- Panel 内可直接调整主音量、静音和每个应用的独立音量。
- 不再显示第二个会跳到 Windows 声音浮层的搜索结果。
- 不使用 Windows 未公开支持的默认音频端点切换私有接口。

升级前仍会恢复原生任务栏；现有 SQLite 数据、桌面文件路径、自定义分区和用户设置不会被删除。
