# FocusPanel 0.10.22

- 桌面收纳设置读取与文件监控器创建不再阻塞主壳构造。
- 默认工作区先显示轻量 Fluent 加载状态，准备完成后原位提交。
- 用户已切换模块时，迟到的收纳结果不会覆盖当前页面。
- 初始化失败会留在可读错误状态，不会让 Panel 闪退。
- 退出期间完成的后台准备会立即释放全部文件监控器。
- Explorer 受保护文件显示状态改为后台读取。
