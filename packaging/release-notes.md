# FocusPanel 0.10.92

这一版缩短从紧凑任务栏进入 FocusPanel 核心功能的路径。

- Shift+点击 Focus 入口可一步打开上次使用的工作区。
- 键盘用户可使用 Shift+Enter 获得相同的直达语义。
- 右键 Focus 入口可直接选择桌面收纳、任务、番茄钟、AI 助手或设置。
- 快捷菜单复用单层 Fluent 主题、弹层互斥和自动收起交互锁。
- 普通左键仍打开 Focus 中心，不增加新的常驻按钮或重复导航状态。

升级前仍会恢复原生任务栏，现有 SQLite 数据、桌面文件与用户设置保持不变。
