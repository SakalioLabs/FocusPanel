# FocusPanel 0.11.85

- 按住 Panel 顶部位置把手，可直接拖到任意显示器左侧或右侧。
- 鼠标横向位置决定左/右边缘，纵向位置同时决定上、中、下。
- 多屏负坐标、不同缩放和屏幕间隙均按物理像素稳定吸附。
- 三项位置设置一次应用，只触发一次重定位，避免拖动时来回跳动。

升级前仍会恢复原生任务栏；现有 SQLite 数据、桌面文件路径、自定义分区、自定义图标和用户设置不会被删除。
