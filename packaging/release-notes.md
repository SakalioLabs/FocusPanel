# FocusPanel 0.10.59

- 统一搜索新增一步任务收集：输入 `任务 买牛奶`、`待办：回复邮件` 或 `todo book dentist`，按 Enter 直接保存到 Inbox。
- 快速收集与任务页共享同一个后台串行写入链；成功显示可点击 Toast，任务页正打开 Inbox 时立即增量出现。
- `task manager` 继续打开系统任务管理器；空标题、过长、换行、控制字符和歧义输入不会写入任务。
