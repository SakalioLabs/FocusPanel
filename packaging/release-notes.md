# FocusPanel 0.11.52

- 更新、任务、专注和桌面恢复通知在重启后仍能打开对应 Panel 页面。
- 持久化文件只保存五种白名单动作，不保存命令行、路径、参数或委托。
- 未知动作、旧格式和解析异常保持不可执行，不把本地 JSON 变成程序启动入口。
- 动作只有真正绑定后才显示按钮；旧快照仍保留清晰的失效说明。

升级前仍会恢复原生任务栏；现有 SQLite 数据、桌面文件路径、自定义分区和用户设置不会被删除。
