# FocusPanel 0.11.32

- “开始”普通点击直接打开 Panel 自有的完整应用目录，固定项优先并支持搜索、启动和固定。
- 再次点击“开始”只关闭应用目录；Shift+点击和右键菜单保留 Windows 开始菜单兼容入口。
- 紧凑栏“窗口”显示运行窗口数量，窗口总览每行新增可点击的 DWM 实时缩略图按钮。
- 窗口可直接从缩略画面切换或正常关闭，不必恢复原生任务栏寻找缩略项。

升级前仍会恢复原生任务栏；现有 SQLite 数据、桌面文件路径、自定义分区和用户设置不会被删除。
