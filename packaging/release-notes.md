# FocusPanel 0.9.58

- 修复打包应用 AUMID 被误当作普通文件名、从统一应用栏无法启动的问题。
- EXE、快捷方式、Shell 路径和 `shell:AppsFolder` AUMID 现在按真实类型分别启动。
- 应用卸载、固定路径移动或 Shell 拒绝启动时不再导致 Panel 闪退。
- 启动失败会自动回到状态中心，说明原因并引导重新搜索固定。
- 新增无副作用启动请求与异常隔离测试，不启动或操作真实应用。
