# FocusPanel 0.10.1

- 番茄钟历史统计改为后台加载，首次打开计时页不再同步查询 SQLite。
- 倒计时完成后立即提醒，统计记录由后台队列串行保存。
- 开始新一轮、暂停或重置后，上一轮迟到保存结果不会覆盖当前提示。
- 保存失败不会终止后续会话保存，并会在计时状态中明确说明。
- 正常退出前排空已排队的完成会话，减少立即退出造成的记录丢失。
- 保持任务栏、桌面收纳、任务、OKR、AI 和现有数据库内容不变。
