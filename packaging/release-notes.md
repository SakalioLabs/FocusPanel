# FocusPanel 0.11.86

- 每个窗口的右键菜单可直接选择任意已连接显示器，不再受 Panel 所在屏幕限制。
- 目标项显示主屏、分辨率和物理坐标，当前窗口所在屏幕会明确勾选。
- 多窗口应用可一次把全部窗口归拢到指定显示器，单个失败不会中断其他窗口。
- 原“移到 Panel 屏幕”快捷操作继续保留，窗口大小、状态和前台焦点保持不变。

升级前仍会恢复原生任务栏；现有 SQLite 数据、桌面文件路径、自定义分区、自定义图标和用户设置不会被删除。
