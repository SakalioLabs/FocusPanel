# FocusPanel 0.9.78

- 删除、关机、更新、桌面收纳、任务和 OKR 等运行期提示统一改为 FocusPanel Fluent 模态对话框。
- 对话框使用 DWM 原生圆角与毛玻璃、动态深浅色、语义图标和统一按钮，不再突然出现白底直角系统 MessageBox。
- 危险确认使用危险色主操作并默认聚焦安全的“否”；Enter、Esc、关闭和返回值语义保持完整。
- 清理桌面救援、插入图片和打开图片提示中的遗留英文；启动前灾难恢复仍保留系统级兜底。
