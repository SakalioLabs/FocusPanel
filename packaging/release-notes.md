# FocusPanel 0.11.65

- 紧凑栏新增“后台”入口，直接打开 Panel 自有应用与窗口抽屉，不调用原生任务栏隐藏图标。
- 修复桌面收纳项目在增量刷新后丢失自定义 ICO 绑定。
- 图标视图改为按真实宽度实时算列的密排网格，末行左对齐并避免异常单列。
- 窗口菜单新增左右半屏和四角布局，不依赖 Win+Z 或任务栏贴靠浮层。

升级前仍会恢复原生任务栏；现有 SQLite 数据、桌面文件路径、自定义分区、自定义图标和用户设置不会被删除。
