# FocusPanel 0.9.29

- 桌面文件卡片改为超过 Windows 系统拖拽阈值后才开始拖动，避免点击或轻微移动被误判为拖拽。
- 自动滚动只驱动明确命名的桌面收纳主内容区，不再搜索并误用嵌套滚动容器。
- 拖拽期间根据鼠标当前所在位置连续重算滚动方向和速度，越靠近边缘速度越高。
- 鼠标回到中部、离开收纳区、取消拖拽、释放或完成放置时立即停止自动滚动，消除残留上滚。
- 继续使用 GitHub Releases 零配置一键更新，现有任务、收纳数据和固定顺序无需迁移。
