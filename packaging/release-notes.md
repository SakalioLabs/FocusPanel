# FocusPanel 0.10.31

- 设置可精确选择当前连接的任意一台显示器，不再只限“最右侧 / 主屏”。
- 显示器选项标注主屏、分辨率与虚拟桌面坐标，复杂排列也能明确定位。
- Panel、12px 热区和 3px 指示条始终使用同一设备标识并整体迁移。
- 已选显示器断开时临时回退主屏，保留选择；重新连接后可继续使用。
- Setup 改由 Windows Installer 强制使用所选目录；有可用非系统盘时默认不再推荐 C 盘，已有 C 盘安装也可确认后迁移。
- 自动整理与新增图标收纳增加完整异步异常边界，单项失败不再导致 Panel 退出。
