# FocusPanel 0.11.25

- 在窗口总览行或应用窗口菜单中右键，可直接置顶或取消置顶目标窗口。
- 窗口列表副标题和读屏名称会显示真实“已置顶”状态，菜单使用勾选项反馈当前值。
- 操作只改变窗口层级，不移动、不缩放、不激活窗口，也不写入业务数据库。
- 无响应应用不会阻塞 Panel；Windows 拒绝操作时继续通过状态中心明确提示。

升级前仍会恢复原生任务栏；现有 SQLite 数据、桌面文件路径、自定义分区和用户设置不会被删除。
