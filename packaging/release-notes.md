# FocusPanel 0.10.69

这一版把常用窗口状态操作直接放进统一应用栏。

- 右键应用后可对每个窗口直接最小化、最大化或还原。
- 菜单按窗口当前状态隐藏无意义动作。
- 单窗口和多窗口应用使用同一套操作。
- 操作后读取真实窗口状态，不把发出命令误报为成功。
- 状态变化原位同步，不重建任务栏按钮或跳动列表。
- 使用 Windows 公开 ShowWindow，不强杀进程或注入应用。

升级前仍会恢复原生任务栏，现有 SQLite 数据、桌面文件与用户设置保持不变。
