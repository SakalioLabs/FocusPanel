# FocusPanel 0.9.96

- 运行窗口枚举改为最后有效快照提交，系统瞬时失败不会让统一应用栏整批消失。
- 单个受保护窗口的进程、身份或图标读取失败只降级该项目，不中断其他应用。
- 应用身份读取异常使用隔离临时身份，进程 ID 缺失时按窗口句柄区分，仍可切换且不会错误合并。
- 窗口快照按订阅者隔离发布，一个监听器异常不会导致 Panel 退出。
- 退出后的 WinEvent 和 Dispatcher 关闭竞态会静默丢弃，不再产生迟到刷新。
- Release 构建 0 错误、0 警告，全量 559 项测试和界面冒烟通过。
