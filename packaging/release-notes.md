# FocusPanel 0.11.42

- “运行…”改为 Panel 统一搜索中的显式 `>` 模式，支持引号路径、参数和环境变量，不再唤起 Win+R 或经过 `cmd.exe`。
- 收纳卡片右键可选择并持久保存自己的 `.ico`，已有快捷方式自定义图标在收纳前固化，属性刷新和重启后继续显示。
- 图标收纳盒按完整可用宽度布局，修复偶发一行只有一个图标和异常大空隙。
- 状态中心只打开 Panel 自有后台应用与窗口，不提供会从原生任务栏弹出的“显示隐藏图标”入口。

升级前仍会恢复原生任务栏；现有 SQLite 数据、桌面文件路径、自定义分区和用户设置不会被删除。
