# FocusPanel 0.10.85

这一版让状态中心第一屏直接承担调音量和调亮度。

- 主音量滑块紧跟在顶部常用动作之后。
- 内置显示器亮度滑块与主音量并列为首屏控制。
- 不再需要越过网络扫描和应用混音器寻找两个高频滑块。
- 网络、Wi‑Fi、媒体、电池和应用音量详情完整保留在下方。
- 原有串行写入、硬件能力检测和失败回滚保持不变。

升级前仍会恢复原生任务栏，现有 SQLite 数据、桌面文件与用户设置保持不变。
