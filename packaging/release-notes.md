# FocusPanel 0.10.88

这一版让紧凑栏入口不用猜，并明确显示当前状态。

- 开始、搜索、Focus 和状态按钮直接显示短标签，不再只有图标。
- 五个固定入口统一为 54px 点击区，仍保持 76px 紧凑栏宽度。
- 当前打开入口显示左侧强调色状态线，关闭后立即消失。
- Focus 指示同时覆盖设置子表面，状态指示同时覆盖电源子表面。
- 应用列表仍在中部独立滚动，不改变固定顺序和窗口语义。

升级前仍会恢复原生任务栏，现有 SQLite 数据、桌面文件与用户设置保持不变。
