# FocusPanel 0.11.21

- 紧凑栏原“搜索”入口改为明确的“窗口”入口，普通点击一步展示全部窗口。
- 点击窗口行直接切换；右侧新增关闭按钮，连续关闭时总览保持打开。
- 关闭仍只发送正常 `WM_CLOSE`，不会强杀应用进程。
- 窗口按钮二次点击只关闭弹层，不清空当前分类或查询。
- 全局快速搜索快捷键始终进入“全部”范围，保留完整应用、任务、命令和计算搜索。

升级前仍会恢复原生任务栏；现有 SQLite 数据、桌面文件路径、自定义分区和用户设置不会被删除。
