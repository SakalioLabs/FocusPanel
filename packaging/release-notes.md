# FocusPanel 0.9.36

- 修复设置页恢复数据库时新进程被单实例锁拦截、实际没有执行恢复的问题。
- 当前实例先恢复原生任务栏并完整退出，交接进程等待锁释放后再启动恢复实例。
- 备份跨两个目录按时间排序并逐个执行 SQLite 完整性检查；损坏的最新备份会自动回退到更早的有效文件。
- 启动备份使用 SQLite 在线备份 API，保留 WAL 中已提交数据，并生成不依赖 WAL/SHM 的独立备份。
- 全部备份无效时不会覆盖当前数据库；不迁移业务表，不改变任务、桌面收纳、固定应用和设置数据。
