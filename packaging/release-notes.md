# FocusPanel 0.11.92

- 任务栏运行数量和刷新提示只统计真实可切换窗口，不再计入无窗口后台宿主。
- 紧凑栏移除全量后台进程数字角标，默认界面不再呈现任务管理器式杂项数量。
- 固定应用、普通窗口和最小化窗口继续正常显示；完整后台数据仅在主动打开独立抽屉后出现。

升级前仍会恢复原生任务栏；现有 SQLite 数据、桌面文件路径、自定义分区、自定义图标和用户设置不会被删除。
