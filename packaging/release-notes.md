# FocusPanel 0.10.67

这一版继续补齐统一应用栏的原生任务栏操作，加入可靠的管理员启动。

- 对桌面应用按 `Ctrl+Shift+左键` 可直接请求管理员启动。
- 右键应用图标可选择“以管理员身份运行”。
- UAC 取消会被识别为取消，不再显示成启动失败或闪退。
- 普通左键、Shift 新实例、中键新实例和多窗口管理保持不变。
- 商店应用和无可靠路径的进程不会伪装支持管理员启动。
- 使用 Windows 公开 Shell `runas`，不保存凭据、不绕过 UAC。

升级前仍会恢复原生任务栏，现有 SQLite 数据、桌面文件与用户设置保持不变。
