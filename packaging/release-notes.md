# FocusPanel 0.11.49

- 没有可验证的空窗口区域或 DWM cloak 时不再假报任务栏接管成功。
- Explorer 首次重建或抑制层丢失时允许一次受控修复，再次失效立即安全恢复。
- 修复过程不重复写工作区、不结束 Explorer，也不会形成隐藏/显示循环。
- 写入成功但回读失败时仍会精确撤销已经发生的任务栏表面变更。

升级前仍会恢复原生任务栏；现有 SQLite 数据、桌面文件路径、自定义分区和用户设置不会被删除。
