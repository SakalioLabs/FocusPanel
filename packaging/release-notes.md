# FocusPanel 0.9.93

- 运行应用的前台状态、窗口标题、窗口数量和固定状态现在原位同步到已有任务栏项目。
- 前台窗口切换不再触发应用集合 Replace，不会销毁并重建对应按钮、图标和状态条，统一应用栏显示更稳定。
- 状态摘要、工具提示、读屏名称和多窗口列表继续跟随最新窗口快照更新。
- 不同 AppUserModelID 或可执行路径继续严格隔离，不会错误合并同名应用。
- Release 构建 0 错误、0 警告，全量 535 项测试和界面冒烟通过。
