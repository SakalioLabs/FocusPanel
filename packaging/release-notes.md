# FocusPanel 0.9.87

- 桌面收纳的视图选项、新建收纳盒、收纳盒操作和修复工具现在始终只允许一个 Popup 打开。
- 打开新弹层时先关闭旧弹层，并保持 transient-interaction 计数平衡，避免 Panel 提前自动收起或永久保持展开。
- Popup 打开后焦点自动进入首个可操作控件，键盘无需绕过悬浮内容继续 Tab。
- 在独立 Popup 内按 `Esc` 会关闭当前弹层、同步复位触发按钮，并把焦点返回原入口。
- 切换模块或卸载桌面收纳页面时主动清理活动弹层；新增互斥时序与键盘契约测试，全量 511 项测试通过。
