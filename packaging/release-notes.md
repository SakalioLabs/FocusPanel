# FocusPanel 0.10.44

- 自定义安装器同时识别 Velopack 名称注册项与 MSI GUID 注册项，并直接验证所选目录中的真实 `FocusPanel.exe`；非系统固定盘只需 512MB 可用空间即可成为默认推荐，C 盘紧张时可直接选择 D/E 盘。
- 桌面自动整理改为整批只提交一次 WPF 文件集合和布局快照，文件属性、SQLite 状态与失败恢复仍逐项完成，避免大量图标收纳期间因逐项刷新和文件监视重入导致 Panel 闪退。
- 手动自动整理不再重复执行第二次全量桌面扫描；任务、固定应用、收纳记录、设置和更新数据格式保持不变。
