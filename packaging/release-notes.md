# FocusPanel 0.9.52

- 音频状态不再把设备读取失败伪装成“音量 0%、未静音”。
- Core Audio 默认端点、激活、读取和写入都会检查 HRESULT。
- 音量或静音写入失败时立即恢复最后确认值，并在状态中心显示原因。
- 没有默认输出设备时停用音量控件；设备恢复后自动重新启用。
- 紧凑栏滚轮仅在音量写入成功后取消静音，不再产生半成功状态。
- 新增音量成功、拒绝、异常和不可用状态测试，不访问真实音频设备。
