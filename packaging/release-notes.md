# FocusPanel 0.11.81

- 修复部分正在运行的应用未出现在 Panel 任务栏的问题，带原生 `APPWINDOW` 标记的窗口不再被误删。
- 启动后和 Panel 重新出现时主动刷新运行应用；应用区为空时提供可见的“刷新”入口。
- 顶部直接显示当前“位置 · 上 / 中 / 下”，单击即可选择右上、右中或右下，拖动与多屏记忆继续保留。
- 仍只使用公开窗口信息，不读取原生任务栏或 Explorer 的私有数据。

升级前仍会恢复原生任务栏；现有 SQLite 数据、桌面文件路径、自定义分区、自定义图标和用户设置不会被删除。
