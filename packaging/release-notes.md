# FocusPanel 0.11.2

这一版统一展开工作区顶部导航，让用户始终知道自己位于哪个模块。

- 概览、桌面收纳、任务、番茄钟、AI 和设置统一改为单层 Fluent 分段导航。
- 当前模块使用唯一的强调表面、描边和底部指示线，不再六个按钮看起来完全一样。
- 打开设置时只高亮设置，不会同时把背后的桌面收纳或任务标为当前页。
- 关闭设置后自动恢复此前模块的选中状态，不改变用户最后使用的工作区。
- 复用项目已有主题控件，圆角、悬停、按下和键盘焦点反馈保持一致。

升级前仍会恢复原生任务栏，现有 SQLite 数据、桌面文件与用户设置保持不变。
