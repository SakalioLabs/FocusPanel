# FocusPanel 0.9.31

- 统一应用列表在固定或运行应用超出可视高度时显示轻量上下导航，不再只能依赖隐藏的滚动条。
- 导航箭头仅在对应方向仍有应用时悬浮出现，不挤压列表高度；单次点击按一个应用图标步长移动，鼠标滚轮仍可直接滚动。
- 六个固定入口及应用排序、点击、右键、多窗口和拖动固定语义保持不变。
- 继续保留 0.9.30 的隐藏态资源优化、任务栏安全恢复和 GitHub Releases 一键更新。
- 不迁移数据库，不改变现有任务、桌面收纳、固定应用和设置数据。
