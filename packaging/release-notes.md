# FocusPanel 0.10.81

这一版把可靠的虚拟桌面操作直接放进 FocusPanel 开始中心。

- 一步切换到上一个或下一个虚拟桌面，无需先打开任务视图。
- 可直接新建虚拟桌面，并显示对应 Windows 快捷键提示。
- 关闭当前虚拟桌面前要求确认，明确说明应用不会退出，窗口会移到相邻桌面。
- 所有入口保持 44px 点击区、读屏名称和统一 Fluent 图标样式。
- 操作失败继续使用现有状态反馈，不静默吞掉错误。

升级前仍会恢复原生任务栏，现有 SQLite 数据、桌面文件与用户设置保持不变。
