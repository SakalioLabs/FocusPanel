# FocusPanel 0.10.0

- 固定应用改为线程安全内存快照，切换窗口不再重复查询 SQLite。
- 启动时固定项在后台载入，数据库较慢也不会阻塞 Panel 构造和呼出。
- 固定、取消固定和排序成功后立即同步缓存，顺序保持稳定。
- 旧应用目录索引不能覆盖用户刚完成的固定或排序操作。
- SQLite 临时繁忙时保留最后有效固定项，不会让应用栏整批消失。
- 保持任务栏窗口分组、桌面收纳、任务、番茄钟、OKR、AI 和现有数据库内容不变。
