# FocusPanel 0.10.64

这一版继续让 Panel 在原地完成任务栏需要跳转二级面板的高频操作。

- 状态中心新增应用级音量混音器。
- 每个正在使用默认输出设备的应用都能独立调音量或静音。
- 活动会话优先，系统声音与受保护进程都有清晰名称降级。
- 连续拖动只提交最后值，不同应用严格串行，不阻塞界面。
- 会话结束或设备切换时回滚并刷新，不让旧快照覆盖滑块。
- 使用 Windows 公开 Core Audio 会话接口，不读取播放器私有数据。

升级前仍会恢复原生任务栏，现有 SQLite 数据、桌面文件与用户设置保持不变。
