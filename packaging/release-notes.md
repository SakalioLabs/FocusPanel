# FocusPanel 0.11.33

- Panel 会跟随当前 Windows 虚拟桌面，不再在切换后留在旧桌面。
- 常驻紧凑栏自动迁移；右缘、全局快捷键和托盘唤出也会先迁移再显示。
- 窗口总览新增上一个、新建、下一个和安全关闭虚拟桌面四个直接入口。
- 使用 Windows 公开虚拟桌面管理接口；接口不可用时保持原状，不闪退、不循环移动。

升级前仍会恢复原生任务栏；现有 SQLite 数据、桌面文件路径、自定义分区和用户设置不会被删除。
