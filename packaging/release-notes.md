# FocusPanel 0.11.40

- `Shift+点击/Enter` 开始入口现在打开 Panel 统一搜索，不再唤起 Windows 开始菜单。
- 右键开始菜单删除 Windows 开始和重复搜索项，状态服务与快捷键映射同时移除两条调用链。
- 焦点在紧凑栏任意按钮时可直接输入，自动打开统一搜索且不丢首字符。
- 编辑框、组合框、菜单和 Ctrl/Alt/Windows 命令组合不会被直接输入功能截获。

升级前仍会恢复原生任务栏；现有 SQLite 数据、桌面文件路径、自定义分区和用户设置不会被删除。
