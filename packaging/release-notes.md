# FocusPanel 0.9.79

- 修复 Fluent 模态对话框打开后，背后的 Panel 可能因失去前台焦点而在 220ms 后自动收起的问题。
- 对话框生命周期接入现有临时交互锁；无论确认、取消、关闭或异常，锁都会成对释放，不会残留为永久展开。
- 应用搜索整行按钮和“最近使用”入口补齐动态读屏名称，复合图标按钮不再成为无名称控件。
- OKR 状态图标和任务数量徽标改用统一控件圆角令牌，新增契约阻止页面重新写入任意圆角值。
