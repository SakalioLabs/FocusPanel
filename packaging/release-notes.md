# FocusPanel 0.11.13

- AI 助手默认提供 DeepSeek 接入，同时继续支持已有 OpenAI 配置。
- DeepSeek 与 OpenAI API Key 独立加密存储，旧 OpenAI Key 不会自动迁移或混用。
- 桌面收纳保留本地按类型与 AI 智能分区两种方式，手动分区始终优先。
- 智能分区只发送文件名、扩展名和项目类型，不上传完整路径或文件内容；失败时自动回退本地规则，不影响收纳事务。

升级前仍会恢复原生任务栏；现有 SQLite 数据、桌面文件路径、自定义分区和用户设置不会被删除。
