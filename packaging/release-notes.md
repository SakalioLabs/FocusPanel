# FocusPanel 0.9.15

- 修复替代模式关闭却没有明确提示、导致用户误以为任务栏隐藏失败的问题。
- `Shell_TrayWnd` 隐藏现在会验证最终可见状态，失败时显示具体原因并安全回滚。
- 新增面板内系统控制中心：直接调节 Core Audio 音量、静音，查看真实网卡、IPv4、电池状态，并执行锁定、睡眠。
- 网络、通知和输入法仅唤起 Windows 原生快捷浮窗，不再失败后胡乱跳转设置页。
