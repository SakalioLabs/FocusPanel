# FocusPanel 0.10.49

- 应用图标右键菜单新增 Windows 原生 Jump List“最近项目”，最多 8 项直接铺开，打开常用文档无需先进入应用。
- 运行窗口优先使用系统返回的原始 AppUserModelID；应用目录另外保留快捷方式和打包应用的精确身份，不按显示名称猜测或错误合并。
- 最近项目在可取消的 STA 后台线程按需读取；关闭菜单、更新或退出会丢弃迟到结果，读取失败不影响现有启动、固定和窗口管理操作。
