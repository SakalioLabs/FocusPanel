# FocusPanel 0.11.75

- 右键紧凑栏顶部位置把手，可直接选择 Panel 所在屏幕，不必进入设置页。
- 菜单实时显示自动最右、Windows 主屏和所有具体显示器，并勾选当前目标。
- 切换复用现有物理坐标、Per-Monitor DPI、热区和持久化链，不复制定位逻辑。
- 显示器断开回退与重新连接恢复语义保持不变，现有应用和用户数据不受影响。

升级前仍会恢复原生任务栏；现有 SQLite 数据、桌面文件路径、自定义分区、自定义图标和用户设置不会被删除。
