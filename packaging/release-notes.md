# FocusPanel 0.11.53

- 紧凑栏状态入口直接显示 Panel 通知未读徽标，最多显示 99+。
- 工具提示和读屏名称同步播报准确未读数量。
- Shift+点击或 Shift+Enter 一步打开通知详情；再次执行只收起详情，不关闭整个 Panel。
- 普通状态中心、滚轮音量、中键媒体和右键菜单行为保持不变。

升级前仍会恢复原生任务栏；现有 SQLite 数据、桌面文件路径、自定义分区和用户设置不会被删除。
