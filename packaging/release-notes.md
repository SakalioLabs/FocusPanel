# FocusPanel 0.10.43

- 搜索、Focus 中心、状态中心、月历、设置和电源弹层统一键盘焦点：打开后进入首个有效内容，按 `Esc` 返回原紧凑栏入口，不再把焦点留在已隐藏元素上。
- 时间入口右键或按 `Shift+F10` 可直接打开 Windows“调整日期和时间”与“通知设置”，继续使用微软公开的 `ms-settings:` URI，不读取 Explorer 私有结构。
- 弹层焦点调度会检查窗口生命周期、可见性与可用状态；退出、自动收起和快速切换时不会执行迟到焦点请求。
