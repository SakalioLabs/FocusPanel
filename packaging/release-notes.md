# FocusPanel 0.10.62

- 原生任务栏接管在空窗口区域之外新增公开 `DWMWA_CLOAK` 合成隐藏层，鼠标触底或 Explorer 重新设置逻辑可见位时不再露出原生任务栏表面。
- 启用前保存原始 DWM app-cloak 位；正常退出、更新、异常、紧急快捷键与 watchdog 均精确还原。任一层失败都会取消接管或安全恢复，不循环隐藏、不结束 Explorer。
- 自动测试通过替代 Win32/DWM 边界覆盖启用、原状态恢复、失败回滚、守护只读、损坏会话兜底和恢复重试；1112 项测试与 UI 冒烟通过，不操作开发机真实任务栏。
