# FocusPanel 0.11.45

- “网络与无线”新增蓝牙设备列表，可直接查看已连接、已配对、在附近和可配对设备。
- 支持在 Panel 内发起配对或移除设备，不打开任务栏蓝牙浮层；首次配对仅保留 Windows 必需的安全确认。
- 经典蓝牙与低功耗端点按设备身份去重，已连接和已配对设备优先显示。
- 配对取消、权限拒绝、验证失败、设备拒绝和离开范围均会明确提示，不显示虚假成功。

升级前仍会恢复原生任务栏；现有 SQLite 数据、桌面文件路径、自定义分区和用户设置不会被删除。
