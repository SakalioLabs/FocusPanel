# FocusPanel 0.11.68

- 新增 `Ctrl + Alt + W` 全局窗口总览，冲突时自动使用 `Ctrl + Shift + W`。
- 快捷键从任意应用一步进入 Panel 全部窗口，不发送 Win+Tab，也不显示原生任务栏。
- 方向键选择窗口，Enter 切换，Delete 正常关闭，Esc 返回；空列表自动聚焦搜索框。
- 设置页显示本次真正生效的组合，注册失败时保留紧凑栏“窗口”入口。

升级前仍会恢复原生任务栏；现有 SQLite 数据、桌面文件路径、自定义分区、自定义图标和用户设置不会被删除。
