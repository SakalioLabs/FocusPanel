# FocusPanel 0.9.45

- 搜索结果和统一任务栏现在共用同一套应用图标呈现，不再出现一处有图标、另一处空白的状态。
- Windows Shell 无法读取快捷方式、打包应用或受保护进程图标时，会显示应用名称首字符组成的 Fluent 圆角占位。
- 中文、英文和数字名称均可稳定识别；前导标点会被跳过，空名称使用一致的通用占位。
- 真实图标仍保持高质量缩放，降级表面复用现有主题与圆角，不增加多层边框。
- 新增图标降级单元测试、界面契约和真实 WPF 组件加载检查，应用与测试项目保持 0 错误、0 警告。
