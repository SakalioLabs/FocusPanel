# FocusPanel 0.11.24

- 多屏环境下，在窗口总览行或应用窗口菜单中右键，可把目标窗口直接移到 Panel 所在屏幕。
- 该动作只在窗口位于其他显示器时出现，不增加紧凑栏常驻按钮。
- 移动保持窗口大小与相对位置；目标屏较小时自动限制在可用工作区内。
- 最大化、最小化状态和当前输入焦点保持不变，失败时在状态中心明确提示。

升级前仍会恢复原生任务栏；现有 SQLite 数据、桌面文件路径、自定义分区和用户设置不会被删除。
