# FocusPanel 0.9.25

- 软件更新来源可在 GitHub Releases、局域网 HTTP 地址和 Windows 共享目录之间切换，并按设备记忆。
- 局域网中的其他设备配置一次后，即可继续使用“一键检查并安装更新”完成下载、任务栏安全恢复和重启安装。
- 新增 `publish-lan-update.ps1`，按“更新包优先、清单最后”的顺序发布文件，避免客户端发现未复制完整的版本。
- 设置面板支持低高度滚动，并显示当前实际生效的更新来源。
- 保留 GitHub 默认更新源，现有安装和业务数据库无需迁移。
