# FocusPanel 0.11.77

- 短屏布局由八个固定按钮自动收束为六个，给运行应用与滚动控制保留真实空间。
- 后台入口并入状态中心；收纳和任务合并为 Focus 中心，所有能力仍可达。
- Focus 中心会打开最近使用模块，展开后可从顶部导航一步切换全部主要功能。
- 普通高度继续显示完整八入口，用户数据、固定应用和窗口顺序不受影响。

升级前仍会恢复原生任务栏；现有 SQLite 数据、桌面文件路径、自定义分区、自定义图标和用户设置不会被删除。
