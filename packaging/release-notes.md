# FocusPanel 0.10.74

这一版纠正紧凑栏功能入口的二次点击语义。

- Panel 未展开时，搜索、Focus 中心、状态中心和时间入口第一次点击仍会展开并立即打开内容。
- 对应功能区已打开时，第二次点击只关闭该功能区，Panel 保持展开。
- 设置和电源子页分别归属 Focus 中心与状态中心，点击所属入口会关闭子页，不会切错内容。
- 鼠标离开后仍按原有自动收起策略工作；按钮二次点击不再主动折叠整个侧栏。

升级前仍会恢复原生任务栏，现有 SQLite 数据、桌面文件与用户设置保持不变。
