# FocusPanel 0.10.83

这一版让开始中心和搜索入口不再依赖猜图标、猜能力。

- 虚拟桌面操作直接显示“上一个、新建、下一个、关闭”。
- 对应 Windows 快捷键继续显示在工具提示中。
- 关闭当前虚拟桌面的二次确认与应用保护保持不变。
- 开始中心说明明确包含虚拟桌面能力。
- 放大镜入口统一命名为“统一搜索”，明确支持应用、窗口、任务、系统命令和计算式。

升级前仍会恢复原生任务栏，现有 SQLite 数据、桌面文件与用户设置保持不变。
