# FocusPanel 0.10.3

- 桌面收纳分区与偏好改为后台快照读取，文件变化不再同步阻塞 Panel。
- 多次监控通知和拖拽刷新自动合并，保持分区卡片与滚动位置稳定。
- 图标缩放、视图模式和自动整理开关合并为串行后台保存。
- 快速连续调整只写入最终状态，退出前排空已排队的设置。
- 数据库暂时繁忙时保留最后有效收纳布局，不显示成空白页面。
- 保持文件原始路径、隐藏属性、分区数据和拖入拖出语义不变。
