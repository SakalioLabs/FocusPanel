# FocusPanel 0.11.79

- 设置页新增“一键检查接管状态”，分别核对宿主、边缘呼出、工作区和抑制层。
- 正常时明确显示窗口区域、DWM 或双层保护；异常时指出具体失效环节。
- 检查严格只读并在后台执行，不写任务栏、不自动修复，也不会造成闪烁循环。
- 成功接管后自动检查一次，也可随时手动复查。

升级前仍会恢复原生任务栏；现有 SQLite 数据、桌面文件路径、自定义分区、自定义图标和用户设置不会被删除。
