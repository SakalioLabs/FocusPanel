# FocusPanel 0.11.31

- 状态中心直接列出已安装输入法、当前项、完整名称与语言信息。
- 选择输入法会应用到刚才使用的窗口，并读取目标线程布局确认结果，不显示假成功。
- 网络、应用音量、媒体和输入法组成四个互斥 Panel 原生详情入口。
- Win+Space 输入法浮层仅保留为失败时的兼容回退。

升级前仍会恢复原生任务栏；现有 SQLite 数据、桌面文件路径、自定义分区和用户设置不会被删除。
