# FocusPanel 0.11.28

- 删除会唤起原生任务栏的“显示隐藏图标”入口，改为 Panel 自有后台与窗口总览。
- 收纳后的 Windows 快捷方式继续显示用户配置的自定义 `.ico`，文件变化后会刷新图标缓存。
- 图标模式使用全宽分区和自适应均匀网格，不再被左右双列压成一行一个图标。
- 列表模式及既有分区、文件路径、隐藏状态和数据库内容保持不变。

升级前仍会恢复原生任务栏；现有 SQLite 数据、桌面文件路径、自定义分区和用户设置不会被删除。
