# FocusPanel 0.11.6

这一版纠正紧凑栏角标的含义，让状态出现在真正所属的入口。

- “收纳”入口不再用软件更新圆点冒充收纳状态。
- 收纳入口直接显示当前已收纳项目数量，超过 99 个稳定显示 `99+`。
- 工具提示和读屏名称继续播报真实完整数量，不因视觉截断丢失信息。
- 隐藏、恢复、自动整理和文件刷新后通过现有文件事件增量更新角标，不新增后台轮询。
- 软件更新圆点迁到展开工作区的“设置”，更新提醒与安装入口现在位于同一语义位置。

升级前仍会恢复原生任务栏，现有 SQLite 数据、桌面文件与用户设置保持不变。
