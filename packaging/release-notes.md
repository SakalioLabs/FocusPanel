# FocusPanel 0.9.99

- 待办数量和日历专注摘要改为后台查询，打开日历与翻月不再卡住 Panel。
- 每轮查询使用独立数据库上下文，只读取需要的日期和分钟数。
- 快速切换月份时旧结果不会覆盖当前日历，重复请求自动合并。
- SQLite 临时繁忙时保留最后有效摘要，不再突然显示 0 项。
- 退出后取消迟到的界面提交，不触碰已释放绑定。
- 保持任务、番茄钟、桌面收纳、OKR、AI 和现有数据库内容不变。
