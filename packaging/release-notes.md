# FocusPanel 0.10.86

这一版让统一搜索的隐藏能力打开就能看懂。

- 空查询显示任务管理器、音量、专注和新建任务四个示例。
- 点击示例只填入文本，不会自动执行任何操作。
- 搜索框保持焦点，光标停在末尾便于继续编辑。
- 输入后示例自动隐藏，不挤占结果列表空间。
- 固定应用与原有搜索排序保持不变。

升级前仍会恢复原生任务栏，现有 SQLite 数据、桌面文件与用户设置保持不变。
