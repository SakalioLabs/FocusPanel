# FocusPanel 0.11.23

- 在窗口总览中右键任意窗口行，可直接切换、最小化、最大化、还原或正常关闭。
- 菜单根据当前窗口状态只显示有效动作，避免无意义和互相冲突的选项。
- 菜单打开期间 Panel 保持展开，并停止该窗口的悬停预览；菜单关闭后恢复正常自动收起。
- 切换窗口后同步关闭窗口总览；关闭仍只发送正常 `WM_CLOSE`，不会强制结束进程。

升级前仍会恢复原生任务栏；现有 SQLite 数据、桌面文件路径、自定义分区和用户设置不会被删除。
