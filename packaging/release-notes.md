# FocusPanel 0.11.72

- 紧凑任务栏顶部新增位置把手：单击循环右上、右中、右下，上下拖动后自动吸附。
- 拖动使用目标屏幕物理坐标，支持多屏负坐标和不同 DPI，移动过程中不会自动收起。
- 右缘 3px 指示条与 Panel 高度和固定位置同步，不再始终铺满整块屏幕。
- 设置页和右键位置菜单继续保留，三种入口共享同一个持久化位置设置。

升级前仍会恢复原生任务栏；现有 SQLite 数据、桌面文件路径、自定义分区、自定义图标和用户设置不会被删除。
