# FocusPanel 0.9.41

- AI 助手由英文占位页升级为完整中文 Fluent 对话工作区。
- 接入 OpenAI Responses API，支持模型选择、快速提示、停止请求、中文错误和多轮上下文。
- API Key 使用 Windows DPAPI 按当前用户加密，保存后不回显、不写日志。
- 本地任务、OKR 与专注摘要默认关闭；主动授权后也不读取文件内容、路径或凭据。
- AI 第一版保持只读，不直接修改任务、OKR 或执行系统操作。
- 新增无真实网络和无真实业务数据库写入的自动测试；不迁移业务数据。
