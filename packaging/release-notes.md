# FocusPanel 0.10.94

这一版让多窗口应用切换更接近 Windows 任务栏的肌肉记忆。

- Ctrl+点击多窗口应用可直接循环到下一个窗口。
- 普通点击仍打开文字窗口列表，便于精确选择。
- 连续点击沿用刚刚切换的窗口，避免前台快照延迟造成跳回。
- Ctrl+滚轮继续双向循环，普通滚轮仍只滚动应用栏。
- 工具提示与读屏说明改为真实的 Ctrl 手势，不再误导。

升级前仍会恢复原生任务栏，现有 SQLite 数据、桌面文件与用户设置保持不变。
