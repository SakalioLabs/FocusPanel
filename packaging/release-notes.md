# FocusPanel 0.10.96

这一版先纠正 Panel 最基础的可发现性：新安装不再要求用户寻找屏幕右缘热区，76px 紧凑栏默认保持可见。

- 首次引导直接展示“始终显示 76px 紧凑栏（推荐）”，默认开启并保存选择。
- 开始、统一搜索、应用任务栏、Focus、状态和时间无需触边即可找到。
- 工作区仍按需展开，鼠标离开后折回紧凑栏，不会整条消失。
- 旧版本升级继续尊重已有偏好；没有新偏好键的老用户保持旧自动隐藏行为。
- 配置读取失败时仍采用安全默认，不会擅自覆盖用户设置。

升级前仍会恢复原生任务栏，现有 SQLite 数据、桌面文件与用户设置保持不变。
