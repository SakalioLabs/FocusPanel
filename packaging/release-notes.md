# FocusPanel 0.10.90

这一版让右缘悬停呼出的灵敏度适合不同鼠标和多屏习惯。

- 设置页新增灵敏、标准、稳妥和刻意四档右缘呼出速度。
- 选择立即更新现有后台监测器，不重建窗口或抢夺焦点。
- 修改时重置正在进行的停留，避免切档瞬间误触发。
- 12px 感应带、30ms 后台采样、32px 复位和全屏抑制保持不变。
- 选择写入现有 Shell 偏好，异常配置自动回到标准 100ms。

升级前仍会恢复原生任务栏，现有 SQLite 数据、桌面文件与用户设置保持不变。
