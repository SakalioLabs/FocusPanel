# FocusPanel 0.10.27

- 安全退出改为两阶段异步关闭，不再阻塞 WPF Dispatcher。
- 任务、桌面布局、番茄钟、开机启动和 Shell 设置会完整排空后退出。
- 固定应用数据库写入也通过异步闸门完成，不丢失最后一次排序。
- 原生任务栏优先恢复；重复退出请求只等待同一关闭事务。
- 主安装器现在直接提供目录选择，不再要求另找 CustomSetup。
- 双屏混合缩放时先读取目标显示器 DPI，展开始终锚定最外侧右缘。
