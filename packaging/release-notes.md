# FocusPanel 0.11.83

- 每次打开状态中心都会自动刷新任务栏接管健康，不必进入设置页手动检查。
- 检查期间两个入口统一显示“正在检查…”并禁用，重复请求只合并、不并行执行。
- 停用、安全恢复或更新前恢复后，在途旧结果不会覆盖当前状态。
- 检查异常转为可读提示，不会因后台诊断异常导致 Panel 闪退。

升级前仍会恢复原生任务栏；现有 SQLite 数据、桌面文件路径、自定义分区、自定义图标和用户设置不会被删除。
