# FocusPanel 0.10.95

这一版把四个高频 Windows 操作直接接进统一搜索，减少离开 Panel 寻找系统入口的步骤。

- 输入“截图”或“截屏”即可打开 Windows 屏幕剪辑。
- 输入“声音输出”或“音量混合器”即可打开输出设备面板。
- 输入“投影”或“扩展屏”即可选择多屏显示方式。
- 输入“无线显示器”或“投屏”即可打开连接面板。
- 所有入口使用 Windows 公开快捷键语义，失败时提供明确状态反馈。

升级前仍会恢复原生任务栏，现有 SQLite 数据、桌面文件与用户设置保持不变。
