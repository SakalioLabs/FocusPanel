# FocusPanel 0.11.22

- 窗口总览行悬停约 420ms 后显示由 Windows DWM 提供的实时缩略画面。
- 点击缩略画面直接切换窗口；受保护窗口不允许预览时仍保留文字入口。
- 实时预览不截屏、不轮询窗口、不抢键盘焦点，并复用现有 DPI 与透明效果降级链。
- 窗口列表获得焦点后可按 `Delete` 正常关闭当前焦点窗口。
- 关闭弹层、切换功能、收起或退出时会停止预览并释放 DWM 资源。

升级前仍会恢复原生任务栏；现有 SQLite 数据、桌面文件路径、自定义分区和用户设置不会被删除。
