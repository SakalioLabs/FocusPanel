# FocusPanel 0.9.98

- 音频、网络、输入法和电池状态改为后台读取，设备枚举不会再卡住悬停呼出与 Panel 动画。
- 刷新请求自动合并，同一时刻只有一轮采样，不会每 2 秒堆积后台任务。
- Core Audio 后台采样使用独立 COM 生命周期，避免跨线程复用音频设备对象。
- 用户调节音量或静音时，迟到的旧快照不会把控件回滚。
- Panel 退出后会取消在途界面提交，不再触碰已释放绑定。
- 保持任务栏、桌面收纳、任务、番茄钟、OKR、AI 和现有数据库不变。
