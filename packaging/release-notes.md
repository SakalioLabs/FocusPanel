# FocusPanel 0.9.30

- Panel 隐藏时暂停全局窗口事件触发的完整窗口枚举、应用身份解析和任务栏集合刷新。
- 再次唤出时立即重新获取窗口快照，固定/运行应用、活动状态和多窗口列表不会使用过期数据。
- 时钟只在 Panel 可见时每秒更新；网络、音量、电池仅在状态中心打开时刷新，任务摘要仅在日历打开时刷新。
- 右缘热区、全屏抑制、安全恢复 watchdog 和 GitHub Releases 更新检查保持运行，不牺牲呼出与恢复能力。
- 继续保留现有任务、桌面收纳、固定应用和设置数据，无需数据库迁移。
