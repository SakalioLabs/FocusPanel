# FocusPanel 0.9.89

- 未处理异常现在先恢复原生任务栏和桌面图标，再记录日志并安全退出，不再在未知损坏状态中继续运行。
- 删除崩溃路径中删错数据库和把程序集 DLL 当程序启动的旧逻辑；运行期异常不会删除、覆盖或自动重建业务库。
- 崩溃日志迁移到 `%LOCALAPPDATA%\FocusPanel\Logs\crash.log`，安装目录只读时仍可诊断。
- 启动恢复只有在原数据库成功归档后才允许创建新库；归档失败或恢复库仍不兼容时保持所有文件不动。
- 移除无条件 `EnsureDeleted()`，后台线程致命异常也不再从非 UI 线程弹窗。
- Release 构建 0 错误、0 警告，全量 521 项测试和界面冒烟通过。
