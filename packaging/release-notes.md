# FocusPanel 0.9.34

- 键盘浏览紧凑栏和工作区时显示统一的 2px Fluent 圆角焦点环，当前入口清晰可见。
- 焦点环只在键盘导航时出现，鼠标点击不会留下常驻矩形边框，也不会叠加第二层卡片表面。
- 按钮、搜索框、下拉框和单选控件复用同一轮廓；高对比度模式使用 Windows 系统高亮色。
- 保留键盘任务栏导航、更新徽标、应用溢出导航、隐藏态资源优化和 GitHub Releases 唯一更新源。
- 不迁移数据库，不改变现有任务、桌面收纳、固定应用和设置数据。
