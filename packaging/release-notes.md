# FocusPanel 0.9.59

- 窗口切换、最小化和关闭现在检查 Windows 返回的真实结果。
- Windows 拒绝前台切换、窗口失效或关闭消息失败时，状态中心会显示明确原因。
- 固定、取消固定和拖动排序的 SQLite 写入异常不再导致 Panel 闪退。
- 排序失败会保留已经完成的固定状态，并提示稍后重新排序。
- 新增无副作用窗口命令测试，不切换、最小化或关闭真实窗口。
