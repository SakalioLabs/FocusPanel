# FocusPanel 0.11.70

- 修复 Panel 自动收起后运行应用列表可能过期或看不到新应用的问题；窗口跟踪继续使用 WinEvent，不增加进程轮询。
- 可见窗口优先于纯后台宿主显示，后台应用仍可从 Panel 自有“后台”抽屉完整访问。
- 紧凑任务栏应用区在短屏和高缩放下保留最低可见高度，并继续支持滚轮与上下按钮浏览。
- 设置页新增右上、右中、右下固定位置；多屏、负坐标和不同 DPI 下按所选屏幕独立计算并记忆。

升级前仍会恢复原生任务栏；现有 SQLite 数据、桌面文件路径、自定义分区、自定义图标和用户设置不会被删除。
