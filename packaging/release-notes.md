# FocusPanel 0.9.27

- 统一应用栏改为按应用身份增量同步，窗口前台状态变化时不再清空整条图标集合。
- 保留未变化图标及滚动位置，减少切换窗口时的应用栏闪烁和跳动。
- 时间、系统状态和任务摘要使用分层刷新周期，显著减少空闲时的网络、音频、电池与 SQLite 查询。
- 打开状态中心或日历时仍会立即刷新，日常交互语义保持不变。
- 继续使用 GitHub Releases 零配置一键更新，现有任务、收纳数据和固定顺序无需迁移。
