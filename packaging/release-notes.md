# FocusPanel 0.10.8

- 固定、取消固定和拖动排序改由专用后台闸门串行提交，不再同步阻塞 WPF 界面线程。
- 只有 SQLite 提交成功后才原子替换固定应用快照；失败会保留原顺序并允许下次操作恢复。
- 拖放提交期间保持 Panel 展开，后台异常不会冲出 WPF 事件回调。
- 退出会等待已经进入写入队列的操作完成，避免固定项刚保存就丢失。
- 保持统一应用栏、现有固定顺序和业务数据库结构不变。
