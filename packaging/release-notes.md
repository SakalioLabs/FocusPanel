# FocusPanel 0.10.63

这一版继续把 Panel 从“系统入口集合”推进为可直接完成高频操作的任务栏替代。

- 状态中心可直接调节 Windows 公开的内置显示器亮度，不必打开 Win+A。
- 统一搜索支持 `亮度 35`、`亮度 +10`、`亮度降低 5` 和英文命令。
- 连续拖动会合并最终值并后台串行写入，界面不会被显示驱动阻塞。
- 外接屏或驱动不支持时明确禁用并说明原因，不调用不稳定私有协议。
- 新增安全设备边界和完整自动测试；测试不会修改真实显示器。

升级前仍会恢复原生任务栏，现有 SQLite 数据、桌面文件与用户设置保持不变。
