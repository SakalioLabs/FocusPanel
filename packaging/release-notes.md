# FocusPanel 0.9.81

- Markdown 插入图片改用可替换的 Windows 现代文件选择边界，不再由 ViewModel 直接创建系统对话框。
- 选择窗口归属当前 FocusPanel 窗口，打开期间保持 Panel 和任务详情上下文，不会在后台自动缩回。
- 图片过滤器、标题和 Markdown 替代文字完成中文化；取消不修改正文，失败显示明确原因。
- 新增请求校验、Owner 转发、取消、异常、交互锁和文件决策测试，自动测试不会弹出真实选图窗口。
