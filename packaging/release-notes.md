# FocusPanel 0.10.48

- 在设置中开启且任务栏接管成功后，可用 Ctrl+Alt+1…9 从任意应用直接启动或切换 Panel 前九个应用槽位；选项默认关闭，避免影响 AltGr 输入。
- 加按 Shift 可启动对应应用的新实例；槽位按统一应用栏当前顺序动态映射，并在工具提示和设置中说明。
- 冲突快捷键不会被抢占；恢复原生任务栏、接管异常、更新或退出时自动注销本次注册。
