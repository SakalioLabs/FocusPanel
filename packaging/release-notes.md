# FocusPanel 0.10.82

这一版把全局唤出改成真正一步可用的快速搜索。

- 按 `Ctrl+Alt+Space` 直接展开 Panel 并进入统一搜索，不再需要再按一次 Enter。
- 主组合被占用时继续自动使用 `Ctrl+Shift+Space`。
- 搜索框自动获得焦点并全选原查询，可以直接覆盖输入。
- 重复按快捷键保持搜索打开，不会反向关闭。
- 右缘悬停和托盘恢复仍只进入紧凑栏，保持各自明确语义。

升级前仍会恢复原生任务栏，现有 SQLite 数据、桌面文件与用户设置保持不变。
