# FocusPanel 0.10.45

- 完整任务栏接管不再启用 Explorer 原生自动隐藏，鼠标触底不会重新展开原任务栏；主屏工作区只在启用时释放一次，退出与异常恢复原工作区、AppBar 状态和可见性。
- 运行应用图标悬停约 420ms 即可打开可操作窗口列表，单窗口与多窗口均可直接切换；移出后自动关闭，快速划过、拖拽和右键操作不会误触。
- 守护器继续只读验证，不循环隐藏或写工作区；显示布局或 Explorer 状态变化时安全恢复一次并等待用户重新接管。
