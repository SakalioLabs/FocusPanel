# FocusPanel 0.9.17

- 新增系统托盘箭头，使用 Windows 11 可访问性接口直接唤起 Explorer 原生隐藏图标浮层，保留第三方图标原有的点击和右键菜单。
- 紧凑栏常驻显示输入语言、输入法、网络、音量、通知、时间日期、显示桌面与 FocusPanel 设置。
- 固定应用、运行应用和工作区入口统一进入中部滚动区，系统控制不会再把设置按钮顶掉。
- 不读取 Explorer 进程内存、不复制第三方托盘图标，也不在自动测试中隐藏真实任务栏。
