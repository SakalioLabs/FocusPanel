# FocusPanel 0.11.39

- Panel 自有“开始”将固定应用放在最前，其后展示最多 8 个最近成功启动的应用。
- 最近顺序跨重启保存；同名应用按稳定身份区分，失败或取消启动不会写入历史。
- 输入应用名称后仍按原有搜索相关性排序，不让历史干扰明确查询。
- 移除已经废弃的 UI Automation 托盘探测程序集引用，启动器不依赖原生任务栏。

升级前仍会恢复原生任务栏；现有 SQLite 数据、桌面文件路径、自定义分区和用户设置不会被删除。
