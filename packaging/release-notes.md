# FocusPanel 0.10.89

这一版让 Panel 的自动收回速度适合不同用户，而不是强迫所有人接受一个硬编码值。

- 设置页新增快速、标准、从容和较慢四档自动收起速度。
- 普通鼠标离开使用所选速度，默认调整为更稳妥的 0.5 秒。
- 输入、拖拽、菜单、模态确认和固定展开仍会阻止误收起。
- 主动唤出宽限和窗口失焦安全收回继续使用独立时序。
- 选择写入现有 Shell 偏好，异常配置自动回到标准档。

升级前仍会恢复原生任务栏，现有 SQLite 数据、桌面文件与用户设置保持不变。
