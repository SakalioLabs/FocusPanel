# FocusPanel 0.10.73

这一版修复已收纳图标通过右键菜单移动到其他收纳区域时可能闪退的问题。

- 右键菜单打开时固定当前文件和分区列表快照，不再依赖可能变化的全局选中项。
- 点击“移动到”或“收纳到面板”后先关闭菜单，再提交分类并刷新卡片，避免实时菜单与虚拟化集合同时改写。
- “恢复到桌面”也直接携带当前右键文件，不会误用上一次选择。
- 交互边界会捕获意外异常并保留原布局，不再让事件异常结束 Panel。

升级前仍会恢复原生任务栏，现有 SQLite 数据、桌面文件与用户设置保持不变。
