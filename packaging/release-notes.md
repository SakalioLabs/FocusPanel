# FocusPanel 0.11.89

- 在任意应用中按 `Ctrl + Alt + F`，无需展开 Panel 即可专注当前应用。
- 再按一次恢复本次收起的窗口，并保留它们原来的普通或最大化状态。
- 快捷键冲突时自动使用 `Ctrl + Alt + Shift + F`，设置页显示真实生效组合。
- 没有可专注窗口、部分失败和恢复失败都会给出明确的 Panel 通知。

升级前仍会恢复原生任务栏；现有 SQLite 数据、桌面文件路径、自定义分区、自定义图标和用户设置不会被删除。
