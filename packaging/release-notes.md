# FocusPanel 0.11.34

- 实时窗口缩略卡新增最小化、最大化/还原和关闭三个直接按钮。
- 最大化按钮会根据窗口真实状态切换为还原；已最小化窗口不会重复发送最小化请求。
- 窗口状态操作继续复用现有安全边界，失败会由 Panel 状态中心明确反馈。
- 操作后自动关闭已过期预览，窗口列表随 WinEvent 快照更新真实状态。

升级前仍会恢复原生任务栏；现有 SQLite 数据、桌面文件路径、自定义分区和用户设置不会被删除。
