# FocusPanel 0.11.27

- 多窗口应用普通左键现在直接打开按该应用精确筛选的完整窗口总览。
- 实时预览、切换、关闭、窗口状态、置顶和跨屏移动无需再进入小型文字菜单。
- 同名但应用身份不同的程序不会混入筛选结果，搜索框继续筛选当前应用的窗口标题。
- 顶部筛选条明确显示当前应用，可一键“查看全部”；旧文字列表只作为 DWM 不可用时的悬停降级。

升级前仍会恢复原生任务栏；现有 SQLite 数据、桌面文件路径、自定义分区和用户设置不会被删除。
