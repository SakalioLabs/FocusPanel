# FocusPanel 0.11.29

- 修复全宽图标收纳盒在部分机器上不显示或仍显示旧布局的问题。
- 反向布尔绑定现在按目标类型正确输出 WPF 可见性，同时保留复选与启用状态语义。
- 开始按钮直接使用 Windows 键输入边界，不再访问被接管的原生任务栏窗口。
- 自定义图标、分区、文件路径、隐藏状态及用户数据库保持不变。

升级前仍会恢复原生任务栏；现有 SQLite 数据、桌面文件路径、自定义分区和用户设置不会被删除。
