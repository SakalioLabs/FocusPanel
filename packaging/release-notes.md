# FocusPanel 0.11.20

- 统一搜索顶部新增“全部、窗口数量、应用、系统”四个清晰入口。
- 点击“窗口”无需输入即可查看当前所有已打开窗口，当前窗口置顶，点击一行直接切换。
- 窗口列表复用现有 WinEvent 快照，不轮询 Explorer，也不会临时显示原生任务栏。
- “应用”只显示启动项；“系统”可直接浏览任务管理器、设备管理器、设置等入口。
- 搜索输入、方向键选择、Enter 执行和 Esc 返回紧凑栏的原有交互保持不变。

升级前仍会恢复原生任务栏；现有 SQLite 数据、桌面文件路径、自定义分区和用户设置不会被删除。
