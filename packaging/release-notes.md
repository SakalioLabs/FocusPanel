# FocusPanel 0.10.9

- 主题、全屏热区和任务栏替代状态不再在 WPF 界面线程同步写入 SQLite。
- 同一个设置的连续变化会合并为最后值，并由单消费者后台队列严格串行保存。
- 启动时用一份无跟踪快照读取四项 Shell 设置，不再重复打开数据库。
- 保存失败会保留当前会话、显示可重试提示，并继续处理后续设置。
- 退出会排空已经接收的设置写入；不改变现有 AppConfig 数据和任务栏恢复语义。
