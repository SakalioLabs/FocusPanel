# FocusPanel 0.11.67

- “后台应用与窗口”支持直接搜索应用名或可靠的可执行文件名。
- 新增“全部 / 有窗口 / 纯后台”三种筛选，不必在长列表中反复滚动。
- 筛选直接复用统一任务栏应用集合，不改变固定与运行应用的稳定顺序。
- 紧凑栏“后台”入口会直接聚焦搜索框；输入期间 Panel 不会自动收起。

升级前仍会恢复原生任务栏；现有 SQLite 数据、桌面文件路径、自定义分区、自定义图标和用户设置不会被删除。
