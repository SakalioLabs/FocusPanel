# FocusPanel 0.9.49

- 桌面收纳网格和列表只生成当前可见行与前后缓冲行的文件卡片。
- 保留双列收纳盒、统一滚动条、完整集合和全部拖拽、选择及右键操作。
- 文件容器在滚动时回收复用；图标缩放、窗口宽度、折叠和集合变化会自动重算。
- 修复行边界多生成一行以及回收容器滚动后未重新挂载的问题。
- 1000 项真实 WPF 验证中，首屏、中段、集合变化和返回顶部仅生成 9/12/12/9 个容器。
- 桌面文件路径级增量刷新、分区差量同步和大量文件虚拟化现已形成完整性能链。
