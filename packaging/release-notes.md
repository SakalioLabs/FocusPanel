# FocusPanel 0.11.62

- 状态中心的多窗口列表新增逐窗口实时预览按钮，不必回到底部任务栏寻找缩略项。
- 预览卡使用 Windows DWM 实时画面，可直接切换、最小化、最大化、还原或正常关闭窗口。
- 预览不激活 Panel、不截屏、不轮询像素，也不显示原生任务栏。
- DWM、远程桌面或受保护窗口拒绝预览时会明确提示，窗口名称切换仍然可用。
- 鼠标离开按钮和预览卡后自动收起；Esc、切换弹层和退出会清理预览会话。

升级前仍会恢复原生任务栏；现有 SQLite 数据、桌面文件路径、自定义分区和用户设置不会被删除。
