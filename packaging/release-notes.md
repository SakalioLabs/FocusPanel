# FocusPanel 0.11.4

这一版恢复任务栏状态入口符合习惯的右键语义，不再一次右键就意外切换静音。

- 右键打开单层 Fluent 状态快捷菜单，不再执行隐藏的即时静音动作。
- 菜单明确提供静音/取消静音、播放/暂停、快捷设置、通知中心、输入法和电源。
- 静音项目直接使用当前真实状态生成文案，并在没有默认输出设备时禁用。
- 普通点击仍打开完整状态中心，滚轮仍直接调音量，中键仍播放或暂停媒体。
- 菜单打开期间暂停自动收起，关闭后恢复既有延时，不增加新的固定栏按钮。

升级前仍会恢复原生任务栏，现有 SQLite 数据、桌面文件与用户设置保持不变。
