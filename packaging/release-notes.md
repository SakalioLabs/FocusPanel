# FocusPanel 0.11.80

- 状态中心顶部直接显示最近任务栏接管检查结果，并可一键重新检查。
- 接管异常或安全恢复时，紧凑状态按钮显示独立 `!`，不与 Panel 通知混用。
- 正常、等待检查和未启用状态不显示常驻警告，避免按钮信息过载。
- 读屏名称同步播报任务栏健康摘要，检查仍严格只读并在后台执行。

升级前仍会恢复原生任务栏；现有 SQLite 数据、桌面文件路径、自定义分区、自定义图标和用户设置不会被删除。
