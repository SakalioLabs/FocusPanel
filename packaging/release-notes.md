# FocusPanel 0.11.46

- 当前 Wi-Fi 可直接从 Panel 断开，不再显示不可点击的“当前”。
- 每个已保存网络都有明确的“忘记”按钮；当前网络会先断开再删除配置。
- 断开和删除后都会读取 Windows 网络状态确认，不显示虚假成功。
- 权限拒绝、Radio 关闭、WLAN 服务不可用及网络消失都有明确反馈。

升级前仍会恢复原生任务栏；现有 SQLite 数据、桌面文件路径、自定义分区和用户设置不会被删除。
