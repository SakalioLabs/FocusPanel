# FocusPanel 0.10.50

- 可把 Explorer 中的文件或文件夹直接拖到统一应用栏图标上，用目标应用打开；外部文件拖放与内部固定排序严格分流。
- 桌面应用在后台一次接收最多 32 个路径；打包应用使用 Windows 公开 FileActivated 合约，不模拟按键、不临时显示原生任务栏。
- 目标图标提供单层 Fluent 强调反馈；无效、不可访问或超限项目会显示部分成功/失败 Toast，退出会等待已接收操作完成。
