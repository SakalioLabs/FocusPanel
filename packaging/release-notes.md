# FocusPanel 0.11.82

- 设置页新增“屏幕左侧 / 屏幕右侧”，并可继续选择上、中、下位置。
- 左侧模式会同步镜像紧凑栏、工作区展开方向、热区、指示条、窗口缩略卡和位置菜单。
- 自动目标在左侧模式选择最左显示器；指定显示器、主屏、多屏负坐标和不同 DPI 继续支持。
- 边缘选择会持久保存，升级不会删除桌面收纳数据、任务、AI 配置或固定应用。

升级前仍会恢复原生任务栏；现有 SQLite 数据、桌面文件路径、自定义分区、自定义图标和用户设置不会被删除。
