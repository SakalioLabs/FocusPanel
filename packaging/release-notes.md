# FocusPanel 0.10.68

这一版让应用右键菜单更接近原生任务栏 Jump List。

- 同时显示 Windows 公开的“最近项目”和“常用项目”。
- 两类都有内容时优先各展示 4 项，总计最多 8 项。
- 同一文件跨分类只显示一次，不浪费菜单空间。
- 某一分类读取失败时保留另一分类，不让整个菜单变空。
- 项目仍在单层菜单中直达，不增加多级子菜单。
- 不读取 Explorer 私有数据，也不冒充支持不可访问的固定项。

升级前仍会恢复原生任务栏，现有 SQLite 数据、桌面文件与用户设置保持不变。
