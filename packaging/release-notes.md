# FocusPanel 0.10.36

- 统一 Setup 的目录选择结果会同时写入 MSI 的两个真实目录属性，安装结束后反查注册表验证实际落盘位置。
- 从 C 盘迁移旧版时等待卸载注册和旧 Update.exe 真正释放，未完成时停止安装，不再与旧版竞态后写回原目录。
- 自动收纳的监听任务、进度渲染和文件列表通知均隔离异常；关闭应用会先等待已开始的收纳完成。
- 单个桌面项目、界面观察者或退出竞态失败只进入可恢复状态，不再触发整个 Panel 的全局闪退。
