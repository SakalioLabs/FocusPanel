# FocusPanel 0.11.88

- 应用或窗口右键可直接进入“窗口专注”，自动收起其他可管理窗口。
- 恢复时保留原普通/最大化状态，并让原活动窗口最后回到前台。
- 搜索面板持续显示专注目标和收起数量，可随时一键恢复。
- 单窗口失败不会中断其余操作，失败恢复可重试，正常退出也会主动还原。

升级前仍会恢复原生任务栏；现有 SQLite 数据、桌面文件路径、自定义分区、自定义图标和用户设置不会被删除。
