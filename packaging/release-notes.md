# FocusPanel 0.10.4

- 收纳盒新建、重命名、删除和文件分类移出 WPF 界面线程。
- 拖拽排序与跨列移动通过共享数据库闸门串行提交。
- 拖拽期间保持 Panel 展开，写入完成后才刷新最终布局。
- 来源列和目标列同步重排，避免重复或跳跃顺序。
- 数据库失败时保留原布局并显示明确错误，不让异常导致闪退。
- 退出前等待已经进入仓库的分区写入完成，避免丢失最后一次拖拽。
- 保持桌面文件路径、隐藏属性和恢复记录不变。
