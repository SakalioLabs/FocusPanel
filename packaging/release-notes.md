# FocusPanel 0.11.91

- 紧凑任务栏只显示固定应用和确实拥有普通或最小化窗口的应用。
- 未固定的托盘常驻、消息窗口、后台宿主和无窗口进程不再混入任务栏。
- 最小化应用继续保留，用户主动固定的后台应用也不会被误删。
- 后台观察项仍可在独立“后台与窗口”抽屉中管理。

升级前仍会恢复原生任务栏；现有 SQLite 数据、桌面文件路径、自定义分区、自定义图标和用户设置不会被删除。
