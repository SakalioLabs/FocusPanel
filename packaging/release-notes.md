# FocusPanel 0.10.10

- 音量 Slider、静音点击和紧凑栏滚轮不再在 WPF 界面线程同步调用 Core Audio。
- 等待中的音量变化只保留最终值，音量与静音通过一个后台工作者严格串行。
- 每次音频读写在当前工作线程独立初始化 COM，并创建、释放自己的设备枚举器。
- 旧写入结果不会覆盖较新的界面操作；最新写入失败时回退到最后真实成功值。
- 写入期间暂停旧音频快照回填，退出会排空已经接收的音频控制请求。
- 同版本发布前重建会保留上一完整包，确保差分更新仍可生成。
