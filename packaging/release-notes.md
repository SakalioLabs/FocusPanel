# FocusPanel 0.10.2

- OKR 首次打开改为后台读取目标、飞书配置、同步间隔和最后同步时间。
- SQLite 暂时繁忙时保留当前界面，不清空本地目标。
- 本地编辑发生后，迟到的旧快照不会覆盖新目标或关键结果。
- 手动飞书同步、配置读取和凭据保存不再阻塞 WPF 界面线程。
- 只发生推送的同步也会刷新本地同步状态。
- AI 预留 OKR 接口改为可等待调用，不再使用同步 Wait 或 Dispatcher.Invoke。
- 保持任务栏、桌面收纳、任务、番茄钟、AI 和现有数据库内容不变。
