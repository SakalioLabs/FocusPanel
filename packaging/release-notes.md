# FocusPanel 0.11.78

- 应用任务栏的上下箭头直接显示尚未露出的应用数量，不再盲滚。
- 滚轮、按钮、键盘、拖拽和活动应用自动露出后，数量都会实时同步。
- 到达顶部或底部时对应箭头自动消失，超过 99 个应用显示 `99+`。
- 44px 点击区域、统一圆角和现有应用顺序保持不变。

升级前仍会恢复原生任务栏；现有 SQLite 数据、桌面文件路径、自定义分区、自定义图标和用户设置不会被删除。
