# FocusPanel 0.11.64

- 统一搜索支持“Chrome 音量 30”“微信静音”“Spotify 取消静音”等逐应用音量命令。
- 支持绝对音量、相对提高/降低、静音和取消静音，并提供英文 `volume up/down` 语法。
- 结果绑定当前真实 Core Audio 会话 ID，同名多会话不会错误合并。
- 搜索空态会用当前正在播放的应用生成可编辑示例。
- 会话结束或写入失败时安全回滚并刷新，不误改主音量或其他应用。

升级前仍会恢复原生任务栏；现有 SQLite 数据、桌面文件路径、自定义分区和用户设置不会被删除。
