# FocusPanel 0.11.50

- 更新、任务、番茄钟和恢复消息会进入状态中心的 Panel 通知历史，不再随 7 秒提示一起消失。
- 新增未读徽标、全部已读、清空、去重更新和通知原动作回放。
- 状态按钮右键可直接打开 Panel 通知；Windows 通知中心降级为详情底部明确标注的兼容入口。
- 不虚假承诺读取其他应用通知：该能力仍需要 Windows 安装包身份、清单声明和用户授权。

升级前仍会恢复原生任务栏；现有 SQLite 数据、桌面文件路径、自定义分区和用户设置不会被删除。
