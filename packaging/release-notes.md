# FocusPanel 0.9.47

- 桌面文件变化后不再清空并重建全部收纳盒、左右列和文件卡片。
- 未变化的收纳盒继续复用原对象，保留展开状态、选中文件和当前滚动位置。
- 文件跨收纳盒只更新对应文件集合；收纳盒排序和换列只发送真实的移动、插入或删除事件。
- 同路径文件确需替换时保留原选择状态，数据库瞬时读取失败时保留最后一次有效界面。
- 文件偏好改用不区分大小写字典匹配，减少大量桌面项目刷新时的重复遍历。
- 新增差量同步测试、禁止整页 Clear 的界面契约，以及真实 WPF 非零滚动偏移稳定性验证。
