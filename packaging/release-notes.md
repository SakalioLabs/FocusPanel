# FocusPanel 0.10.91

这一版让 FocusPanel 可以像真正的任务栏一样留在桌面边缘，不必每次使用都重新触边呼出。

- 设置页新增“鼠标离开后保留 76px 紧凑栏”。
- 展开的工作区离开后只折回紧凑栏，应用、状态和时间继续可见。
- 独占全屏时自动让路，退出全屏后恢复紧凑栏。
- 托盘隐藏与 Esc 仍作为明确隐藏操作，不被常驻设置反向覆盖。
- 输入、拖拽、菜单和固定展开继续阻止误收起。

升级前仍会恢复原生任务栏，现有 SQLite 数据、桌面文件与用户设置保持不变。
