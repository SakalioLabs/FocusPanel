# FocusPanel 0.10.76

这一版减少无效入口，并把任务栏替代体验放回首位。

- 移除 OKR 可见功能和不可靠的任务视图入口；旧 OKR 数据保留，不做删除或迁移。
- 展开工作区新增桌面收纳、任务、番茄钟、AI 和设置五个常驻一级入口。
- 普通滚轮直接滚动统一应用栏，`Ctrl+滚轮`用于多窗口切换。
- 原生任务栏的窗口区域或 DWM 增强隐藏被 Windows 拒绝时可安全降级，不再因此整次接管失败；真实隐藏或工作区释放失败仍会回滚并说明原因。

升级前仍会恢复原生任务栏，现有 SQLite 数据、桌面文件与用户设置保持不变。
