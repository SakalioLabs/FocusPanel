# 文件收纳板块优化实施计划

## 目标
1. 实现部分隐藏桌面图标功能（收纳到面板，桌面隐藏）
2. 优化性能，解决面板唤出卡顿问题

---

## 第一阶段：性能优化

### 1.1 图标加载优化
- [x] **IconHelper 缓存** - 内存缓存限制为 500 项，避免重复解析相同文件
- [x] **延迟加载** - 当前搜索结果与固定项按需进入独立 STA 图标队列
- [x] **占位图标** - 图标缺失或尚未加载时显示应用名称首字符

### 1.2 文件扫描优化
- [x] **增量更新** - FileSystemWatcher 按路径合并变化并只更新受影响项目，仓库变化或监视器异常时安全回退全量刷新
- [x] **后台预加载** - 开始菜单、AppsFolder 和应用身份在可取消 STA 线程构建

### 1.3 分区构建优化
- [x] **差量更新** - 收纳盒、左右列与文件卡片按身份执行 Insert/Move/Remove
- [x] **虚拟化** - 网格和列表模式按外层滚动视口回收文件卡片容器，仅生成可视行与前后缓冲行

---

## 第二阶段：部分隐藏功能

### 2.1 数据模型修改
- [x] **DesktopFilePreference** - 已持久化 `IsHiddenFromDesktop` 与恢复事务字段
- [x] **DesktopFile** - 已提供 `IsHidden` 与 `NeedsRecovery` 状态

### 2.2 扫描逻辑修改
- [x] **FileOrganizerService** - 扫描包含已登记的 `Hidden + System` 项目
- [x] **保留完整文件列表** - `AllFiles` 供面板显示，`Files` 保留可整理项

### 2.3 收纳功能
- [x] **收纳命令** - 选中文件后保留原路径并追加 `Hidden + System`
- [x] **取消收纳** - 精确恢复原始文件属性
- [x] **批量操作** - 选中集合支持批量分区与自动整理

### 2.4 UI 交互
- [x] **收纳按钮** - 文件卡片与工具栏提供收纳入口
- [x] **快捷键** - 右键菜单提供收纳和取消收纳
- [x] **视觉反馈** - 已收纳、恢复失败与受保护系统文件限制均有状态提示

---

## 实施顺序

1. 先修改数据模型（向后兼容）
2. 实现核心扫描逻辑
3. 添加收纳命令
4. 优化性能
5. 添加 UI 交互
