# 文件收纳板块优化实施计划

## 目标
1. 实现部分隐藏桌面图标功能（收纳到面板，桌面隐藏）
2. 优化性能，解决面板唤出卡顿问题

---

## 第一阶段：性能优化

### 1.1 图标加载优化
- [ ] **IconHelper 缓存** - 添加内存缓存，避免重复解析相同文件
- [ ] **延迟加载** - 只加载可视区域内的图标
- [ ] **占位图标** - 加载前显示默认图标

### 1.2 文件扫描优化
- [ ] **增量更新** - FileSystemWatcher 检测变化时只更新变更项，而非全量刷新
- [ ] **后台预加载** - 启动时后台加载，UI 只在需要时显示

### 1.3 分区构建优化
- [ ] **差量更新** - 使用 ObservableCollection 的差异化更新
- [ ] **虚拟化** - 对大量文件使用 UI 虚拟化

---

## 第二阶段：部分隐藏功能

### 2.1 数据模型修改
- [ ] **DesktopFilePreference** - 添加 `IsHiddenFromDesktop` 字段
- [ ] **DesktopFile** - 添加 `IsHidden` 属性

### 2.2 扫描逻辑修改
- [ ] **FileOrganizerService** - 扫描桌面时过滤已隐藏文件
- [ ] **保留完整文件列表** - 用于面板显示

### 2.3 收纳功能
- [ ] **收纳命令** - 选中文件 → 隐藏 → 移至分区
- [ ] **取消收纳** - 从分区移除 → 恢复桌面可见
- [ ] **批量操作** - 支持多选收纳

### 2.4 UI 交互
- [ ] **收纳按钮** - 选中文件后显示收纳选项
- [ ] **快捷键** - 右键菜单添加收纳选项
- [ ] **视觉反馈** - 收纳状态显示

---

## 实施顺序

1. 先修改数据模型（向后兼容）
2. 实现核心扫描逻辑
3. 添加收纳命令
4. 优化性能
5. 添加 UI 交互