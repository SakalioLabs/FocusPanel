# Changelog

## v0.10.26 - 2026-07-29

- 更新包下载完成后的数据库在线备份不再由 `MainWindow.ApplyDownloadedUpdate()` 在 WPF 线程同步执行；新的 `UpdateInstallPreparationCoordinator` 在工作线程创建 `DatabaseBackupService`、执行 SQLite `quick_check`、在线备份和滚动清理，大数据库或慢磁盘不会冻结“正在安全重启”界面。
- `RequestApplyUpdate` 从无法等待的 `Action` 改为 `Func<Task>`；设置页更新命令会等待安装准备、任务栏恢复与 Velopack 启动交接完成后才解除 `IsUpdateBusy`，用户不能在备份仍进行时重复点击更新。
- 安装准备期间使用现有临时交互租约保持 Panel 展开，并停用右缘监测；成功后按原顺序恢复主屏任务栏、原生桌面图标并启动安装。准备异常会恢复热区、更新失败状态并显示具体原因，当前数据库和已下载更新包保持不动。
- 协调器在退出阶段停止接受新准备任务，所有异常在后台边界内转换为结果；新增非阻塞、工作线程、异常隔离、退出拒绝及可等待事件源码契约测试。README 增加更新安装后台交接流程图。Release 构建 0 错误、0 警告，全量 720 项测试与界面冒烟通过。

## v0.10.25 - 2026-07-29

- `EdgeHotZoneMonitor` 不再使用 WPF `DispatcherTimer` 每 30ms 在界面线程读取物理鼠标、目标显示器和前台全屏状态；新的 `PeriodicTimer` 循环在工作线程保持采样节拍，工作区布局、毛玻璃展开动画或 Dispatcher 短暂繁忙时不会直接漏掉 100ms 右缘停留。
- 后台循环只在热区可用状态真正变化或检测器达到呼出条件时，以 `DispatcherPriority.Input` 提交一次 UI 事件；稳定状态不再每秒占用约 33 次 WPF 调度，12px 感应带、100ms 停留、32px 复位和全屏抑制语义保持不变。
- 启动、停止、托盘隐藏、更新重启与重新显示均推进采样代际；已经在执行的旧原生检查完成后会被静默丢弃，不会在 Stop 之后迟到展开。显示器边界另有修订号，主屏切换或 DPI/分辨率变化期间的旧坐标样本不能污染新屏检测。
- 新增后台线程、非阻塞启动、停止丢弃迟到结果、单次可用状态通知和停留呼出测试；源码契约禁止热区重新使用 `DispatcherTimer`。README 增加后台热区采样流程图。Release 构建 0 错误、0 警告，全量 716 项测试与界面冒烟通过。

## v0.10.24 - 2026-07-29

- `ShellCoordinator` 创建更新服务时不再同步构造 Velopack `UpdateManager`；安装根目录、当前版本目录和便携状态定位改为共享工作线程任务，主窗口构造与 XAML 首帧不再等待安装文件系统。
- 自动检查与设置页手动检查统一等待同一个初始化任务；后台初始化尚未完成时不再因为暂时的 `CanUpdate=false` 跳过首轮检查，也不会把安装版短暂误报成开发运行版后保持六小时。
- 初始化完成后原子发布当前版本和可更新能力；开发运行版仍不会访问更新源，安装版与便携版继续使用 GitHub Latest Release 静态清单。下载、进度、恢复任务栏后重启安装的原有语义不变。
- 新增慢安装定位非阻塞、工作线程执行、共享初始化与首轮能力发布测试；README 增加更新管理器后台准备示意图。Release 构建 0 错误、0 警告，全量 713 项测试与界面冒烟通过。

## v0.10.23 - 2026-07-29

- `TaskbarController` 构造不再同步创建 `%LOCALAPPDATA%\FocusPanel` 恢复目录；主壳仅装配任务栏边界、但用户未启用替代模式时，不再为尚未发生的接管执行文件系统写入。
- 恢复目录创建移动到 `TryEnableReplacement()` 的既有会话事务中，并严格位于隐藏任务栏之前；目录、禁用标记或会话快照任一步失败都会中止接管并清理内存状态，原生任务栏保持可见。
- watchdog 启动、一次隐藏、幂等恢复、紧急快捷键和旧会话恢复语义不变；新增构造零副作用测试，并由现有启用/恢复测试确认首次接管仍能创建缺失目录。
- README 增加任务栏恢复会话按需创建示意图。Release 构建 0 错误、0 警告，全量 711 项测试与界面冒烟通过。

## v0.10.22 - 2026-07-29

- `MainViewModel` 构造不再直接创建 `SettingsService`、`FileOrganizerService` 和 `FileOrganizerViewModel`；新的异步工厂在工作线程读取 `%APPDATA%` 设置、检查旧收纳目录并创建桌面/公共桌面/旧仓库监控器，主壳对象、窗口句柄和偏好安全门不再被这些文件系统准备步骤阻塞。
- 保持“桌面收纳”为默认工作区，但首帧改为使用现有 Fluent 进度样式的轻量加载状态；完整 ViewModel 准备完成后原位替换，布局快照、文件扫描、自动收纳、拖拽与数据库语义不变。
- 工作区导航增加单调修订号：准备期间切换到任务、番茄钟、OKR、AI 或今日概览时，迟到的桌面收纳结果只进入缓存，不会覆盖用户当前页面；再次进入“桌面收纳”可立即复用已完成实例。
- 初始化异常被任务边界隔离并在当前工作区显示可读错误，不会逃逸到 WPF Dispatcher 导致 Panel 闪退；主壳先退出时，后台刚完成的 ViewModel 会立即释放文件监控器、计时器和布局队列。
- Explorer 的“显示受保护的系统文件”注册表状态不再在主壳构造或设置按钮命令中同步读取；启动与重复打开设置页统一进入合并式后台刷新器，迟到结果只在 Dispatcher 存活时提交，退出会取消刷新，不让配置文件服务延迟冻结 Panel。
- 新增真实工作线程、调用方非阻塞、异常隔离、导航代际与退出丢弃测试，界面契约确认主壳不再直接构造收纳 ViewModel；README 增加后台准备与迟到提交示意图。Release 构建 0 错误、0 警告，全量 709 项测试与界面冒烟通过。

## v0.10.21 - 2026-07-29

- 修复双屏横向排列时 Panel 固定使用主屏右缘、主屏位于左侧便展开到两屏接缝的问题；新增纯逻辑显示器选择边界，Panel、12px 热区和 3px 指示条统一锚定虚拟桌面最外侧的右屏。
- 目标屏选择使用物理像素矩形并稳定处理负坐标、左右排列与相同右边界；显示设置变化时三者同步重算，右侧副屏存在时不再一部分留在主屏、一部分落到副屏。
- DPI 不再从 `Shell_TrayWnd` 推断；窗口先用当前 DPI 粗定位到目标屏，再通过微软为 Per-Monitor V2 推荐的 `GetDpiForWindow` 读取目标屏真实 DPI并执行一次精确校正。紧凑/展开尺寸、边距和动画使用承载屏幕缩放，修复混合 100%/150% 缩放下的横向错位。
- 新增 `FocusPanel-win-CustomSetup.exe`：提供真实的图形化文件夹选择器，确认后调用 Velopack 官方 `Setup.exe --installto`，继续保留原生更新根目录与后续一键更新能力。MSI 说明纠正为当前用户/整机及企业部署，不再误称任意目录选择器。
- 发布器不再假设 Velopack 会上传自定义扩展资产：标准更新包完成后通过 GitHub Release API 显式上传 `CustomSetup.exe`，同名同大小时幂等跳过、大小不符时精确替换，最后再与完整包、MSI 和清单统一验收。
- 发布后补传支持幂等续跑：tag 对应 Release 已存在时跳过会拒绝重复发布的 Velopack 上传阶段，直接进入自定义资产修复、Latest 标记和完整验收；只有明确的 404 才创建新 Release，其他 GitHub 响应异常原样中止。
- 新增左右副屏、相同右边界和物理定位测试，发布契约要求 CustomSetup、MSI 与更新清单同时存在；README 更新安装入口说明，并增加自定义安装和双屏目标屏示意图。

## v0.10.20 - 2026-07-29

- `MainViewModel` 构造不再同步调用 `ShellPreferenceRepository.Load()` 打开 SQLite；首次引导、任务栏替代、主题和全屏热区通过共享的 `LoadAsync()` 后台任务读取，主壳对象、窗口句柄和右缘定位可以先完成，不被数据库锁等待或存储延迟阻塞。
- `MainWindow.Loaded` 新增偏好就绪安全门：窗口先定位并隐藏自身，再等待快照；读取期间不创建/启动热区，不显示边缘指示条，不接受托盘、热区或全局快捷键展开，也不调用任务栏控制器。快照就绪且窗口未退出后才启动壳层输入能力。
- 启动决策保持严格顺序：快照要求引导时展开引导并激活；否则保持隐藏，仅在持久化替代状态为启用时把一次接管请求排到 UI 空闲队列。等待期间不会把安全默认值误当用户配置，也不会出现先按默认值显示再突然隐藏的闪动。
- 偏好仓库缓存并共享同一后台读取任务，多处等待不会重复查询 SQLite；`CompleteAsync()` 与 `Dispose()` 同时排空读取和已接受写入，退出后的迟到快照不会修改 ViewModel。读取异常继续回落到不接管任务栏、显示引导的安全默认值。
- 新增后台线程、非阻塞、单次共享快照测试，并扩展源码契约验证窗口先隐藏、等待偏好、再开放入口与接管；README 增加主壳启动安全门流程图。Release 构建 0 错误、0 警告，全量 700 项测试与界面冒烟通过。

## v0.10.19 - 2026-07-29

- 主壳构造不再同步调用 `AutoStartupService.IsStartupEnabled()` 读取 Windows `Run` 注册表项；页面先显示“正在读取 Windows 启动项…”，新的 `AutoStartupCoordinator` 在工作线程读取并将最终状态安全返回 WPF，用户配置文件服务或安全软件响应慢时不会延迟 Panel 壳层初始化。
- 设置页和首次引导中的开机启动切换不再同步写注册表；启用、禁用与失败后的真实状态复读全部进入同一个 `SemaphoreSlim` 串行边界，快速“开→关”或“关→开”严格按请求顺序执行，最后一次选择必定最后落盘。
- 每个读取和写入请求分配单调修订号；启动期尚未完成的旧读取不能覆盖用户后来完成的切换，旧写入反馈也不能覆盖最新状态。写入失败时后台复读注册表并精确回滚开关，复读本身失败则按操作前的安全状态回退。
- 退出阶段停止接受新启动项操作并等待已经进入边界的读写完成；迟到结果检测到 ViewModel 已释放后不会更新界面。原有可执行文件路径解析、带引号启动命令、当前用户注册表位置和任务栏替代模式联动语义保持不变。
- 新增后台线程、非阻塞、严格串行、失败复读、委托异常、最新请求所有权、退出排空和拒绝新操作测试，并更新开机启动界面契约；README 增加注册表后台协调图。Release 构建 0 错误、0 警告，全量 699 项测试与界面冒烟通过。

## v0.10.18 - 2026-07-29

- 状态中心、六入口紧凑栏与 Win+X 风格菜单的开始、搜索、任务视图、小组件、运行、快捷设置、通知、输入法、管理工具、电源设置、显示桌面、锁定和睡眠不再由 WPF 命令同步调用 `ISystemStatusService`；新的 `SystemActionCoordinator` 在工作线程进入 Win32、Windows Shell 和 `Process.Start` 边界。
- 重启和关机继续先使用 FocusPanel Fluent 对话框二次确认，确认后的系统调用才进入后台；操作语义、系统权限与失败提示保持不变，不增加自动重试，也不在测试中执行任何真实电源或系统动作。
- 多个系统入口可以独立执行，每次请求分配单调修订号；只有最后一次请求有资格关闭弹层或更新状态中心，旧请求的迟到失败不会盖住用户后来打开成功的入口。同步异常统一转换为失败结果，不逃逸异步命令造成 Panel 闪退。
- GitHub 下载页打开与数据库恢复交接进程也复用同一后台边界；恢复交接只有启动成功后才请求关闭当前应用，失败时当前数据库、系统任务栏和正在运行的实例保持原状。
- 新增后台线程、非阻塞、原生失败、异常隔离、最新请求所有权和空动作参数测试，并增加 `MainViewModel` 无同步 `Process.Start`/系统入口调用的源码契约；README 增加系统动作后台协调图。Release 构建 0 错误、0 警告，全量 693 项测试与界面冒烟通过。

## v0.10.17 - 2026-07-29

- 任务详情的 Markdown 图片导入不再在 WPF 命令中同步执行 `Directory.CreateDirectory` 与 `File.Copy`；新的 `TaskImageImportCoordinator` 在工作线程完成目标目录创建、唯一文件名生成和复制，网络盘、云盘占位文件、大图片或安全软件扫描不会冻结任务详情和 Panel。
- 图片选择器仍在界面线程以原生模态窗口工作，但选择完成后的每个导入请求可以独立并发；源路径与保存目录在调度前复制并清理，空路径会直接返回可解释错误，文件系统异常统一转换为失败结果而不逃逸异步命令。
- 导入完成前若用户关闭任务详情、切换任务或退出应用，迟到结果不会再修改已经失效的字段或新任务；正常完成后才在当前长文本末尾追加 Markdown，并继续复用既有任务合并保存队列。
- 点击 Markdown 图片不再由 `CustomFieldValueViewModel` 同步调用 `Process.Start`，改为复用 `ShellPathOpenCoordinator`；字段离开当前任务时会被停用，迟到打开失败不会弹到其他任务之上，连续打开只允许最后一次请求反馈。
- 新增非阻塞、后台线程、路径快照、无效输入、异常隔离、并发导入以及真实临时目录逐字节复制测试，并增加 ViewModel 不含同步文件复制与 Shell 启动的源码契约；README 增加任务图片后台管线图。Release 构建 0 错误、0 警告，全量 687 项测试与界面冒烟通过。

## v0.10.16 - 2026-07-29

- 桌面收纳的文件双击与“打开桌面文件夹”不再从 WPF 命令同步调用 `Process.Start`；新的 `ShellPathOpenCoordinator` 在工作线程调用 Windows Shell，慢磁盘、离线路径、失效文件关联或 Explorer 短暂繁忙不会冻结收纳区、滚动或 Panel 自动收起。
- 两个打开命令显式允许并发执行；一个缓慢路径不会禁用其他文件卡片。调度前复制并规范化路径，每个请求分配单调修订号，只有最后一次点击有资格显示失败提示，旧失败不会覆盖后续成功。
- Shell 返回失败或抛出同步异常时统一转换为可恢复结果；界面仍使用 FocusPanel Fluent 对话框说明项目可能已移动、删除或无法处理，不让异常逃逸到 WPF 命令并造成 Panel 闪退。
- 新增后台线程、非阻塞、路径快照、空路径、异常隔离与最新请求所有权测试，并增加 ViewModel 不含同步 `Process.Start`/`explorer.exe` 的源码契约；README 增加桌面文件后台打开流程图。Release 构建 0 错误、0 警告，全量 679 项测试与界面冒烟通过。

## v0.10.15 - 2026-07-29

- 搜索结果整行启动、未运行固定项点击以及右键/中键“启动新实例”不再在 WPF 命令中同步执行 `_appCatalog.Launch()`；新的 `AppLaunchCoordinator` 在工作线程调用 Windows Shell，网络快捷方式、失效关联或 Explorer 繁忙不会冻结 Panel。
- 三个启动命令显式允许并发执行；一个慢启动不会禁用整条搜索结果或统一应用栏，用户可以继续启动其他应用。每次请求分配单调修订号，只有最后一次点击能关闭搜索或更新状态中心，旧失败不会覆盖新成功。
- 调度前复制仅含显示名、启动类型、目标、参数和身份的分离快照，不把 `AppLaunchItem` 上的 WPF `ImageSource`、固定状态或其他 UI 可变属性带入工作线程。
- `Process.Start`、请求构建和原有可执行文件/快捷方式/AUMID 语义保持不变；任意同步异常被协调器转换为失败结果，已释放 ViewModel 和过期结果不会触碰界面。
- 新增启动离开调用线程、异常隔离、最新请求所有权和无 UI 图标快照测试，并增加 ViewModel 无同步 `_appCatalog.Launch(app)` 的界面契约；README 增加并发后台启动图。Release 构建 0 错误、0 警告，全量 673 项测试与界面冒烟通过。

## v0.10.14 - 2026-07-29

- OKR 目标与关键结果的新增、编辑和删除不再从 `OkrViewModel` 直接创建 `AppDbContext`、执行 `EnsureSchema()` 或调用 SQLite async provider；六类 CRUD 全部迁入 `OkrWorkspaceRepository` 的工作线程持久化边界。
- 工作区快照读取、目标写入、关键结果写入和 AI 草稿保存复用单一 `SemaphoreSlim` 闸门；操作严格串行，后台刷新不会与在途保存同时读取数据库，异常路径会释放闸门并允许后续操作恢复。
- ViewModel 在调用仓库前生成不可变目标、关键结果和父目标聚合快照；数据库提交成功后才写回自增 ID、集合、目标进度与同步状态，避免把 EF 跟踪实体跨线程带回 WPF。
- 飞书目标删除仍保留 `LocalDeleted` 供后续同步，但本地工作区快照会主动排除待删除目标；删除后触发刷新时不会短暂重新出现。AI 草稿会复制完整对象图，在后台保存后把带真实主键的分离对象返回界面。
- 彻底移除桌面收纳中已无界面入口的“全局隐藏所有桌面图标”字段、命令和服务方法；产品只保留逐项 `Hidden + System` 收纳，异常与退出阶段恢复旧版本隐藏的 Explorer 图标不受影响。
- 新增 CRUD 后台线程、严格串行、读取等待在途写入、草稿复制和异常传播测试，以及 ViewModel 无数据库上下文与全局隐藏命令不存在的契约；README 增加 OKR 后台持久化图。Release 构建 0 错误、0 警告，全量 669 项测试与界面冒烟通过。

## v0.10.13 - 2026-07-29

- AI 工作区构造不再同步读取 `AppConfig`：页面先立即显示“正在读取 AI 配置”，凭据状态与模型通过后台任务加载，SQLite 短暂繁忙不会阻塞模块展开或 Panel 自动收起。
- API Key 解密、配置保存和凭据清除统一改为异步接口，并通过单一 `SemaphoreSlim` 闸门严格串行；数据库上下文只在工作线程短暂创建，快速发送或设置操作不会并发访问同一配置边界。
- 保存直接返回已提交的凭据状态与规范化模型，不再在写入后从 UI 线程额外查询一次；既有 `AppConfig` 键名、DPAPI 密文和“仅当前 Windows 用户可解密”语义保持不变。
- 初始化、发送、保存和清除均复用同一初始化任务；读取失败会打开设置并显示明确错误，释放 ViewModel 后的迟到初始化结果不会再更新页面。
- 修复混合 DPI 双屏定位：不再把主屏物理像素原点除以窗口当前 DPI；Panel 的紧凑、展开和每一帧动画均通过 `SetWindowPos` 使用物理像素锚定主屏右缘，主屏位于副屏左侧、右侧、上方或负坐标时不会再展开到屏幕夹缝。
- 右缘 3px 运行指示条复用同一物理定位边界，宽度始终为 3 个物理像素；显示设置、主屏或缩放改变后会重新读取主任务栏所在屏 DPI 并同步 Panel、热区和指示条。
- 发布包新增 `FocusPanel-win.msi`，提供 Windows 安装向导与自定义目录/当前用户/整机范围选择；原 EXE 继续用于默认目录一键安装，也支持 `--installto`。两种安装方式都保留 Velopack 根目录，后续一键更新不会跳回默认路径。
- 新增“构造函数不等待设置存储”“发送时凭据读取异常不调用 API”、偏移主屏混合 DPI、负坐标和指示条物理宽度测试；发布脚本强制生成并校验 MSI，GitHub 发布后也会验证资产存在。README 增加三张流程图。Release 构建 0 错误、0 警告，全量 666 项测试与界面冒烟通过。

## v0.10.12 - 2026-07-29

- 新增 `DesktopDropPreflight`，Explorer 外部拖入的路径复制、规范化、目录尾部分隔符清理、存在性检查和用户/公共桌面分类统一在一次后台预检快照中完成；WPF `Drop` 回调不再逐项同步访问文件系统。
- 预检按规范化完整路径大小写不敏感去重并保留首次出现顺序，同一个文件不会因重复数据格式项被收纳多次或虚增成功计数；只有 Windows 合并桌面的直接根项目会进入后续属性事务。
- 路径非法、项目已消失、存在性检查被权限或设备错误拒绝时只隔离当前项，剩余有效项目继续处理；公共桌面仍只询问一次管理员授权。
- `DesktopImportResult` 与 Fluent 结果提示现在分别报告越界路径、缺失或无效项目、重复项、授权取消和属性写入失败，不再把所有前置问题含混归为“写入失败”。
- 新增后台非阻塞、稳定顺序、大小写去重、分类计数和单项异常隔离测试；README 增加 Explorer 拖入后台预检图。Release 构建 0 错误、0 警告，全量 661 项测试与界面冒烟通过。

## v0.10.11 - 2026-07-29

- 新增可测试的 `DesktopVisibilityIo` 后台边界；收纳前的存在检查、完整原属性读取、`Hidden + System` 写入以及非提权路径的 Explorer 属性通知不再恢复到 WPF 调用上下文执行。
- 用户桌面与公共桌面继续使用同一收纳事务顺序：数据库先写入“收纳中”和原始属性，后台属性操作成功后再标记稳定；实体文件保持原路径、不改名、不移动。
- 修复公共桌面失败回滚的权限不对称：正常属性应用与异常恢复现在都复用管理员授权路径，不再出现提权收纳已改变属性、后续数据库失败却只能用普通权限回滚的状态分叉。
- 后台 I/O 异常继续传播到既有恢复事务，属性恢复失败时保留 `RecoveryRequired`，成功恢复时精确还原原始位并清理收纳标记；Panel 的拖拽交互锁和错误提示语义不变。
- 新增后台线程读取、普通写入与 Shell 通知、提权路径隔离和项目缺失测试；README 增加桌面属性后台事务图。Release 构建 0 错误、0 警告，全量 656 项测试与界面冒烟通过。

## v0.10.10 - 2026-07-29

- 新增 `AudioControlCoordinator`，把状态中心 Slider 的每次 `PropertyChanged`、静音点击和紧凑栏滚轮从同步 Core Audio 调用迁入单消费者后台控制边界；WPF Dispatcher 只负责即时显示请求值。
- 等待中的音量请求按最终值合并，音量与静音写入严格串行并携带独立修订号；旧成功结果只推进真实确认基线，不覆盖更新的界面状态，最新请求失败会精确回退到最后一次真实成功值。
- 系统状态快照增加音频写入期隔离：请求在途时拒绝旧设备快照回填，避免滑块拖动中途突然跳回；写入完成后继续复用现有后台状态刷新恢复设备可用性。
- `SystemStatusService` 不再跨线程长期持有 UI 线程创建的 COM 枚举器；每次音频读取、音量写入和静音写入都在当前调用线程初始化 COM、创建端点枚举器，并在结束时完整释放和反初始化。
- 音频写入失败与事件订阅者异常不会堵住尾随请求；正常退出停止接收新控制并排空已入队操作。新增非阻塞、最终值合并、严格串行、失败后继续、退出排空和修订隔离测试；README 增加音频后台控制链路图。Release 构建 0 错误、0 警告，全量 652 项测试与界面冒烟通过。
- 发布脚本新增 `-ReplaceCurrentVersion`：最终复核后需要重建尚未发布的同版本时，只清理该版本生成物与共享清单，保留上一完整包继续生成差分更新；与全量 `-CleanPackages` 互斥，README 同步说明正确的增量打包用法。

## v0.10.9 - 2026-07-29

- 新增 `ShellPreferenceRepository`，把主题、全屏热区、首次引导和任务栏替代状态从 `MainViewModel` 的同步 SQLite 访问中剥离；主 ViewModel 不再直接构造 `AppDbContext`。
- 启动阶段使用一次无跟踪查询生成完整 Shell 设置快照，替代原来四次独立上下文读取；加载期间抑制反向保存，避免把数据库现值当成用户修改再次写回。
- 运行期设置写入采用按键合并的单消费者后台队列：不同设置严格串行，同一设置在等待期间只保留最终值，主题切换、热区调整和替代状态变更不再执行 UI 线程 `SaveChanges`。
- 写入失败会隔离通知订阅者、保留当前会话状态并显示可恢复提示，后续设置继续处理；正常退出停止接收新值并排空已经进入队列的写入，避免最后一次选择丢失。
- 新增快照归一化、后台非阻塞、同键合并、异常后继续和退出排空测试；README 增加 Panel 设置后台持久化流程图。Release 构建 0 错误、0 警告，全量 645 项测试与界面冒烟通过。

## v0.10.8 - 2026-07-29

- 固定、取消固定和图标拖动排序的 SQLite 读写从 WPF 界面线程迁入专用后台写入闸门；连续操作严格串行，不会再因数据库短暂繁忙冻结应用栏或 Panel 自动收起。
- 固定状态不做乐观伪成功：持久化边界返回数据库中的稳定顺序，只有提交成功后才原子替换内存快照并刷新统一应用栏；提交失败完整保留原状态和顺序。
- 任务栏拖放改由统一异步交互边界承接，写入期间保持 Panel 展开，成功、失败和异常路径都会释放临时交互锁，后台异常不会逃逸至 WPF 事件回调。
- 应用退出会先停止接收新操作并排空已经进入固定项写入队列的任务，再释放图标资源；避免在途固定或排序提交被提前截断。
- 新增后台非阻塞、写入严格串行、失败后闸门恢复、稳定顺序提交和退出排空测试；README 增加固定项后台提交流程图。Release 构建 0 错误、0 警告，全量 639 项测试与界面冒烟通过。

## v0.10.7 - 2026-07-29

- 移除 `TasksViewModel` 长期持有的 EF `AppDbContext`；根任务、子任务、单项查询、新增、更新、删除和全局字段配置全部迁入 `TaskService` 的工作线程持久化边界。
- `TaskService` 继续复用单一 `SemaphoreSlim` 操作闸门，但每次操作独立创建并释放短生命周期上下文；读取使用 `AsNoTracking`，任务详情使用身份解析无跟踪查询，避免跨异步操作共享 EF 跟踪状态。
- 任务操作通过 `Task.Run` 离开 WPF 调用线程，数据库慢查询、锁等待、变更检测和 `SaveChanges` 不再阻塞模块切换、列表输入或 Panel 自动收起；异常路径始终释放闸门，下一次操作可继续恢复。
- 更新任务时只复制全部持久化标量，不把 `Parent`/`Children` 导航图附加到新上下文；删除使用主键存根并继续依赖现有数据库级级联，Inbox 的服务端保护保持不变。
- 全局自定义字段的读取、旧设置迁移与数据库保存统一进入任务持久化闸门；界面只解析已加载的内存 JSON，不再在任务详情打开或范围切换时同步查询 `AppConfigs`。
- 图片目录和全局字段的 `settings.json` 备份写入也改在工作线程执行；失败会保留明确状态或回滚目录选择，不再在原生选择器关闭后同步卡住界面。
- 新增后台非阻塞、操作串行、异常恢复、Inbox 保护、全局字段转发和持久化标量完整性测试；README 增加后台短上下文流程图。Release 构建 0 错误、0 警告，全量 634 项测试与界面冒烟通过。

## v0.10.6 - 2026-07-29

- 运行窗口的 `EnumWindows` 全量枚举、AUMID/进程身份解析、窗口分组和 Shell 图标提取从 WPF Dispatcher 移入现有合并式后台刷新边界；前台切换、窗口创建/关闭和标题变化不再把整轮系统读取压在 Panel 界面线程。
- WinEvent 的 140ms 防抖继续过滤无意义突发；后台捕获期间再次到达的多个请求只合并为一次尾随捕获，窗口快速变化不会并行枚举或无限堆积线程池任务。
- 每次窗口快照请求都会推进修订号；较旧捕获、Panel 隐藏后的迟到结果、跟踪暂停期间结果、取消结果和退出后的回调均在进入 UI 集合前被拒绝，避免应用栏短暂闪回旧窗口状态。
- 完整捕获成功后只在 `DispatcherPriority.Background` 原子提交最终快照并隔离发布订阅者；枚举或提交失败继续保留上一份有效任务栏，不把系统瞬时错误显示成运行应用全部消失。
- 主 ViewModel 在读取首份窗口与应用目录快照前先订阅变更事件，消除后台初始化恰好完成在“首次读取”和“订阅”之间时应用栏长期保持空白的竞态。
- 新增后台窗口跟踪契约、修订隔离、暂停、取消和退出测试；README 增加合并式后台快照流程图。Release 构建 0 错误、0 警告，全量 627 项测试与界面冒烟通过。

## v0.10.5 - 2026-07-29

- 修复统一应用栏多窗口列表在深色主题下偶发退回 WPF 系统菜单模板的问题；不再出现透明浅底、白色标题和系统亮蓝直角选中条的冲突组合。
- 动态菜单在创建、挂载以及 Popup 打开后都会重新应用 FocusPanel Fluent 样式，避免独立 Popup HWND 的资源解析时序覆盖应用主题。
- 为系统菜单后备模板显式提供动态 Popup 背景、主题文字、控件文字、强调选中背景和选中文字；即使自定义模板尚未完成装载，标题仍保持可读。
- 菜单级资源同时注册 `MenuItem` 与 `Separator` 默认样式，运行时生成的窗口项目和子菜单不会因容器生成顺序漏用圆角主题。
- 新增主题契约测试，覆盖动态菜单重应用、后备系统色和运行时容器样式；Release 构建 0 错误、0 警告，完整测试与界面冒烟通过。

## v0.10.4 - 2026-07-29

- 桌面收纳的新建、重命名、删除、拖拽排序、跨列移动和普通文件分类从 `FileOrganizerViewModel` 的同步 `AppDbContext` 写入迁入 `OrganizerLayoutRepository`；ViewModel 不再直接构造数据库上下文。
- 所有分区读取、布局设置保存和分区写操作复用同一个 `SemaphoreSlim` 闸门；快速连续操作严格串行，不能与后台布局快照或设置落盘并发修改 SQLite。
- 分区写操作在工作线程执行并返回“是否实际改变”；只有成功提交后才请求布局刷新，名称为空、目标缺失或无变化不会伪造成功，数据库异常会回到 Fluent 对话框明确说明并保留原界面。
- 新建与重命名使用忽略大小写的收纳盒身份，阻止只因大小写不同产生视觉上重复的分区；重命名和删除继续在同一事务中更新关联文件偏好。
- 普通分类使用最后一条同名文件偏好，与布局读取语义一致；取消分类时继续保留已收纳项目的隐藏记录，只移除不承担恢复职责的普通偏好。
- 新增纯逻辑 `OrganizerPartitionOrdering`：同列前后插入、跨列排序和移动到列尾都会同时连续重排目标列与来源列，保持现有双列语义。
- 三个分区拖拽入口统一交给既有 `StartPartitionDrop` 异步交互边界；SQLite 提交期间 Panel 保持展开，成功、失败和异常路径都会释放临时交互锁。
- 新增在途写入跟踪器；关闭模块或退出应用时先停止接收新的分区修改，再等待已经进入仓库的任务结束，最后释放文件监视器。写入失败不会阻断安全退出。
- 新增同列排序、跨列重排、非法列、缺失目标、参数归一化、异常传播、布局读取与分区写入互斥以及退出排空测试；Release 构建 0 错误、0 警告，全量 619 项测试通过。
- README 新增桌面收纳分区写入与拖拽锁流程图。

## v0.10.3 - 2026-07-29

- 桌面收纳的分区重组不再在 WPF UI 线程同步打开 SQLite、迁移旧设置并读取全部 `DesktopPartitions` 与 `DesktopFilePreferences`；新的 `OrganizerLayoutRepository` 在工作线程生成独立布局快照。
- 桌面监控、拖入拖出、自动整理、视图切换和手动刷新统一请求合并式后台布局刷新；采样期间的重复通知只保留一次尾随读取，不再在 Dispatcher 上连续堆积全表查询。
- 快照回到 UI 后继续复用 `PartitionCollectionSynchronizer` 差量同步；未变化的分区、文件卡片、展开状态、选中状态和滚动偏移保持原对象，不因后台化退回整树重建。
- 个性化分区和时间线分组提取为可测试的 `OrganizerLayoutComposer`；同名文件偏好仍以最后一条记录为准，时间线继续按今天、昨天、本周、本月和更早排序并交替分列。
- 图标缩放、列表/网格、个性化/时间线和自动整理开关改为 180ms 合并式后台保存；同一保存状态只排队一次，快速连续操作持久化最终值，数据库读取与设置写入通过同一闸门严格串行。
- 设置快照增加交互修订保护：用户调整期间完成的旧读取不能回滚当前选择；保存完成后自动补取最终数据库状态，退出前排空已经排队的设置写入。
- SQLite 读取失败时保留最后有效收纳布局；设置写入失败时当前会话选择继续生效并显示非打断提示，不再静默丢失。
- 移除收纳 ViewModel 中同步 `BuildPartitions()`、`LoadLayoutSettings()`、`SaveLayoutSettings()` 和 `Dispatcher.Invoke()` 刷新路径；文件路径、Hidden/System 属性和桌面恢复事务保持不变。
- 新增布局归一化、读取失败、读写互斥、最终设置状态、修订隔离、个性化文件分配和时间线排序测试；Release 构建 0 错误、0 警告，全量 607 项测试通过。
- README 新增桌面收纳后台布局快照流程图。

## v0.10.2 - 2026-07-29

- OKR 工作区首次打开不再在 WPF UI 线程分别读取飞书配置、同步间隔、最后同步时间和本地目标；新的 `OkrWorkspaceRepository` 在工作线程使用一个短生命周期 `AppDbContext` 生成完整快照。
- 快照读取通过合并式单消费者刷新边界执行；快速重复刷新不会并行查询 SQLite，数据库暂时繁忙时保持现有目标集合并显示可恢复提示，不把失败伪装成空数据。
- 本地目标或关键结果保存成功后推进数据修订；读取开始后发生编辑时，迟到快照会被拒绝并自动补取最新一代，旧数据不能回滚刚完成的新增、删除或进度修改。
- 手动飞书同步从线程池启动，网络请求后的 SQLite 合并、同步日志与配置写入不再恢复到 WPF Dispatcher；同步只发生推送时也会刷新目标的远端 ID 与同步状态。
- 飞书 App ID 读取、凭据保存/清除和同步间隔持久化移出界面线程；自动同步直接复用已加载的间隔，不再为了启动计时器再次同步打开数据库。
- `IOkrDataProvider` 的上下文读取、目标快照、AI 草稿创建和手动同步改为可取消的异步接口；移除同步 `Dispatcher.Invoke()`、`Task.Wait()` 和 `.Result`，AI 草稿先安全落盘再回到 UI 集合。
- 新增快照归一化、读取失败、草稿写入转发、持久化失败、修订隔离和慢仓库不阻塞构造测试；Release 构建 0 错误、0 警告，全量 596 项测试通过。
- README 新增 OKR 后台快照与异步同步边界流程图。

## v0.10.1 - 2026-07-29

- 番茄钟首次打开不再在 WPF UI 线程同步统计全部历史会话；统计通过合并式后台快照加载，数据库较大或暂时繁忙也不会阻塞工作区展开。
- 倒计时归零后立即更新内存统计、关闭悬浮计时窗并触发声音与 Toast；完整会话交给后台单消费者队列串行写入 SQLite，完成反馈不再等待 `SaveChanges()`。
- 会话持久化提取为独立 `PomodoroSessionRepository`，每次统计读取和会话写入都创建自己的短生命周期 `AppDbContext`，不与 UI 或其他模块共享 EF 上下文。
- 初始统计快照和完成会话分别使用统计修订与交互修订；完成期间到达的旧统计不能回滚乐观计数，上一轮迟到保存成功也不能覆盖新一轮、暂停、重置或修改时长后的状态文字。
- 保存失败会在当前计时状态中明确提示，但不终止队列，后续完成会话仍可继续保存；保存成功后通知主壳刷新任务与日历摘要。
- 正常退出会先停止计时和统计刷新，再排空已经进入队列的完成会话，降低完成后立即退出造成记录丢失的风险；退出后的回调不再触碰已释放 ViewModel。
- 新增统计归一化、读取失败、会话转发、无效时长、统计修订和保存结果交互修订测试；Release 构建 0 错误、0 警告，全量 588 项测试和界面冒烟通过。
- README 新增番茄钟非阻塞统计与安全落盘流程图。

## v0.10.0 - 2026-07-29

- 统一应用栏的固定项改为线程安全内存快照；窗口创建、关闭、标题变化和前台切换触发的高频 `RefreshTaskbarApps()` 不再反复打开 SQLite 查询 `PinnedApps`。
- 固定项在应用目录后台索引开始时读取并发布，构造函数和 `GetPinned()` 均不会等待缓慢数据库访问；启动期间缓存尚未就绪时立即返回空快照，加载完成后通过现有目录事件原位补齐应用栏。
- 固定、取消固定和拖动排序仍先提交业务数据库，保存成功后才原子替换内存快照并同步目录 `IsPinned` 状态；保存失败不会伪造缓存结果。
- 后台索引增加固定项修订保护：用户写入发生在索引读取期间时，旧读取结果不能覆盖较新的固定或排序状态；被新一代索引取代的迟到加载同样直接丢弃。
- SQLite 临时繁忙或固定项后台重载失败时保留最后有效快照，窗口切换期间应用栏不会因一次读取失败整批丢失。
- `GetPinned()` 使用一次固定项快照和一次目录快照完成匹配，不在循环中反复加锁；图标仍复用现有后台缓存和异步加载边界。
- 新增慢固定项存储不阻塞构造与读取、重复高频读取只访问一次存储、旧索引隔离和失败保留测试；Release 构建 0 错误、0 警告，全量 578 项测试和界面冒烟通过。
- README 新增统一应用栏固定项内存快照流程图。

## v0.9.99 - 2026-07-29

- 待办数量和日历 42 天番茄钟摘要从 WPF UI 线程同步查询改为合并式后台快照；打开日历、每 30 秒刷新和切换月份不再直接阻塞 Panel。
- 新增无状态 `TaskSummaryReader`：每轮在工作线程创建并释放独立 `AppDbContext`，任务使用服务端计数，专注记录只投影日期与分钟数后按天聚合，不加载完整实体图。
- 月份请求通过原子参数传入后台消费者；快速点击上月、下月或今天时只保留一次尾随读取，旧月份快照仍可刷新全局待办数，但绝不覆盖当前日历网格。
- 切换月份时立即显示正确的空网格，等对应快照到达再填充专注标记，不短暂复用上个月的数据。
- SQLite 临时繁忙、备份锁竞争或查询异常时保持最后有效任务数与日历摘要，不再把界面清空成 0 项；下一轮刷新仍可自动恢复。
- 退出时取消在途 Dispatcher 提交，后台数据库读取结束后也不会触碰已释放的 ViewModel。
- 新增查询范围、按日聚合、无效计数归一化、数据库失败、当前月份提交和旧月份隔离测试；Release 构建 0 错误、0 警告，全量 574 项测试和界面冒烟通过。
- README 新增日历摘要后台查询与月份隔离流程图。

## v0.9.98 - 2026-07-29

- 系统状态刷新从 WPF UI 线程同步读取改为合并式后台快照；音频、网络、输入法和电池枚举不再阻塞右缘悬停呼出、弹层动画或输入响应。
- 同一时刻只执行一轮设备读取；采样期间由 Panel 显示、状态中心打开或 2 秒计时器产生的重复请求只合并为一次尾随刷新，不会堆积并行任务。
- Core Audio 后台采样在工作线程显式初始化 COM，并使用独立 `IMMDeviceEnumerator`；不跨线程复用 UI 线程创建的枚举器，采样结束后成对释放 COM 资源。
- 状态快照仅在 WPF Dispatcher 后台优先级提交；Panel 退出会取消在途提交，迟到结果不再修改已释放绑定。
- 音量与静音写入增加修订号；用户操作期间完成的旧音频快照会被丢弃，不会把刚调整的滑块或静音状态回滚。
- 新增非阻塞请求、突发合并、单消费者、采样失败恢复、诊断失败隔离、退出取消和音频快照修订测试。
- README 新增系统状态合并式后台刷新说明与流程图。
- Release 构建 0 错误、0 警告，全量 568 项测试和界面冒烟通过。

## v0.9.97 - 2026-07-29

- 修复统一应用栏的动态多窗口菜单在旧版或部分 WPF Popup 资源继承路径下出现“浅色菜单底 + 深色主题白字”的问题。
- 菜单、菜单项和代码动态创建的窗口标题现在都显式引用 `FocusTextBrush`；主题切换时继续动态更新，不再依赖独立 Popup 是否继承主窗口前景色。
- 菜单模板把标题内容的 `TextElement.Foreground` 绑定到菜单项前景色；鼠标高亮和键盘焦点统一使用半透明强调色表面，并明确保持主题文字色，避免回退为系统蓝色直角选中条。
- UI 冒烟以“packages - 文件资源管理器”动态标题验证菜单、菜单项与标题三层前景色，同时保留不透明 Popup 表面、圆角和子菜单检查。
- Release 构建 0 错误、0 警告，主题契约测试及完整界面冒烟通过。

## v0.9.96 - 2026-07-29

- 运行窗口跟踪改为“捕获后提交”的最后有效快照：`EnumWindows` 整体失败、返回空引用或快照组合抛错时保留上一份应用栏，不再让所有运行图标瞬间消失或异常冲出 Dispatcher。
- `EnumWindows` 反向回调内部隔离每个窗口；受保护进程、标题、样式、AppUserModelID 或其他单窗口读取异常只跳过或降级该窗口，不跨越 Win32 回调边界。
- 应用身份解析异常时使用隔离的 `window:{processId}` 临时身份；Win32 连进程 ID 都无法返回时进一步按窗口句柄隔离，不按名称猜测也不把多个未知窗口合并为 `window:0`。图标提取失败回退现有首字符占位，不放弃其窗口切换能力。
- `SnapshotChanged` 改为逐订阅者安全发布；任一监听器抛错或诊断回调自身失败都不会阻断后续监听器或终止 Panel。
- WindowTracker 退出新增幂等状态：先停用跟踪、解绑计时器并解除 WinEvent；退出后到达的原生回调以及与 Dispatcher 关闭竞态的 `BeginInvoke` 均直接丢弃。
- 新增最后有效快照提交、失败保留、空快照拒绝、订阅者隔离、诊断失败和零进程 ID 窗口隔离测试；Release 构建 0 错误、0 警告，全量 559 项测试和界面冒烟通过。
- README 新增运行窗口最后有效快照与异常隔离示意图。

## v0.9.95 - 2026-07-29

- 任务栏只读守护器新增连续异常确认：单次采样看到 `Shell_TrayWnd` 临时可见、AppBar 状态短暂变化或宿主暂时不可用时，不立即停用替代模式。
- 只有连续两次、约 4 秒的同类异常才执行一次安全恢复；中间任何正常采样都会清除待确认状态，避免 Win+A、开始菜单等 Shell 交互造成误恢复和视觉回弹。
- 待确认阶段不调用 `ShowWindow`、`SetWindowPos`、`ABM_SETSTATE` 或 `SPI_SETWORKAREA`，不会为了验证而重新隐藏任务栏，也不形成占用/释放循环。
- 接管成功后记录主屏任务栏窗口句柄；Explorer 创建新的 `Shell_TrayWnd` 时准确报告 `ExplorerHostChanged`，不再仅依赖“当前是否可见”推测宿主变化。
- 守护采样绑定替代会话代际；手动恢复、退出或重新接管发生在状态读取期间时，旧会话的迟到结果直接作废，不会停止新会话或发布迟到警告。
- 紧急停用标记继续绕过连续确认，`Ctrl+Alt+Shift+F10` 仍在下一次守护检查立即恢复；手动恢复、退出和重新接管会清空所有待确认状态。
- 新增单次瞬态、连续失效、正常采样复位、原因切换、宿主句柄变化、旧会话结果隔离及紧急恢复测试；Release 构建 0 错误、0 警告，全量 551 项测试和界面冒烟通过。
- README 新增任务栏守护连续异常确认示意图。

## v0.9.94 - 2026-07-29

- 消除桌面文件拖入、拖出事件中的裸 `async void` 工作流，统一交给受观察的异步交互边界执行；同步抛出、异步失败和错误提示自身失败均不会逃逸到 WPF Dispatcher 导致 Panel 退出。
- 文件拖到分区后，在 Hidden/System 属性写入及数据库恢复记录完成前持续持有临时交互锁；WPF `Drop` 在首次 `await` 后返回源端时也不会让 Panel 提前自动收起。
- 内部文件拖放改为直接等待可测试的 `HideDraggedFileToPanel`，不再通过异步命令触发后立即返回，收纳结果与拖拽生命周期保持一致。
- 新增桌面拖拽会话状态，明确区分 Panel 自己发起的拖出与 Explorer 外部拖入；重复 `DragEnter` 不会把内部会话降级为外部会话。
- 外部拖拽离开壳层、落在内容区或完成分区收纳后均幂等复位；取消一次 Explorer 拖入后不会永久卡在“拖拽中”并阻止自动收起。
- 新增异步异常观察、清理保证、会话归属、防降级与退出复位测试；Release 构建 0 错误、0 警告，全量 544 项测试和界面冒烟通过。
- README 新增桌面拖拽交互锁与异常边界示意图。

## v0.9.93 - 2026-07-29

- 统一应用栏的同身份项目改为原位应用窗口快照；前台状态、窗口数量、窗口标题和固定状态变化不再触发 `ObservableCollection.Replace`。
- WPF 继续复用原有应用按钮、图标呈现器、焦点状态和滚动容器，只更新受影响的绑定属性，避免前台窗口切换时任务栏按钮闪一下。
- `TaskbarAppItem` 现在按属性发布活动状态、运行状态、窗口摘要、读屏名称和交互提示变化，多窗口列表及辅助功能信息仍与最新窗口快照一致。
- 不同 AppUserModelID 或可执行路径仍严格作为不同项目插入，不会为了复用视觉对象而错误合并同名应用。
- 新增活动状态及多窗口切换原位同步、属性通知和不同身份隔离测试；Release 构建 0 错误、0 警告，全量 535 项测试和界面冒烟通过。
- README 新增统一应用栏原位状态同步示意图。

## v0.9.92 - 2026-07-29

- 修复桌面收纳刷新竞态：文件监控增量刷新、手动刷新和自动整理后的完整刷新统一通过 `_refreshGate`，不再并发修改 `AllFiles`、`Files` 及其绑定视图。
- 监控批处理在已持有闸门时直接调用 `RefreshFilesCore()`，公开 `RefreshFiles()` 负责外部串行入口，避免递归获取同一 `SemaphoreSlim` 造成自锁。
- 新增文件可见性操作闸门；收纳、外部多次拖放、自动整理和拖出恢复串行修改 Hidden/System 属性及恢复记录，避免同一项目同时进入收纳和恢复。
- 等待中的自动整理在获取闸门后检查服务退出状态，Panel 已关闭时不再开始新的文件属性写入；正在进行的单项操作仍完成原子恢复语义。
- 完整刷新、增量刷新、重命名、收纳和恢复的 UI 回调均在执行前复查退出状态；Dispatcher 已关闭时直接丢弃迟到的内存同步，不触碰已释放界面。
- 收纳与恢复的 `AllFiles`/`Files` 更新统一通过 UI Dispatcher，不再依赖调用者恰好来自 UI 线程。
- 新增刷新闸门无递归、退出后回调隔离、排队自动整理停止及属性操作串行契约测试；Release 构建 0 错误、0 警告，全量 533 项测试通过。
- README 新增桌面收纳异步生命周期示意图。

## v0.9.91 - 2026-07-29

- 重构任务自动保存：移除每次属性变化直接进入数据库的 `async void` 路径，改为 180ms 合并、单消费者串行保存；快速输入标题不再并发写同一个 EF Core 上下文。
- 同一任务在一次保存进行期间继续变化时只保留一个尾随保存；不同任务保持首次变化顺序，某一项保存失败不会终止后续队列。
- `TaskService` 的查询、添加、更新和删除统一通过 `SemaphoreSlim` 操作闸门，显式命令与后台自动保存也不会同时使用同一个 `AppDbContext`。
- 切换任务范围前解除旧项目监听并排空保存队列；查询失败时恢复原项目监听和界面内容，不再因瞬时失败丢掉当前可见列表。
- 任务范围加载增加代际校验；连续快速进入不同项目时，较早完成的查询不会覆盖较新的导航结果，退出后的迟到结果也不会再修改已释放界面。
- 删除任务会先解除监听并移除待保存项，活动保存完成后再串行删除；失败时重新监听并展示错误，不从界面误删项目。
- 退出时先停止接收新变化、排空队列并等待任务数据库空闲，最后才释放上下文，避免末次编辑落盘途中命中已释放对象。
- 新增合并、稳定排序、尾随保存、失败隔离、观察者异常隔离、丢弃、排空与退出契约测试；Release 构建 0 错误、0 警告，全量 529 项测试通过。
- README 新增任务编辑到安全落盘的生命周期示意图。

## v0.9.90 - 2026-07-29

- 修复统一应用栏多窗口列表在深色主题下仍可能显示白字浅底、选中行文字对比不足的问题。
- 新增 `FocusPopupSurfaceBrush`：独立 HWND 的菜单、子菜单、下拉框、工具提示和桌面收纳工具弹层不再复用依赖 DWM 背景取样的半透明壳层表面。
- Popup 深色主题固定使用不透明深色表面，浅色主题使用不透明白色表面；文字继续使用动态 `FocusTextBrush`，运行期间切换主题立即生效。
- 运行时任务栏菜单改为每次在视觉树外创建新实例、递归应用 Fluent Style 后再打开；取消 WPF 已开始解析的旧菜单，避免回退到系统浅色模板。
- UI 冒烟新增独立弹层表面资源、完全不透明和运行时样式挂接检查；Release 构建 0 错误、0 警告，全量 521 项测试通过。
- README 新增独立 Popup 与 DWM 壳层的主题隔离示意图。

## v0.9.89 - 2026-07-29

- 重构未处理异常恢复：无论数据库结构异常还是未知 UI 异常，均先幂等恢复原生任务栏和桌面图标，再记录日志并安全退出，不在可能损坏的进程状态中继续运行。
- 删除异常路径中对安装目录 `focuspanel.db` 的错误删除及 `ResourceAssembly.Location` DLL 启动逻辑；运行期崩溃不再删除、覆盖或自动重建业务数据库。
- 崩溃日志从可能只读的安装目录迁移到 `%LOCALAPPDATA%\FocusPanel\Logs\crash.log`，目录按需创建，日志写入失败也不会阻断系统壳层恢复。
- 启动恢复采用显式状态机：只有原数据库成功归档后，且没有有效备份可恢复时，才允许创建新库；归档失败或恢复后的数据库仍不兼容时保持文件不动并停止启动。
- 移除启动兜底中的无条件 `EnsureDeleted()`；新库创建前原文件已经带时间戳归档，任务、番茄钟、OKR、固定应用和桌面收纳数据均保留人工救援入口。
- AppDomain 后台线程异常不再从非 UI 线程弹系统对话框；日志和任务栏恢复保持最佳努力，避免异常处理器自身再次出错。
- 新增异常分类、嵌套 SQLite 错误、恢复状态机、日志位置、日志创建/失败隔离和源代码安全契约测试；Release 构建 0 错误、0 警告，全量 521 项测试与 UI 冒烟通过。
- README 新增崩溃与数据库安全恢复流程图。

## v0.9.88 - 2026-07-29

- 修复统一应用栏的多窗口列表在部分设备深色主题下仍退回 WPF 系统菜单模板的问题；窗口标题不再以浅色文字叠在系统浅色背景上。
- 运行时菜单不再对尚未挂接视觉树的 `ContextMenu` 设置延迟 Style 资源引用，改为从应用资源直接取得 `FocusContextMenu`、`FocusMenuItem` 和 `FocusMenuSeparator`。
- 菜单项与所有嵌套子菜单递归应用同一份 Fluent Style，动态颜色仍通过主题令牌实时切换；深色、浅色及运行期间切换主题无需重建业务数据。
- 同时设置菜单的 `ItemContainerStyle`，后续由 WPF 自动生成的容器也不会回退到系统蓝色直角选中态。
- UI 冒烟增加运行时菜单和嵌套菜单的真实 Style 引用检查；Release 构建 0 错误、0 警告，全量 511 项测试通过。
- README 新增运行时菜单主题解析示意图。

## v0.9.87 - 2026-07-29

- 桌面收纳的收纳盒操作、视图选项、新建收纳盒和修复工具四类独立 `Popup` 统一接入单一活动弹层协调，不再允许多个浮层并存。
- 新弹层打开时关闭旧弹层；旧弹层稍后到达的 `Closed` 事件不会误清理当前新弹层，transient-interaction 深度保持成对，避免 Panel 提前收起或关闭后仍被锁住。
- Popup 打开后通过 `FocusNavigationDirection.First` 把键盘焦点送入第一个可操作控件；新增收纳盒直接进入输入框，其余弹层进入首个选项或按钮。
- 四类 Popup 独立处理 `Esc`：关闭当前弹层、通过双向绑定复位 ToggleButton，并在关闭完成后把焦点还给原 `PlacementTarget`。
- 文件右键菜单打开前也会关闭活动工具弹层；切换模块导致桌面收纳卸载时主动关闭 Popup 并幂等释放交互锁。
- 新增可测试的 `ExclusiveSurfaceTracker<T>`，覆盖首次激活、替换、重复激活、旧表面延迟关闭和清理；结合 XAML 键盘契约，全量 511 项测试通过。
- README 新增桌面收纳弹层互斥与键盘路径图。

## v0.9.86 - 2026-07-29

- 修复桌面文件右键菜单资源默认共享实例的问题；网格、列表和视口虚拟化容器现在通过 `x:Shared="False"` 获得独立 `ContextMenu`，不在不同文件卡片间泄漏打开状态或 DataContext。
- 菜单 DataContext 显式绑定当前 `PlacementTarget.DataContext`，即使视觉容器被回收后承载另一文件，也会在打开时重新解析真实目标。
- 菜单打开事件先从 `PlacementTarget` 收窄到 `FrameworkElement` 并取得当前 `DesktopFile`，再调用现有 `SelectFileCommand`；“移动到”“收纳到面板”等依赖 `SelectedFile` 的动作不会误操作上一次选中项。
- 文件菜单显式引用 `FocusContextMenu`，并保留现有菜单项 Fluent 样式与 transient-interaction 锁；深色主题、右键操作和 Panel 自动收起使用同一生命周期。
- UI 冒烟真实连续解析两次 `FileContextMenu`，验证实例互不相同且都引用全局 Fluent 样式；新增当前卡片绑定与右键目标契约，全量 506 项测试通过。
- README 新增桌面右键菜单目标安全链路图。

## v0.9.85 - 2026-07-29

- 修复统一应用栏多窗口列表在深色主题下退回 WPF 默认浅色 `ContextMenu`、导致浅色窗口标题与背景失去对比度的问题。
- 三处主壳菜单显式引用 `FocusContextMenu`；运行时创建的应用菜单递归套用 `FocusContextMenu`、`FocusMenuItem` 与 `FocusMenuSeparator`，不再依赖独立 Popup 的隐式资源查找。
- 菜单容器与菜单项统一使用 `Segoe UI Variable Text`，背景、文字、悬停、当前窗口标记和分隔线继续使用动态主题资源，运行期间切换深浅色无需重建菜单。
- 紧凑栏布局按交互顺序重排为“开始、搜索、统一应用列表、任务视图、Focus 中心、状态中心、时间”，六个固定入口与中部应用区的层级更加明确。
- 下拉选择展开状态纳入自动收起判断：即使 Popup 使用独立窗口、鼠标没有被捕获，也会在 `ComboBox.IsDropDownOpen` 为真时保持 Panel 展开；关闭后恢复原自动收起语义。
- 新增瞬态表面组合策略及菜单主题契约测试；Debug 构建 0 错误、0 警告，505 项测试通过。
- README 新增紧凑栏顺序与深色菜单可读性图示。

## v0.9.84 - 2026-07-29

- 修复“新增桌面项目自动按类型收纳”开关实际在任意桌面变化后调用 `OrganizeAllFiles()`、从而可能把开启前的全部可见项目一起收纳的问题。
- 文件监控批次新增独立 `CreatedPaths`：只有 `Created` 事件进入自动收纳候选；普通写入、属性变化、删除、既有项目重命名、启动全量扫描和监视器错误恢复均不会触发。
- 同一新增路径的连续 `Created/Changed` 事件保持一次新增身份；复制过程从临时名称重命名为最终名称时把身份迁移到新路径，避免漏收或重复处理。
- 自动规则按规范化完整路径匹配最新可见集合，过滤已收纳、需要恢复、已删除和无效项目；不按文件名猜测，也不会重新整理无关既有项目。
- 新增带 15 秒过期的一次性内部路径抑制器：取消旧版仓库收纳和“救援桌面”创建的项目不会被自动规则立即抓回；过期后不影响同路径未来的真实外部创建。
- 公共桌面项目不会在后台弹出提权窗口，保持可见并在设置区提示手动授权；自动成功、部分成功和失败也使用非打断式状态反馈。
- 自动收纳开关继续写入原 `AppConfig`；本次会话立即按内存状态生效，持久化失败时明确说明，不伪装为已经永久保存。
- 新增路径去重、重命名继承、全量扫描隔离、内部操作抑制、过期保护、可见项筛选及状态文案测试；全量 500 项测试通过。
- README 新增“只收纳新增项目”流程图并修正番茄钟通知说明。

## v0.9.83 - 2026-07-29

- 修复 0.9.82 发布资产错误沿用 0.9.81 更新说明的问题；应用功能与数据格式不变。
- 打包前强制校验 `packaging/release-notes.md` 首行必须精确匹配目标版本，说明遗漏更新时立即中止，不再生成内容错误但表面可用的 Release。
- 正式 Latest 版本前移至 0.9.83，允许已安装 0.9.82 的设备继续通过设置页获取差分更新。

## v0.9.82 - 2026-07-29

- 将“发现更新”和“番茄钟完成”从 Explorer 托盘气泡迁移为 FocusPanel 自有的无激活 Fluent Toast；隐藏原生任务栏或 Explorer 通知宿主异常时，核心通知仍然可见。
- Toast 复用 Windows 11 DWM 原生圆角与 Desktop Acrylic，外层只有一块毛玻璃表面；透明效果、高对比度、远程桌面或 DWM 不可用时继续沿用现有不透明主题降级。
- Toast 使用 `WS_EX_NOACTIVATE | WS_EX_TOOLWINDOW`，显示和自动消失不会抢走当前应用的键盘焦点；悬停时暂停计时，离开后继续自动关闭。
- 更新通知可直接打开设置与更新区域，专注完成通知可直接展开番茄钟工作区；用户主动点击后才激活 Panel。
- 新增稳定 FIFO 通知队列，相同类型当前通知不会重复堆叠，等待队列中的同类通知只更新内容且不改变顺序。
- 关闭到托盘、正常退出和显示器布局变化分别完成通知清理、资源释放与位置重算；托盘图标仅保留显示、恢复原生任务栏和安全退出菜单。
- 新增队列顺序、去重、替换和清理测试，以及无激活、DWM 背景、语义字体和禁止 `ShowBalloonTip` 的界面契约；全量 489 项测试与 UI 冒烟通过。
- README 新增无激活原生毛玻璃通知图示。

## v0.9.81 - 2026-07-29

- 将任务长文本“插入图片”从 ViewModel 直接创建 `Microsoft.Win32.OpenFileDialog` 改为可替换的 `IFilePickerService` 与 `WindowsFilePickerService`，系统 UI 边界不再混入业务逻辑。
- 现代文件选择窗口显式使用当前活动 FocusPanel 窗口作为 Owner；打开期间复用临时交互租约，Panel 和任务详情不会因系统窗口取得焦点而自动缩回。
- 新增类型化 Selected/Canceled/Failed 结果和纯 `FileSelectionPolicy`；取消不写正文、不显示错误，系统异常或空路径才进入统一 Fluent 错误提示。
- 选图标题改为“选择要插入的图片”，过滤器改为中文“图片文件”，默认从 Windows 图片目录打开；生成的 Markdown 替代文字从 `Image` 改为“图片”。
- `WpfFileDialogBoundary` 保留 Windows 现代通用文件对话框行为，启用文件与路径存在检查、单选和扩展名处理。
- 自动测试使用替代文件选择边界，覆盖请求与结果转发、取消、异常、空标题/过滤器、交互锁、选择/取消/失败决策；不会弹出真实系统窗口。
- XAML/源码契约禁止任务 ViewModel 再直接实例化 `OpenFileDialog`，并确保 Owner 与交互锁持续存在。
- README 新增现代图片选择器图示。

## v0.9.80 - 2026-07-29

- 移除任务图片保存路径使用的老式 `System.Windows.Forms.FolderBrowserDialog`，改用 Windows Shell 公开的 `IFileOpenDialog` 现代通用文件夹选择器；继续保持 .NET 7，不引入额外 UI 包。
- 按微软公开接口启用 `FOS_PICKFOLDERS | FOS_FORCEFILESYSTEM | FOS_PATHMUSTEXIST | FOS_NOCHANGEDIR | FOS_DONTADDTORECENT`，用 `SHCreateItemFromParsingName` 定位现有目录，并通过 `SIGDN_FILESYSPATH` 获取选择结果。
- 选择器使用当前活动 FocusPanel 窗口句柄作为 Owner，标题和确认按钮中文化；仅在应用窗口不可用时回退当前前台句柄。
- 文件夹选择流程接入 0.9.79 的临时交互租约，系统对话框打开期间 Panel 不会自动缩回，正常选择、取消或 COM 异常均会成对释放。
- 新增 `IFolderPickerService`、Shell 原生边界、类型化 Selected/Canceled/Failed 结果和纯 `FolderSelectionPolicy`；取消不修改当前路径，失败显示中文原因。
- 保存新路径失败时立即回滚界面和内存设置到原值，避免界面显示已更改但重启后反弹。
- 自动测试使用替代 Shell 边界，覆盖请求与 Owner 转发、取消、异常、缺少标题、交互锁、选择/取消/失败决策；契约禁止任务页面重新引入 FolderBrowserDialog。
- README 新增 Windows Shell 现代文件夹选择器图示。

## v0.9.79 - 2026-07-29

- 修复 0.9.78 Fluent 模态窗口成为前台后，主 Panel 收到 `Deactivated` 并可能在 220ms 后自动收起的问题；确认过程现在始终保留背后的工作区上下文。
- 新增可测试的 `IFocusDialogInteractionHost` 与幂等 `FocusDialogInteractionLease`，对话框显示前进入现有 transient-interaction 锁，确认、取消、关闭或异常退出时均通过 `Dispose` 成对释放。
- 覆盖正常进入/退出、重复释放和无宿主三种生命周期测试，避免对话框关闭后 Panel 永久保持展开或交互深度变负。
- 应用搜索整行启动按钮和 Focus 中心“最近使用”复合按钮新增动态 UI Automation 名称，图标与绑定文字组合不再对读屏暴露为空名称。
- OKR 同步状态图标和任务看板数量徽标移除页面硬编码 `12/8` 圆角，统一引用 `FocusControlCornerRadius`。
- 新增全视图契约：复合 Button 必须提供 Content、ToolTip 或 AutomationProperties.Name；除 DWM 客户区零圆角与 2px 状态标记外，页面不得绕过共享几何令牌。

## v0.9.78 - 2026-07-29

- 新增统一 `FocusDialogWindow` 与 `FocusDialogService`，任务、OKR、桌面收纳、任务栏接管、数据库恢复、系统电源和自动更新等运行期确认及错误提示不再跳回白底直角的 WPF `MessageBox`。
- 模态窗口复用 DWM 原生圆角与 Desktop Acrylic、动态壳层画刷、语义文字、Segoe Fluent Icons 和统一按钮模板；跟随深色、浅色、高对比度及关闭透明效果的降级策略。
- 完整支持 OK、确定/取消、是/否、是/否/取消四种返回语义；危险确认的“是”使用动态危险按钮，默认焦点落在安全的“否”，Enter、Esc、窗口关闭与原命令判断保持一致。
- 对话框自动选择当前活动窗口为 Owner、跨线程调用回到 UI Dispatcher、长更新说明使用可滚动正文，并补充读屏名称。
- 桌面救援、插入图片和打开图片失败提示完成中文化；应用资源尚未初始化时仍可回退系统 MessageBox，数据库灾难恢复链路不被自定义 UI 反向阻塞。
- UI 冒烟真实加载信息与危险确认对话框，验证按钮可见性、动态样式、安全默认项和文字；发布版直接渲染对话框根视觉快照检查通过。
- 新增运行期对话框契约，ViewModel 与 View 代码再次直接调用系统 MessageBox 时自动失败；README 新增 Fluent 模态对话框图。

## v0.9.77 - 2026-07-29

- 修复应用搜索结果唯一仍未绑定样式的启动按钮；整行点击区域改用 `FocusRowButton`，继续保持至少 44px 点击区、拉伸内容和统一单层圆角 Chrome，不再出现 WPF 原生矩形边框。
- 新增 `FocusDangerButton` 与动态 `FocusDangerSoftBrush`，统一退出 FocusPanel、关机、删除目标、删除分区、清除凭据和移除 API Key 等文字危险操作。
- 危险操作在深色、浅色和 Windows 高对比度下分别使用动态柔和背景、危险前景与描边；主题服务切换时同步更新，不再由各页面手写前景色。
- `FocusIconButton` 内容宿主开始尊重水平与垂直内容对齐，派生的整行按钮可以真正拉伸内容，同时保留共享悬停、按下、禁用和键盘焦点状态。
- 新增全视图按钮契约：任何 `Views/*.xaml` 中没有共享或局部样式的 Button 都会让测试失败；UI 冒烟真实创建行按钮与危险按钮，验证圆角、点击区、字重和动态资源。
- README 新增 Fluent 操作按钮状态图，发布版构建与完整测试通过。

## v0.9.76 - 2026-07-29

- 完成全部主要界面字体审计，将 18 种页面直接字号收敛为共享语义层级：页面标题、章节标题、空状态标题、卡片标题、正文、辅助正文、说明、元信息、指标和计时展示。
- 新增 `FocusTextBase` 及 10 个语义文字样式；标题与指标统一使用 `Segoe UI Variable Display`，正文继续使用 `Segoe UI Variable Text`，辅助文字继续引用动态弱化画刷。
- 主壳、Dashboard、桌面收纳、任务、番茄钟、OKR、AI、日历、任务详情和浮动番茄钟全部迁移；图标、紧凑栏时间和“需恢复”徽标保留确有用途的专用尺寸。
- 清理 78 处非图标 TextBlock 直接字号，当前仅保留 3 个紧凑栏时间与恢复徽标专用值；资源契约会在页面重新绕过语义字体样式时失败。
- UI 冒烟真实创建页面、章节、卡片、正文、说明、元信息、指标和计时文字，验证字号、字重、字体与动态辅助文字画刷。
- Dashboard 与日历发布版快照渲染检查通过；README 新增统一 Fluent 字体层级图。

## v0.9.75 - 2026-07-29

- 修复应用搜索与任务列表各自维护选中模板、后续页面容易漏回 WPF 默认浅蓝背景和系统前景色的问题；新增全局 `FocusListBox` 与 `FocusListBoxItem`。
- 选中态统一使用动态 `FocusAccentSoftBrush` 背景、`FocusAccentBrush` 描边和 `FocusTextBrush` 文字，深色、浅色及 Windows 强调色变化下均保持可读。
- 悬停、键盘焦点、禁用状态和至少 44px 点击区全部作用于同一个圆角 `ListItemChrome`，不再叠加系统矩形焦点框。
- 应用搜索仅保留 54px 行高与零内边距差异；任务列表仅保留内容间距差异，两者不再复制控制模板。
- UI 冒烟真实创建已选列表项并验证动态前景、强调背景、描边、圆角和点击区；资源契约确保搜索与任务页面持续继承公共样式。
- README 新增统一 Fluent 列表状态图。

## v0.9.74 - 2026-07-29

- 完成全量 `Views/*.xaml` 状态色审计，清理最后 3 处页面硬编码颜色：右缘指示条 `#C8FFFFFF`、重命名遮罩 `#50000000` 和 Explorer 系统限制警告 `#33FFB020`。
- 新增 `FocusEdgeIndicatorBrush`：深色主题保留清晰半透明白色提示，浅色主题改用当前 Windows 强调亮色，高对比度使用系统 Highlight；3px 物理宽度、无激活和点击穿透语义不变。
- 新增 `FocusOverlayBrush`，桌面收纳重命名遮罩根据深浅色使用不同透明度，不再在浅色表面形成过重黑层。
- 新增 `FocusWarningBrush`、`FocusWarningSoftBrush` 和 `FocusWarningTextBrush`，系统限制说明的背景、描边和文字分别适配深色、浅色及高对比度。
- UI 冒烟真实加载 `EdgeIndicatorWindow` 与 `FileOrganizerView`，验证指示表面和重命名遮罩引用应用级动态资源；资源契约禁止任何视图 XAML 回归直接十六进制颜色。
- README 新增动态状态色令牌图。

## v0.9.73 - 2026-07-29

- 修复任务视图分段按钮使用整块高饱和蓝色、桌面收纳工具栏 ToggleButton 复制弱化模板，以及文件选中态硬编码 `#26006FC4/#FF007AFF`、无法跟随 Windows 强调色的问题。
- 新增动态 `FocusAccentSoftBrush`；`ThemeService` 从当前 Windows 强调色生成深色/浅色对应透明度，主题切换或系统强调色变化后同步更新。
- 新增通用 `FocusToggleButton`：单层圆角 Chrome、柔和选中背景、动态强调描边、悬停、按下、禁用和键盘焦点状态完整；收纳页 5 个工具栏开关改为继承该基类。
- `FocusSegmentRadioButton` 改为低饱和选中表面、动态文字和底部强调标记，不再用整块实色蓝表达列表/看板/字段选择；补齐按下和禁用状态。
- 收纳文件网格与列表选中态统一使用 `FocusAccentSoftBrush` 和 `FocusAccentBrightBrush`，移除页面内静态颜色资源。
- UI 冒烟真实创建已选及禁用 ToggleButton、已选 RadioButton，验证圆角、动态资源、底部标记和状态透明度；资源契约覆盖收纳页全部 5 个开关。
- README 新增统一 Fluent 选择状态图。

## v0.9.72 - 2026-07-29

- 修复搜索、任务、OKR、桌面重命名和 AI 输入框虽然已有圆角表面，但文本选择仍可能使用系统蓝色、只读/禁用状态不清晰、键盘焦点叠加额外系统焦点框的问题。
- 新增语义正确的通用 `FocusTextBox`；全部普通 TextBox 默认继承该样式，原 `FocusSearchBox` 保留为兼容别名，现有页面无需迁移且新页面不再错误复用搜索专用命名。
- TextBox 与 PasswordBox 统一使用 Segoe UI Variable Text、主题文字、强调色光标、主题选择背景和选择文字；选择透明度保持可读性。
- 悬停、键盘焦点、只读和禁用状态均作用于同一个圆角 Chrome；键盘焦点通过 2px 强调描边表达，不嵌套矩形 FocusVisual。
- PasswordBox 新增隐式主题兜底，现有 OpenAI API Key 与飞书 App Secret 继续显式使用同一模板。
- UI 冒烟真实执行文本全选、只读切换和密码框禁用，验证内容宿主、圆角、动态画刷、字体、选择范围和状态透明度。
- README 新增统一 Fluent 输入状态图。

## v0.9.71 - 2026-07-29

- 修复状态中心音量仍使用 WPF 默认 Slider、系统蓝色轨道与方形交互反馈的问题；新增全局 `FocusSlider`、圆形滑块、双色圆角轨道和 44px 最小交互高度。
- 修复电池和更新进度仍可能回退系统默认 ProgressBar，以及旧 `FocusLinearProgress` 缺少 WPF 标准 `PART_Track`、完成比例指示宽度可能为 0 的问题。
- 全局 ProgressBar 统一使用圆角动态主题轨道；确定状态按真实比例填充，不确定状态隐藏比例指示并使用强调色脉冲加载层。
- Slider 保留标准 `PART_Track`、方向、值绑定、轨道大步调整、拖动与键盘语义；横向与纵向模板共用同一套资源。
- UI 冒烟真实验证音量值传入 Track、已选/未选动态画刷、圆角滑块、确定进度宽度及运行时切换加载状态。
- README 新增统一 Fluent 滑块与进度状态图。

## v0.9.70 - 2026-07-29

- 修复主壳、Focus/状态中心、桌面收纳、任务、OKR、AI、下拉框和菜单内部仍可能显示 WPF 默认宽滚动条、浅色轨道与直角滑块的问题。
- 新增全局 `FocusScrollBar`、圆角 `FocusScrollThumb` 和透明分页轨道；纵向与横向均使用 10px 紧凑布局，滑块悬停时动态切换为主题强调色。
- 保留标准 `PART_Track`、方向绑定、轨道分页命令、拖动、滚轮和虚拟化语义；纵向滑块最小高度与横向滑块最小宽度均为 28px。
- UI 冒烟真实创建同时溢出的 `ScrollViewer`，验证纵横两个滚动条、圆角模板、动态画刷、可滚动范围与紧凑尺寸；资源契约禁止系统滚动颜色回归。
- README 新增统一 Fluent 滚动系统结构图。

## v0.9.69 - 2026-07-29

- 修复 GitHub Actions 的 Velopack 打包步骤偶发使用系统代码页解释中文发布说明、导致设置页更新内容乱码的问题。
- 发布说明中间文件改用带 BOM 的 UTF-16 LE，由 .NET 和 Velopack 自动识别，不再依赖构建机活动代码页。
- 打包完成后读取 `releases.win.json`，按版本定位完整包并逐字校验 Markdown；缺少完整包或说明内容发生变化时直接终止发布。
- 保留 0.9.68 的 Fluent CheckBox、深浅色动态文字、44px 点击区域以及全部业务绑定。

## v0.9.68 - 2026-07-29

- 修复设置选项、AI 本地摘要授权、任务完成和多选字段仍使用 WPF 默认方形 CheckBox、系统蓝色勾选与不一致点击区域的问题。
- 新增完整 `FocusCheckBox` 模板：20px 圆角标记、Segoe Fluent 勾选字形、44px 最小点击区和单层圆角交互表面统一使用现有设计令牌。
- 悬停、按下、选中、不确定和禁用状态全部显式定义；选中状态使用强调色背景与亮色描边，未选中状态跟随柔和表面和动态描边。
- 保留复选框业务语义，不把任务完成、多选字段等操作错误改造成 ToggleSwitch；全部现有 CheckBox 继续沿用原有 `IsChecked` 绑定和命令。
- UI 冒烟真实创建已选 CheckBox，验证模板部件、勾选可见性、动态画刷和 44px 点击区；资源契约覆盖所有现有视图和三态边界。
- README 新增统一 Fluent 勾选控件结构图。

## v0.9.67 - 2026-07-29

- 修复设置页主题选择仍使用系统默认 ComboBox，以及 AI 模型、任务字段、任务详情和 OKR 间隔下拉层仍可能退回浅色直角 Popup 的问题。
- 新增完整非编辑式 `FocusComboBox` 模板，统一封闭按钮、Segoe Fluent 箭头、鼠标悬停、展开状态、键盘焦点、禁用状态和最大下拉高度。
- 新增 `FocusComboBoxItem`，以单层圆角表面、柔和选中背景和左侧强调标记显示当前项；不使用系统蓝高亮，避免浅色主题下文字对比度失控。
- 下拉 Popup 使用动态 `FocusSurfaceStrongBrush`、`FocusStrokeBrush` 和卡片圆角，不叠加矩形外框或黑色投影；所有现有 ComboBox 均为非编辑式，业务绑定语义保持不变。
- UI 冒烟真实创建 ComboBox、Popup 和 ComboBoxItem，验证模板部件、圆角及动态资源；资源契约覆盖设置页隐式样式和不可编辑边界。
- README 新增统一 Fluent 下拉选择结构图。

## v0.9.66 - 2026-07-29

- 修复按钮、应用状态、文件路径和输入控件提示仍沿用 WPF 默认矩形模板的问题；工具提示现在使用单层 Fluent 圆角表面。
- 移除工具提示硬编码的深色背景、白色文字和固定描边，统一改用动态 `FocusSurfaceStrongBrush`、`FocusTextBrush` 与 `FocusStrokeBrush`，浅色和深色主题均保持一致。
- 工具提示明确关闭系统投影，不再在圆角外出现黑边；最大宽度固定为 360px，多行应用操作说明可自然换行。
- UI 冒烟真实创建包含多行内容的 ToolTip，验证圆角模板、动态资源实例与无投影状态；资源契约同时禁止旧硬编码颜色回归。
- README 新增统一 Fluent 工具提示结构图。

## v0.9.65 - 2026-07-29

- 统一应用栏补齐 Windows 任务栏常用的鼠标语义：`Shift+左键` 或鼠标中键可直接启动应用新实例，普通左键仍负责启动、切换/最小化或展开多窗口列表。
- 新增纯逻辑点击决策边界，区分普通左键、Shift+左键和中键；自动测试不会启动、切换或关闭真实应用。
- 只有具备可靠 EXE、AUMID 或固定启动目标的应用才提供新实例操作；受保护进程或临时运行身份不会显示误导提示，也不会猜测启动目标。
- 应用工具提示新增完整操作说明，读屏帮助文本同步复用同一语义；拖动固定与排序路径保持不变。
- 打包脚本显式把中文发布说明转换为 UTF-8 BOM 临时文件，兼容 Windows PowerShell 5 与 GitHub Actions，更新界面不再显示乱码说明。
- README 新增统一应用栏鼠标操作图。

## v0.9.64 - 2026-07-29

- 修复开始按钮右键、应用管理、多窗口列表、关机子菜单和托盘菜单仍使用 WPF 默认浅色模板的问题；深色主题下不再出现浅底白字、系统蓝高亮或直角菜单。
- 新增单层 `FocusContextMenu` 表面和完整 `FocusMenuItem` 模板，统一圆角、边距、文字、悬停、键盘焦点、禁用与子菜单展开状态；不叠加黑色投影或矩形外框。
- 静态菜单、嵌套子菜单以及代码运行时创建的应用/窗口菜单通过隐式样式自动继承主题，不需要逐项手工指定样式，也不会因窗口数量变化退回系统外观。
- 当前窗口继续显示 Fluent 勾选标记，子菜单使用一致箭头；超长窗口标题、键盘导航、读屏名称和自动收起交互保持不变。
- 分隔线改为动态 `FocusStrokeBrush`，开始管理菜单的多组功能层级在浅色、深色与系统强调色变化下保持一致。
- UI 冒烟新增真实 ContextMenu、勾选项、分隔线和子菜单模板加载验证，界面契约确认没有引用 `SystemColors.Highlight`。
- README 新增统一 Fluent 任务栏菜单结构图。

## v0.9.63 - 2026-07-29

- 补齐应用搜索的完整键盘路径：展开后立即聚焦并全选搜索框，上下方向键移动结果，回车启动当前项；没有显式选择时默认启动首项。
- 搜索结果增加双向选中绑定和统一 Fluent 选中表面；键盘移动会自动滚动到可见区域，不新增矩形/圆角嵌套或不存在的主题资源。
- `Esc` 关闭搜索后把焦点返回紧凑栏搜索入口，用户可以继续浏览六入口任务栏，无需先点击 Panel。
- 应用目录后台补全或固定状态变化后，按 AppUserModelID/可执行路径身份恢复原选择；原项目消失时安全回退首项，避免高频输入期间随机跳动。
- 修复搜索结果文字依赖 WPF 默认控件颜色的问题：列表、启动按钮和标题显式跟随动态 `FocusTextBrush`；选中项使用 `FocusSurfaceSoftBrush`，不再出现深色主题下系统蓝底与低对比度文字。
- 自动更新从需要枚举匿名 GitHub Releases API 的 `GithubSource` 切换为 Latest Release 静态清单；真实验证 `releases.win.json`、`RELEASES` 和完整包直链均返回 200，避免共享 IP 达到 API 限额后的 403。
- 设置页增加“打开官方下载页”兜底；403、超时、权限和文件占用异常转换为中文可操作提示，不再直接展示原始英文 HTTP 异常。
- 搜索框补充 UI Automation 名称与键盘说明；新增纯逻辑边界、更新失败分类、静态源及界面接线契约测试，不启动真实应用。
- README 新增应用搜索键盘流程图和 GitHub 静态清单更新图。

## v0.9.62 - 2026-07-29

- 重做统一应用栏的运行反馈：移除难以辨认的 6px 圆点，改为与右侧竖向任务栏方向一致的 4px 圆角状态条；后台运行 12px，当前活动 24px。
- 当前活动应用增加单一 `FocusSurfaceSoftBrush` 柔和表面，不新增外框、阴影或矩形/圆角嵌套；固定但未运行的图标继续保持干净。
- 状态条显式设置 `IsHitTestVisible=False`，覆盖在按钮上方时也不会吞掉图标左缘点击、拖动或右键操作。
- `TaskbarAppItem` 新增统一状态、辅助功能名称和操作提示，明确区分已固定未运行、后台运行和当前使用，以及启动、单窗口切换和多窗口列表语义。
- 工具提示不再只显示模糊的“1 个窗口”；读屏名称会播报“编辑器，正在使用 · 2 个窗口”一类完整状态，帮助文本说明左右键动作。
- 新增 5 项活动、后台、固定未运行、异常降级和 XAML 命中安全测试；不启动或操作真实应用。
- README 新增统一应用栏三种状态图。

## v0.9.61 - 2026-07-29

- 修复多窗口应用左键复用完整右键管理菜单、每次切换还要进入“窗口标题 → 切换到此窗口”二级子菜单的问题；左键现在只展示一层可直接点击的文字窗口列表。
- 右键菜单继续保留启动新实例、固定/取消固定、逐窗口切换、逐窗口关闭和关闭全部窗口，高频切换与低频管理不再堆在同一层级。
- `WindowReference` 新增精确前台状态；同一应用的两个窗口之间切换时，即使应用级活动状态始终为真，统一应用栏仍会更新具体窗口的当前标识。
- 左键列表和右键窗口项均以勾选状态标记当前窗口；辅助功能名称增加“当前窗口”前缀，键盘和读屏用户能够识别前台项。
- 超长窗口标题使用 340px 单一表面和字符省略，不扩张弹层宽度；辅助功能仍保留完整标题，空标题安全回退到应用名称。
- 新增同应用前台窗口变化的增量集合测试和左右键菜单接线契约；自动测试不激活、最小化或关闭真实窗口。
- README 新增多窗口应用一层直接列表图。

## v0.9.60 - 2026-07-29

- 修复窗口跟踪只监听 `SHOW/HIDE`、标题和前台变化，未监听 `CREATE/DESTROY` 的缺口；新窗口可以在创建阶段触发刷新，最后窗口销毁后不再依赖后续偶然事件才移除运行图标。
- WinEvent 注册覆盖连续的 `EVENT_OBJECT_CREATE`、`DESTROY`、`SHOW`、`HIDE` 范围，并继续单独监听 `NAMECHANGE` 与 `SYSTEM_FOREGROUND`，满足统一应用栏的完整窗口生命周期。
- 新增纯逻辑 `WindowTrackingEventPolicy`，只允许 `OBJID_WINDOW` 窗口本体通过；控件、菜单、滚动条等子对象事件不会驱动完整顶层窗口枚举。
- 全局钩子加入 `WINEVENT_SKIPOWNPROCESS`；FocusPanel 自己的显隐、按钮和弹层变化不再产生无意义刷新，保留 140ms 合并以吸收第三方应用短时间内的重复生命周期通知。
- Panel 隐藏期间仍沿用既有活动策略暂停枚举，重新唤出时执行一次完整刷新；右缘热区、全屏抑制和安全恢复逻辑不受影响。
- 新增 14 项前台、创建、销毁、显示、隐藏、标题、非窗口对象和无关事件测试及接线契约；自动测试不创建、显示或关闭真实窗口。
- README 新增运行应用窗口生命周期跟踪图。

## v0.9.59 - 2026-07-29

- 修复统一应用栏的切换、最小化和关闭操作完全忽略 Win32 返回值的问题；窗口已经失效、Windows 拒绝前台切换或关闭消息未能入队时不再静默显示为成功。
- 新增可替换的窗口命令边界和纯逻辑执行器：切换后确认前台窗口，最小化后读取实际 `IsIconic` 状态，关闭只发送正常 `WM_CLOSE` 并确认消息成功入队，仍不强杀任何进程。
- 按微软公开语义处理 `ShowWindow`：其返回值只表示操作前是否可见，不再被错误用作操作是否成功的依据。
- 固定、取消固定和拖动排序改为显式布尔结果；SQLite 锁定、身份解析异常或写入失败统一隔离在应用目录服务内，不再传播到 WPF UI 线程。
- 窗口或固定操作失败时自动打开状态中心，显示应用/窗口名称和可操作原因；排序失败会保留已完成的固定状态并提示稍后重新排序。
- 新增 9 项失效句柄、最小化确认、前台限制、恢复激活、关闭消息和界面接线测试；自动测试全部使用替代边界，不切换、最小化或关闭真实窗口。
- README 新增任务栏操作结果与失败反馈流程图。

## v0.9.58 - 2026-07-29

- 修复统一应用栏把 `shell:AppsFolder` 返回的原始 AUMID 直接当作文件名启动的问题；打包应用现在通过 Windows Shell 的 AppsFolder 命名空间打开。
- 启动请求会明确区分可执行文件、快捷方式、已有 Shell 路径和 AUMID；Shell 枚举中混合出现的普通绝对路径继续直接启动，不会被错误改写。
- 应用已卸载、固定目标移动、空目标或 Shell 启动异常统一转换为失败结果，不再把异常传播到 WPF UI 线程导致 Panel 闪退。
- 搜索、未运行固定项和右键“启动新实例”共用同一条安全启动链；失败时自动打开状态中心，显示应用名称、可能原因和重新搜索固定的处理方式。
- 新增 9 项无副作用启动策略与异常隔离测试，并扩充接口和界面契约；自动测试只检查 `ProcessStartInfo`，不启动、激活或关闭真实应用。
- README 新增应用启动安全流程图，说明 EXE/LNK、AUMID 和失效目标的不同处理路径。

## v0.9.57 - 2026-07-29

- 修复输入语言和输入法描述分别读取、但两个字段从未绑定到任何界面的问题；状态中心不再只显示无法判断当前状态的固定“输入法”按钮。
- 输入信息收口为单次 `InputMethodStatusSnapshot`：只读取一次前台键盘布局，再从同一布局生成语言、输入法简称、按钮文本和辅助功能摘要，避免切换瞬间语言与输入法错配。
- 输入法入口会显示“输入法 · 中 / 拼”“输入法 · 中 / 五”“输入法 · 中 / 注”“输入法 · EN”等；未知中文输入法稳定显示“中”，布局不可用时退回普通“输入法”。
- 按钮工具提示和读屏名称同步显示“当前输入法 中 / 拼”等完整状态；点击仍调用公开的 Win+Space 输入法切换，不修改系统输入法设置。
- 新增 17 项中文拼音/五笔/注音、英日韩及其他语言、空布局、按钮文本和摘要测试，并扩充系统状态接口与 XAML 契约；自动测试不读取或切换真实输入法。

## v0.9.56 - 2026-07-29

- 修复网络可用性、接口名称和连接详情分别查询，网络切换瞬间可能同时显示“已连接”和“当前没有可用连接”的问题；三项数据统一来自单次 `NetworkStatusSnapshot`。
- 保留公开且稳定的网卡枚举方式：过滤回环与隧道接口、优先显示活动 Wi‑Fi，其次显示其他活动接口；不读取 Explorer 私有结构，也不复制第三方托盘信息。
- 状态中心网络图标不再固定为 Wi‑Fi：无线、有线、其他连接分别显示 Segoe Fluent `WiFi`、`Ethernet`、`Globe`，离线或枚举失败显示 `Error`；图标同步提供“网络 WLAN / 网络未连接”等辅助功能名称。
- 系统报告网络可用但没有可展示接口时统一显示“网络已连接”，避免空名称；接口类型、IPv4 地址缺失时按已有部分组成详情，不显示多余分隔符。
- 新增 12 项离线陈旧数据、空接口名、详情降级、无线/有线/通用/离线字形测试，并扩充系统状态接口和 XAML 契约；自动测试不枚举或修改真实网卡。

## v0.9.55 - 2026-07-29

- 修复 `IsCharging` 虽然持续读取但从未绑定到界面、电池图标始终固定为 Battery0 的问题；状态中心现在按实际百分比显示 0–10 级 Segoe Fluent Battery 图标，充电时切换到对应 BatteryCharging 图标。
- 电池行显示“57% · 充电中”等明确状态，进度条同步提供辅助功能名称；无电池设备继续隐藏整行，不增加无意义占位。
- 紧凑栏状态入口的单一提示整合网络、音频和电池，例如“网络 WLAN · 音量 45% · 电池 82% · 充电中”；保留六入口布局，不新增网络或电池按钮。
- 将三个独立电池属性读取收口为单次 `BatteryStatusSnapshot`，避免插拔电源瞬间把不同采样时刻的“有电池、百分比、充电状态”错误组合；无效比例、异常和台式机无电池统一降级。
- 新增 26 项电池电量分级、充电字形、边界取整、无效比例与无电池测试，另增加 8 项断网、空接口名、台式机和笔记本摘要组合测试；自动测试不读取真实电池或网络。

## v0.9.54 - 2026-07-28

- 修复状态中心已经读取 `IsMuted`、但静音按钮和紧凑栏状态入口始终显示同一个静态图标的问题；音量状态现在真正进入任务栏视觉反馈。
- 新增可测试的 `AudioStatusPresentationComposer`：正常音量使用 Segoe Fluent `Volume`，静音或 0% 使用 `Mute`，没有默认输出设备使用 `Error`，并生成一致的中文摘要与操作名称。
- 状态中心静音按钮的图标、工具提示和辅助功能名称会在“静音 / 取消静音 / 音频设备不可用”之间同步切换；紧凑栏状态入口同时显示当前音频图标和“状态中心，音量 45%”一类读屏名称。
- Panel 隐藏期间仍停止系统状态轮询；每次从隐藏变为可见时只刷新一次系统快照，使右缘刚唤出的紧凑栏不会沿用数小时前的静音状态，也不增加后台常驻轮询。
- 新增 7 项音量边界、舍入、静音、不可用状态测试和 4 项壳层可见性转换测试，并扩充 XAML 契约；测试不读取或修改真实音频设备。

## v0.9.53 - 2026-07-28

- 修复紧凑栏普通按钮、应用图标或模块入口获得 WPF 键盘焦点后，即使鼠标已经离开仍会无限阻止自动收起的问题。
- 自动收起不再使用整棵壳层的 `IsKeyboardFocusWithin`；新增明确的焦点分类，只让文本框、密码框和下拉选择等真实输入控件保持 Panel 展开。
- 搜索输入、设置编辑和下拉选择期间仍保持防误收起；拖拽文件、右键菜单、弹层与鼠标捕获沿用现有交互锁，语义没有退化。
- 普通按钮焦点现在归类为命令焦点，鼠标离开后按既定延迟自动隐藏；窗口失去前台时仍可忽略残留输入焦点快速收回。
- 新增焦点保留策略的 4 项分类测试，并扩充自动收起和界面契约测试；测试不唤出 Panel、不操作真实任务栏。

## v0.9.52 - 2026-07-28

- 音频状态由两个会把失败伪装成 `0% / 未静音` 的属性读取改为单次 `AudioStatusSnapshot`；明确区分可用设备、真实音量与无默认输出设备，设备暂时消失时保留最后确认值而非显示假状态。
- Core Audio 默认端点获取、端点激活、音量读取、静音读取和两项写入均检查 COM HRESULT；失败、异常和设备切换统一返回不可用或写入失败，不再静默当作成功。
- 音量与静音写入通过 `AudioControlPolicy` 记录最后确认状态；系统拒绝写入时滑块或静音图标立即回滚，状态中心显示默认设备与 Win+A 提示，不再留下与系统不一致的 UI。
- 无音频输出设备时状态中心显示说明并停用音量滑块和静音按钮；后台或重新打开状态中心读到设备恢复后自动重新启用，不需要重启 FocusPanel。
- 紧凑栏状态按钮的滚轮调音改为一个事务式入口：仅当音量写入成功且目标音量大于零时才尝试取消静音，音量失败不再意外改变静音状态。
- 新增 9 项音量成功、拒绝、异常、HRESULT 规则和不可用快照测试，以及音频接口、禁用状态和滚轮入口契约；测试不读取或修改真实音量。

## v0.9.51 - 2026-07-28

- 状态中心的电源设置、显示桌面、锁定、睡眠、重启和关机由无返回值调用改为显式成功/失败结果；Windows API 返回失败或启动进程抛出异常时不再静默，也不会把异常传播到 WPF UI 线程。
- 新增无副作用的 `SystemActionExecution` 保护器，统一处理原生布尔结果、启动异常和“Win+D 失败后调用 Shell 桌面回退”的顺序；自动测试只使用委托替身，不锁屏、不睡眠、不重启或关闭测试机。
- 所有系统入口统一通过 `CompleteSystemAction` 收口：成功后关闭 FocusPanel 搜索、中心与弹层，避免遮挡 Windows 快捷设置、通知等原生界面；失败后自动打开状态中心并显示对应快捷键、权限或电源策略说明。
- 将原有但未显示的 `SystemActionMessage` 接入状态中心，并设置为辅助功能实时警告；按钮“网络”更名为准确的“快捷设置”，明确该入口调用 Win+A 而非复制 Explorer 私有网络弹层。
- 新增 5 项原生结果、异常隔离与回退顺序测试，并扩充状态中心接口和 XAML 契约；Release 构建与真实 WPF 冒烟保持通过。

## v0.9.50 - 2026-07-28

- 继续以公开 GitHub Releases 作为所有设备唯一的自动更新主源；客户端沿用 Velopack `GithubSource`，无需配置局域网地址、共享目录或访问令牌。
- 修复发布端可能已上传高版本 Release、但 GitHub `/releases/latest` 仍停留在旧版本的问题；正式发布后通过 GitHub 官方 API 显式设置 `make_latest=true`。
- 发布脚本设置 Latest 后立即回读验证标签，并检查 `releases.win.json`、`RELEASES` 和当前版本完整更新包；标记或资产不完整时让发布流程失败，不再产生表面成功但客户端无法发现的版本。
- 草稿发布保持原行为，不会提前改动 Latest；只有正式发布才更新所有安装设备读取的稳定更新入口。
- 新增发布契约测试，应用与更新服务继续复用现有 Velopack 安装、下载、重启和差分包链路。

## v0.9.49 - 2026-07-28

- 桌面收纳的网格和列表模式改用项目自有 `ViewportVirtualizingPanel`；不改变双列收纳盒和统一滚动条，只为外层 `ScrollViewer` 当前可见行及前后各一行缓冲生成文件卡片。
- 虚拟化面板根据现有 `CardWidth`、`CardHeight`、图标缩放和实际列宽计算完整占位高度；网格自动计算每行数量，列表保持单列固定行高，滚动条范围与未虚拟化时一致。
- 文件容器使用 WPF `IRecyclingItemContainerGenerator` 回收复用；滚动、集合插入删除、收纳盒折叠、上方内容高度变化和窗口尺寸变化均重新计算实现区间，不清空业务集合。
- 选择、右键菜单、双击打开、文件拖拽、分区放置和边缘自动滚动仍绑定原 `DesktopFile`；虚拟化只减少 WPF 视觉容器，不改变文件对象、数据库或收纳语义。
- 修复视口底边刚好落在行边界时额外生成下一整行的问题，并修复回收容器滚到新区间后未重新挂回视觉树的问题；两个缺陷均由真实 WPF 冒烟先发现再修正。
- 新增 6 项列数、范围、边界、离屏和列表计算测试，以及网格/列表 XAML 契约；真实 WPF 使用 1000 项验证首屏、中段、集合变化和返回顶部仅生成 `9/12/12/9` 个容器。
- `IMPLEMENTATION_PLAN.md` 的文件扫描增量更新、分区差量构建和大量文件 UI 虚拟化现已全部完成；应用与测试项目保持 0 错误、0 警告。

## v0.9.48 - 2026-07-28

- 桌面根目录的 `Created`、`Changed`、`Deleted` 和 `Renamed` 事件改为按完整路径合并，500ms 内同一路径的重复通知只处理一次；普通文件变化不再扫描用户桌面、公共桌面和旧仓库的全部项目。
- 路径增量刷新只读取受影响文件或文件夹的属性、大小、创建时间、类型和图标；删除项目同步移出可见列表，已收纳项目外部丢失时保留原对象并转为“需要恢复”，不静默删除恢复记录。
- 外部重命名继续按稳定文件身份更新数据库，并提前复用内存中的 `DesktopFile`，因此名称、路径和类型刷新后仍保留选择状态；用户桌面与公共桌面的同名候选会按现存路径重新解析。
- 已收纳项目若被外部清除 `Hidden + System`，属性监视事件会重新应用收纳属性并通知 Explorer；普通未登记的系统隐藏项目不会被误加入 Panel。
- 文件集合按文件夹优先和名称差量排序，只发送插入、移动和删除事件；大小、日期、类型和快捷方式扩展名变化会同步通知派生文本，不需要替换整个卡片。
- 旧 `.FocusPanel` 仓库变化、`FileSystemWatcher` 缓冲区异常和显式修复仍走全量扫描，保证增量路径无法覆盖的场景优先正确；监视器、计时器和订阅在 ViewModel 释放时一并停止，避免重复刷新。
- 新增 13 项路径批处理、重命名、删除、收纳、排序和派生属性通知测试；应用与测试项目保持 0 错误、0 警告，自动测试不访问真实桌面文件。

## v0.9.47 - 2026-07-28

- 桌面文件刷新不再重新创建所有 `PartitionViewModel` 并清空 `AllPartitions`、`PartitionsCol1` 和 `PartitionsCol2`；改为按“自定义状态＋收纳盒名称”复用现有对象。
- 收纳盒文件按完整路径差量执行插入、移动、替换和删除；未变化刷新不产生集合事件，文件跨收纳盒继续复用同一 `DesktopFile`，保留多选状态。
- 左右列和收纳盒排序只对真实变化发送 `Move/Insert/Remove`，不再用 `Reset` 销毁整棵 WPF 视觉树，因此 `ScrollViewer` 不会在文件监测刷新后自动跳回顶部。
- 同名收纳盒保持原折叠/展开状态和选中引用；同路径文件确需替换实例时把原选择状态移交给新实例。
- 分区匹配和文件偏好查询改用不区分大小写字典，消除每个文件线性遍历全部偏好的重复开销；数据库瞬时读取失败时保留最后一次有效界面。
- 新增 5 项无变化、跨盒移动、排序、换列和实例替换测试，增加禁止 `Clear()` 的界面契约，并在真实 WPF 冒烟中验证非零滚动偏移经过刷新后保持不变。

## v0.9.46 - 2026-07-28

- 将用户开始菜单、公共开始菜单、`shell:AppsFolder` 枚举和快捷方式应用身份解析从 `AppCatalogService` 构造函数迁入可取消的 STA 后台索引；Panel UI 线程不再等待完整应用目录。
- 搜索与固定应用不再同步调用 Shell 图标接口：名称和 0.9.45 的首字符占位会立即显示，真实图标通过单一 STA 队列按 12 项批次加载并在 UI 线程统一提交。
- 图标队列按路径合并重复请求，成功结果在服务内复用，失败路径在本次进程内不反复重试；固定项在目录尚未完成时也能先显示并在图标到达后增量替换。
- 搜索面板新增“正在载入应用目录”和“没有找到匹配的应用”状态；已有结果在后台刷新时保持可操作，不使用加载文本覆盖列表。
- 重建索引的取消令牌所有权：新一轮刷新会丢弃旧结果，旧线程稍后返回也不能覆盖较新目录；正常退出会停止排队并抑制迟到事件。
- 新增 5 项慢目录、慢图标、固定快捷方式、退出事件和新旧索引竞争测试，并增加搜索加载/空状态 XAML 契约；应用与测试项目保持 0 错误、0 警告。

## v0.9.45 - 2026-07-28

- 搜索结果和统一任务栏改用同一个 `AppIconPresenter`，不再各自直接绑定可空图标；真实 Shell 图标加载成功时保持原样显示。
- 当快捷方式、打包应用或受保护进程无法提供图标时，显示单层 Fluent 圆角占位和应用名称首字符，按钮不再出现不可识别的空白区域。
- 降级字符按 Unicode 文本元素提取，支持中文、英文、数字与组合字符；跳过名称前导空格和标点，完全无可识别名称时稳定显示通用 `A`。
- 占位表面复用现有主题颜色、控件圆角和高质量位图缩放，不新增外框、阴影或矩形/圆角嵌套。
- 新增 10 项图标降级单元测试、统一图标组件 XAML 契约和真实 WPF 加载冒烟；应用与测试项目保持 0 错误、0 警告。

## v0.9.44 - 2026-07-28

- 将时间入口从“大号时钟＋快捷按钮”的占位式摘要重建为完整 6 周月历；固定以周一为每周起点，始终生成 42 个日期单元并弱化显示相邻月份。
- 支持上月、下月、回到今天和直接选择日期；点击相邻月份日期时同步切换展示月份，避免选中状态留在不可见页面。
- 复用现有 `PomodoroSessions` 数据，仅查询当前月历覆盖的 42 天：完成过专注的日期显示轻量圆点，选中日期展示完成次数与累计分钟，不新增数据库表或迁移。
- 月历继续展示当前未完成任务数量，并提供“查看任务”和“开始专注”两个直接动作；不再在时间弹层重复堆放通知中心与显示桌面入口。
- 月历拆分为可独立加载的 `CalendarPanelView`，复用全局 Fluent 令牌；今天使用单线描边、选中日期使用单一强调表面、相邻月份降低透明度，不增加矩形/圆角双框嵌套。
- 新增 6 项月历周起点、固定 42 天、闰年、相邻月份、专注记录和 XAML 可达性测试；UI 冒烟新增真实 430×560 月历 PNG 快照，应用构建保持 0 错误、0 警告。

## v0.9.43 - 2026-07-28

- 将普通界面设置从程序安装目录迁移到 `%APPDATA%\FocusPanel\settings.json`，解决 Velopack 版本目录只读或切换后图标大小、视图模式、自定义分区和图片目录可能无法持续保存的问题。
- 首次启动优先读取新位置；尚未迁移时才读取旧版安装目录 `settings.json`，成功复制后仍保留旧文件作为回退，不删除、不覆盖用户原配置。
- 旧版默认的安装目录 `Images` 自动迁移为 `%APPDATA%\FocusPanel\Images`；用户自行选择的图片目录保持原值。
- 设置写入改为同目录随机临时文件加原子替换，失败时清理临时文件并保留可诊断的中文错误；损坏 JSON 会回退到安全默认值，不再静默吞掉异常。
- 永久删除不再创建的全屏 `DesktopOverlayViewModel`、拖放请求类型以及不可达的 Smart Rescue 旧流程；侧边栏收纳、文件属性事务和数据库记录不受影响。
- 收紧模型、图标解析、文件监测和转换器的空值边界；只读转换器反向绑定统一返回 `Binding.DoNothing`，桌面时间分组与遗留提示统一中文。
- 新增 13 项设置迁移、原子保存、失败恢复、只读转换器、中文日期分组和失效入口约束测试；应用项目构建达到 0 错误、0 警告，自动测试不操作真实任务栏、桌面文件或已安装实例。

## v0.9.42 - 2026-07-28

- 将此前不可达的英文 Dashboard 占位页重建为中文 Fluent“今日概览”，并在 Focus 中心增加明确入口、主窗口 DataTemplate 和完整导航生命周期。
- 首页只读聚合未完成任务、今日已完成番茄钟、进度低于 100% 的进行中 OKR 和已收纳桌面项目，不写入或迁移任何业务数据。
- “下一步”优先展示进行中及最近创建的任务，“目标进展”展示最近更新的 OKR 与进度；空数据、加载中和读取失败均提供明确中文状态。
- 快速动作可以直接进入任务、番茄钟、桌面收纳、OKR 和 AI，避免概览页继续堆叠不可操作的信息。
- Dashboard 每次打开或手动刷新时读取最新本地快照；重复刷新会取消上一请求，应用退出时取消查询并解除导航订阅。
- UI 冒烟新增可选的真实 WPF Dashboard PNG 快照输出，已按 640×820 实际工作区尺寸检查卡片、双栏、文本截断和快速动作布局。
- 新增 4 项 Dashboard 数据应用、空状态、导航和界面可达性测试；全部自动测试不隐藏真实任务栏，也不修改用户数据库。

## v0.9.41 - 2026-07-28

- 将仅显示英文标题的 AI 占位页重建为完整中文 Fluent 对话工作区，提供对话气泡、快速提示、模型选择、清空、停止和多行输入。
- 接入 OpenAI Responses API，按响应结构聚合全部 `output_text` 内容；请求支持取消和 90 秒超时，认证、限流、空响应及服务错误均显示中文状态。
- OpenAI API Key 使用 Windows DPAPI 按当前用户加密后写入既有 `AppConfig`，界面不会回显已保存凭据，保存或删除后立即清空密码框，也不会将 Key 写入日志。
- 本地上下文默认关闭；用户主动启用后，只发送最多 20 项未完成任务标题与状态、10 项 OKR 名称与进度，以及近 7 天完成的番茄钟统计，不读取文件内容、文件路径或任何凭据。
- AI 助手第一版保持只读，只提供建议，不直接删除、关机、外发、付费或修改任务与 OKR；最近对话限制为 8 条，避免输入无限增长。
- 主窗口退出时同步取消 AI 请求并释放网络资源；新增 12 项无真实网络、无真实数据库写入的服务、凭据、摘要、ViewModel 与界面契约测试。

## v0.9.40 - 2026-07-28

- 桌面收纳工作区完成最后一轮纯 Fluent 迁移，视图、图标大小、分组、新建、修复、重命名和收纳盒操作统一使用共享圆角按钮、输入框与分段选择控件。
- 删除页面残留的 `PackIcon`、`PopupBox`、HintAssist 以及全部 `MaterialDesign*` 兼容资源，不再维护伪 Material 控件层。
- 收纳盒标题区改为单一外层卡片和内部轻量分隔线，去除矩形边框与圆角外框互相嵌套；操作弹层统一为一个圆角表面。
- 收纳盒操作、视图选项、新建和修复弹层全部接入既有交互锁，菜单打开时 Panel 不会误收起，关闭并离开后恢复自动收起。
- 文件拖拽、自动整理、隐藏与恢复事务、分区排序、旧仓库兼容和数据库内容保持不变，不移动或重命名任何用户文件。
- 新增桌面收纳纯 Fluent 界面契约测试，UI 冒烟继续直接实例化主要页面，防止旧资源名或兼容控件回流。

## v0.9.39 - 2026-07-28

- OKR 工作区完成中文化和 Fluent 统一设计，移除页面中的 Material 兼容控件、英文按钮及旧资源名，目标、关键结果、同步状态和设置使用同一套圆角与信息层级。
- 改为本地优先工作流：未配置飞书凭据时仍可创建、查看、编辑和删除完整 OKR；配置成功后继续复用原有飞书双向同步与本地数据库。
- 补齐此前不可达的目标管理和关键结果操作，支持编辑名称、周期、说明、起始值、当前值、目标值与单位，并提供明确的保存和二次确认删除。
- 修复同步间隔下拉框只改变界面、不写入配置的问题；修改后立即持久化，自动同步计时器按新间隔更新。
- 重建进度计算：支持目标值低于起始值的反向指标，所有进度限制在 `0–100%`，目标按正权重汇总，无关键结果时准确归零。
- 目标与关键结果改为可观察实体，名称、当前值、总进度和同步状态变化会立即刷新所有卡片；关键结果集合新增或删除后无需重新进入页面。
- 新增、更新和删除关键结果时，关键结果与目标总进度在同一次数据库提交中完成；写入失败时不再提前从界面移除数据，并显示中文错误。
- OKR ViewModel 退出时解除同步事件并释放自动同步计时器；后台回调在窗口关闭或 Dispatcher 停止后不会继续触碰界面。
- 新增 OKR 进度计算和界面契约测试，并在 README 增加本地优先同步结构图；不迁移数据库，不删除已有目标、关键结果或飞书配置。

## v0.9.38 - 2026-07-28

- 任务工作区完成中文化和 Fluent 统一设计，列表、看板与字段设置共用一致的圆角、间距、焦点反馈与信息层级，不再依赖旧 Material 兼容外观。
- 看板从项目卡片陈列改为按任务状态组成的真实列；默认显示“待处理 / 进行中 / 已完成”，自定义列会过滤空值和重复项，旧任务的未知状态安全落入首列而不会消失。
- 看板卡片提供前移、后移、详情、进入子任务和删除操作；状态变化后即时重组看板，固定列顺序不会因任务更新跳动。
- 任务详情改用 Windows 11 原生 DWM 毛玻璃与系统圆角，移除透明 WPF 窗口、额外阴影和重复外框；透明不可用时沿用共享的不透明降级主题。
- 修复任务详情窗口重复创建和卸载后残留订阅：页面切换、数据上下文变化和 ViewModel 释放时均关闭详情并解除事件。
- 收紧任务、父项目、封面和设置模型的空值边界；异步加载或保存失败时在任务页显示中文状态，不再因数据库返回空项而直接崩溃。
- 新增看板组合器及任务界面契约测试，并在 README 增加任务工作区结构图；不迁移数据库，不改变已有任务、自定义字段或状态数据。

## v0.9.37 - 2026-07-28

- 重建番茄钟倒计时核心，剩余时间、已专注时间和进度条现在随每次 Tick 准确更新；暂停不会消耗时间，完成事件只在归零瞬间触发一次。
- 番茄钟页面完成中文化和 Fluent 统一设计，提供 25/45/60 分钟预设、开始/继续、暂停与重置，并以单一主表面展示本轮状态和累计统计。
- 专注完成后播放 Windows 系统提示音并通过 FocusPanel 托盘显示通知；不抢占当前前台窗口，数据库仍只记录已完成会话。
- 悬浮计时器改用与主壳共用的 Windows 11 `DWMSBT_TRANSIENTWINDOW` 原生毛玻璃和 DWM 圆角，不再使用黑色渐变、额外阴影或矩形/圆角嵌套。
- 删除番茄钟启动时创建的全屏置顶彩虹 `ScreenBorderWindow`；专注期间不再覆盖整块桌面或其他应用，关闭应用时同步停止计时器和悬浮窗口。
- 抽取共享 `WindowBackdropService`，主壳与悬浮计时器复用同一套 DWM 能力探测、透明降级和系统主题逻辑。
- 新增倒计时状态机及界面契约测试，覆盖进度、暂停、归零一次性完成、完成后新一轮、重置、非法时长、通知接线和全屏覆盖层移除。

## v0.9.36 - 2026-07-28

- 修复设置页数据库恢复被单实例互斥拦截的问题：新增 `--restore-after-exit <parentPid>` 交接模式，先等待当前 FocusPanel 完整退出，再启动真正的 `--restore` 实例。
- 恢复请求改走主窗口 `ForceClose` 生命周期，退出前仍会恢复原生任务栏、停止热区与计时器并释放数据库/窗口资源，不再从 ViewModel 直接调用 `Application.Shutdown()`。
- AppData 与安装目录的备份统一合并并按 `LastWriteTimeUtc` 排序，不再无条件优先 AppData 中可能更旧的文件。
- 每个候选备份和复制后的暂存文件都执行 SQLite `PRAGMA quick_check`；最新备份损坏时自动尝试更早的有效备份，全部失败时保留当前数据库不动并显示具体原因。
- 启动备份改用 SQLite 在线 `BackupDatabase`，能包含 WAL 中已经提交但尚未检查点的数据；输出统一切回 DELETE journal 并清理备份旁的 WAL/SHM，形成独立可恢复文件。
- 数据库替换先写入同目录随机暂存文件并校验，再覆盖目标；成功后删除旧数据库的 WAL/SHM，损坏数据库归档时同时保留其 sidecar 供排查。
- 新增 15 项恢复交接、备份选择、损坏回退、当前库保护、sidecar 清理、双目录备份与 WAL 数据测试；消除 `DatabaseBackupService` 原有的 2 条 Nullable 警告。

## v0.9.35 - 2026-07-28

- 重建“随 Windows 启动”服务：启用时使用 `CreateSubKey` 获取或创建 Run 键，不再假设注册表键必然存在并直接空引用。
- 可执行文件路径优先使用 `Environment.ProcessPath`，回退读取进程主模块；带空格路径统一加引号，路径不可用时拒绝写入而不是生成无效启动项。
- 注册表访问、写入和删除改为可测试边界；权限不足、策略限制或路径异常会返回明确中文原因，不再只写 Debug 日志后让界面假装成功。
- 设置页新增启动状态说明；写入失败时重新读取真实注册表状态并回滚复选框，任务栏替代本身保持可用。
- 新增 6 项自动启动单元测试和界面契约测试，覆盖路径引用、无路径拒绝、禁用删除、权限异常、空值识别、读取失败及 UI 回滚；同时消除该服务原有的 6 条 Nullable 编译警告。

## v0.9.34 - 2026-07-28

- 为 0.9.33 的键盘导航补齐可见反馈：所有 Focus 图标按钮、主次按钮、搜索框、兼容图标按钮、下拉框和单选控件共用一套 2px 圆角焦点环。
- 焦点环通过 WPF `FocusVisualStyle` 只在键盘导航时显示，鼠标点击不会留下持续边框，避免重新出现矩形框与圆角控件相互嵌套的违和感。
- 焦点轮廓复用 `FocusControlCornerRadius`，和按钮表面保持同一圆角；不新增第二层背景、阴影或黑色外框。
- 浅色/深色模式使用主题强调亮色；Windows 高对比度模式自动切换为系统 `HighlightColor`，确保焦点位置可辨认。
- UI 冒烟新增焦点画刷资源检查，界面契约覆盖单一焦点模板、五类控件复用、高对比度适配以及禁止模板内常驻 `IsKeyboardFocused` 边框。

## v0.9.33 - 2026-07-28

- `Ctrl+Alt+Space` 和托盘“显示 FocusPanel”从单纯激活窗口改为主动建立紧凑栏键盘焦点，唤出后可立即用 Tab、Shift+Tab、方向键、Enter 和 Space 操作。
- 紧凑栏建立循环 Tab 与方向键导航范围；应用列表的滚动容器退出焦点序列，避免键盘落到不可操作的空壳上。
- 统一应用按钮向 Windows UI Automation 暴露真实应用名称和窗口摘要；开始、搜索、任务视图、Focus 中心、状态中心、时间及溢出导航补齐可访问名称。
- 开始入口补充键盘帮助文本，明确 Enter 打开开始菜单、Shift+F10 或菜单键打开系统管理菜单；状态中心说明滚轮音量和右键静音语义。
- 新增界面契约测试，验证主动唤出焦点、循环导航、动态应用名称与帮助文本，且继续保持六个固定入口。

## v0.9.32 - 2026-07-28

- 后台 GitHub Releases 检查发现新版本后，紧凑栏 Focus 中心入口显示高对比度状态点，不再只依赖容易错过的系统托盘气泡。
- Focus 中心新增更新卡片，直接显示目标版本并一键进入“设置与更新”；确认没有更新时同步清除徽标和版本状态。
- 更新检查失败时保留上一次已确认的可用版本提示，避免临时网络异常让真实更新入口消失；手动检查发现更新后同样立即刷新徽标。
- 新增更新可用状态策略及界面契约测试，覆盖无更新、空版本、版本规范化、后台检查、手动检查和两个可见入口。
- 根据安装版只读健康检查选择本轮优先级：当前 0.9.24 进程正常响应，最近崩溃仍停留在旧版 0.9.18，但连续多个 Release 尚未安装，更新发现性是更直接的缺口。

## v0.9.31 - 2026-07-28

- 紧凑栏统一应用列表在内容溢出时新增轻量上下导航，屏幕较矮或固定/运行应用较多时不再只能依赖不可见的鼠标滚轮。
- 上下导航仅在对应方向仍有应用时悬浮出现；到达顶部或底部后相应箭头自动消失，不挤压应用列表高度，并保留原有滚轮滚动。
- 单次点击按一个任务栏图标高度移动，避免整页跳跃；导航控件不计入六个固定功能入口，不改变开始、搜索、任务视图、Focus 中心、状态中心和时间的布局语义。
- 新增纯逻辑溢出状态策略和界面接线契约测试，覆盖无溢出、顶部、中部、底部、边缘容差及异常数值。
- 更新 README 六入口示意图与发布说明，继续使用 GitHub Releases 作为唯一自动更新主源。

## v0.9.30 - 2026-07-28

- 根据安装版现场采样优化隐藏态资源占用：Panel 不可见时暂停全局窗口事件触发的完整窗口枚举、应用身份解析、图标组合与任务栏集合同步。
- `IWindowTracker` 新增显式活动生命周期；隐藏期间保留 WinEventHook 和当前快照但忽略刷新请求，唤出时执行一次完整刷新，随后恢复实时增量更新。
- 时钟刷新仅在 Panel 可见时运行；Core Audio、网络、输入法、电池状态仅在状态中心打开时刷新；SQLite 今日任务摘要仅在日历打开时刷新。
- 隐藏态继续保留 30ms 右缘物理坐标监测、全屏抑制、任务栏安全守护和 6 小时 GitHub 更新检查，不降低唤出准确性或故障恢复能力。
- 消除 WinEvent 回调在 UI 线程上仍二次 `BeginInvoke` 的冗余排队；跨线程事件仍安全回到所属 Dispatcher。
- 新增壳层刷新活动策略、窗口跟踪活动策略和生命周期接线契约测试，覆盖隐藏休眠、详情面板按需刷新与重新唤出强制更新。

## v0.9.29 - 2026-07-28

- 桌面文件卡片接入 Windows 系统拖拽阈值，只有横向或纵向移动超过阈值后才进入 OLE 拖拽，普通点击和细小手部抖动不再误启动拖拽。
- 自动滚动改为只绑定具名的桌面收纳主 `ScrollViewer`，不再通过视觉树搜索第一个滚动容器，避免驱动错误区域。
- 新增纯逻辑拖拽交互策略，按照鼠标到视口边缘的距离平滑计算滚动步长，并在已到顶部或底部时返回零。
- 根收纳视图统一监听拖拽移动、离开和放置；鼠标回到中部、离开边界、释放、取消、完成放置或视图卸载时都会停止计时器并清除拖拽候选。
- 新增拖拽阈值、上下边缘、视口外、滚动边界和剩余距离不越界测试，以及具名视口与退出路径的 XAML 契约测试。

## v0.9.28 - 2026-07-28

- 修复右键菜单和 `Popup` 位于主窗口边界之外时，Panel 把鼠标误判为离开并在操作过程中自动收起的问题。
- 应用菜单、多窗口列表、开始按钮系统管理菜单和桌面文件菜单通过明确的 `Opened/Closed` 生命周期锁定壳层。
- 桌面收纳的视图选项、新建收纳盒和修复工具弹层接入同一交互锁；视图卸载时会释放尚未结束的锁，避免残留状态。
- 自动收起状态机同时识别 WPF 鼠标捕获；拖拽、按压或 `StaysOpen=False` 弹层交互结束前不会隐藏 Panel。
- 新增自动收起策略与 XAML 契约测试，覆盖临时交互阻止收起、菜单生命周期和三个桌面收纳弹层。

## v0.9.27 - 2026-07-28

- 统一应用栏改为按 Windows 应用身份增量同步；前台窗口、标题或运行状态变化时，只移动、插入、删除或替换实际变化的图标，不再清空整条集合。
- 未变化的应用图标和 WPF 容器保持原实例，应用栏滚动位置不再因窗口快照刷新而重置，降低前台切换时的视觉闪烁。
- 将每秒时钟刷新与系统状态、任务摘要刷新解耦：时间仍每秒更新，音量/网络/电池改为 2 秒刷新，SQLite 待办统计改为 30 秒刷新。
- 打开状态中心或日历时仍立即刷新对应数据，在降低空闲查询和 UI 线程负担的同时保持入口信息及时。
- 新增应用栏增量同步回归测试，覆盖无变化零通知、活动状态局部替换和排序使用 Move 而非 Reset。

## v0.9.26 - 2026-07-28

- 更新主源统一为 GitHub Releases，移除局域网 HTTP、UNC 共享目录、本机目录的来源选择及相关发布脚本。
- 其他设备首次安装后不再需要配置更新路径；启动后自动检查一次，运行期间每 6 小时低频检查一次。
- 自动检查不阻塞启动、不自动重启；发现新版本时更新设置页状态并显示 FocusPanel 托盘提示。
- 手动“一键检查并安装更新”继续展示版本说明、下载差分包或完整包、备份数据库、恢复任务栏并安全重启。
- 清理局域网更新边界和界面契约，更新 README、测试及发布说明。

## v0.9.25 - 2026-07-28

- 自动更新新增可持久化来源配置：保留 GitHub Releases 默认源，同时支持局域网 HTTP/HTTPS、Windows UNC 共享目录和本机绝对目录。
- 其他设备只需在设置页保存一次局域网来源，之后即可继续使用“一键检查并安装更新”；检查前会校验并保存当前来源。
- 新增 `publish-lan-update.ps1` 局域网发布脚本，先复制完整包、差分包等载荷，最后更新 Velopack 清单，避免客户端读到未复制完成的新版本。
- 设置页的软件更新卡片显示实际生效来源，并支持低高度滚动。
- 新增更新来源规范化、无效地址拒绝、UNC 路径和设置界面契约测试；自动测试不下载或安装真实更新。
- README 新增局域网多设备更新章节与流程图，发布说明和默认打包版本提升至 0.9.25。

## v0.9.24 - 2026-07-28

- README 改为图文说明，新增总览、六入口任务栏、Focus 中心、状态中心、任务栏安全恢复、桌面收纳和一键更新共七张结构图。
- 将 76px 紧凑栏收敛为开始、搜索、任务视图、Focus 中心、状态中心和时间六个固定入口；固定/运行应用继续使用中部唯一列表。
- Focus 中心统一承载五个业务模块、最近使用模块和设置更新；状态中心集中网络、音量、电池、输入法、通知、显示桌面与电源操作，并支持低高度滚动。
- 删除第三方托盘溢出入口、UI Automation 探测和选择器，不读取 Explorer 私有数据，也不为此临时显示原生任务栏。
- 任务栏停止事件改为类型化原因；失效后只安全恢复一次，并由用户在状态中心或设置页点击“重新接管任务栏”主动启用。
- 更新任务栏、系统状态和紧凑栏契约测试；自动测试继续使用替代边界，不操作真实任务栏。

## v0.9.24 - 2026-07-28

- 将 76px 紧凑栏收敛为开始、搜索、任务视图、Focus 中心、状态中心和时间六个固定入口；固定/运行应用继续使用中部唯一列表。
- Focus 中心统一承载五个业务模块、最近使用模块和设置更新；状态中心集中网络、音量、电池、输入法、通知、显示桌面与电源操作，并支持低高度滚动。
- 删除第三方托盘溢出入口、UI Automation 探测和选择器，不读取 Explorer 私有数据，也不为此临时显示原生任务栏。
- 任务栏停止事件改为类型化原因；失效后只安全恢复一次，并由用户在状态中心或设置页点击“重新接管任务栏”主动启用。
- 更新任务栏、系统状态和紧凑栏契约测试；自动测试继续使用替代边界，不操作真实任务栏。

## v0.9.23 - 2026-07-28 04:34 (UTC+8)

- 开始按钮改为左键打开 Windows 开始菜单、右键打开 Win+X 风格系统管理菜单，补齐安装的应用、电源选项、事件查看器、系统、设备管理器、网络连接、磁盘管理、计算机管理、终端、管理员终端、任务管理器、设置、文件资源管理器、搜索、运行、关机与桌面入口。
- 紧凑栏重新按应用、FocusPanel 工作区、Windows 核心操作和系统状态分层，并恢复独立任务视图入口，避免系统管理能力与业务模块混杂。
- 任务栏替代模式先通过 `ABM_SETSTATE + ABS_AUTOHIDE` 释放工作区，再一次性隐藏主屏 `Shell_TrayWnd`；不结束 Explorer、不处理副屏任务栏。
- 守护器改为只读验证，不再定时重写任务栏可见性或工作区。Windows 主动恢复任务栏或 Explorer 宿主变化时，替代模式安全停止并恢复原设置，避免重新出现占用区域反复跳动和闪烁。
- 保留独立 watchdog、紧急恢复快捷键和幂等恢复；自动测试继续使用替代 Win32 边界，不操作开发机真实任务栏。

## v0.9.22 - 2026-07-28 04:18 (UTC+8)

- 将固定应用与运行应用合并为单一 `TaskbarApps` 集合，同一应用不再在侧边任务栏出现两个图标。
- 新增应用身份解析边界：窗口显式 `System.AppUserModel.ID` 优先，其次读取进程 AUMID，最后使用规范化可执行路径；快捷方式优先读取自身 AppID，再通过 `IShellLink` 解析目标路径。
- 固定项继续使用数据库顺序；未固定运行项按首次出现顺序稳定排列，不再因前台状态变化跳动。无法读取身份的受保护进程按 PID 隔离，不按显示名称误合并。
- 统一点击和右键语义：未运行时启动、单窗口激活/最小化、多窗口显示文字列表，并支持启动新实例、固定/取消固定、逐个关闭或关闭全部窗口。
- 拖动未固定运行项时，根据 AUMID 或可执行路径创建固定项并持久化排序；取消固定后，运行窗口关闭前图标继续保留。
- 保持现有 `PinnedApps` 表不变，不迁移或删除用户数据。

## v0.9.21 - 2026-07-28 01:42 (UTC+8)

- 补齐侧边任务栏的 Windows 原生功能菜单，可直接唤起开始菜单、Windows 搜索、任务视图、小组件和运行对话框，不再用普通设置页替代这些操作。
- 多窗口应用不再默认只操作第一个窗口：左键直接显示文字窗口列表，支持逐个切换、正常发送 `WM_CLOSE` 关闭以及关闭全部窗口。
- 紧凑栏音量图标新增滚轮调节和右键静音；调高音量时会自动解除静音。
- 将 Windows 快捷键映射提取为可测试边界，自动测试覆盖所有新增快捷入口；继续保留 Explorer 托盘宿主，不复制第三方托盘图标的私有结构。

## v0.9.20 - 2026-07-27 20:09 (UTC+8)

- 根据 0.9.19 实机现场确认：1920×1080 主屏的 Windows 工作区持续回到 1920×1032，旧守护又定期写回 1080，根因是直接隐藏 `Shell_TrayWnd`、强写 `SPI_SETWORKAREA` 与 Explorer 自身工作区管理长期冲突。
- 任务栏兼容模式改用 Windows 官方 `ABM_SETSTATE + ABS_AUTOHIDE`；不再隐藏原生任务栏窗口、不再写工作区，最大化区域由 Explorer 自己维护，从机制上移除 1032/1080 闪烁循环。
- 原生 `Shell_TrayWnd` 保持存活，快捷设置、通知中心、输入法和托盘溢出继续拥有系统宿主；鼠标移到原任务栏边缘仍可临时唤出，多显示器遵循 Windows 的统一自动隐藏设置。
- 新恢复会话标记为原生自动隐藏模式，退出只恢复原 AppBar 状态；旧版会话仍兼容原工作区恢复。恢复结果验证失败时不再删除会话，watchdog 保留重试机会。
- 修复桌面收纳页拖拽靠近顶部后持续自动上滚：根控件补绑 `DragLeave`/`Unloaded`，所有 Drop 和拖拽结束路径统一停止自动滚动 timer。
- 更新任务栏状态机、失败恢复和界面契约测试；自动测试继续使用替代 Win32 边界，不改变测试机任务栏。

## v0.9.19 - 2026-07-27 18:43 (UTC+8)

- 根据本机 Windows 事件日志定位并修复 0.9.18 闪退：任务栏守护定时器等待跨进程状态锁超时后，未捕获的 `TimeoutException` 会从线程池终止整个进程。
- 任务栏守护检测增加单次执行门闩，前一次检测未完成时直接跳过后续 tick，不再并发争抢工作区和任务栏状态锁。
- 锁超时现在只跳过一次检测，不再触发进程崩溃或任务栏恢复/重新隐藏循环；其他守护故障会安全恢复 Windows 任务栏，并把替代模式关闭状态写回设置。
- 恢复守护进程增加有限重试；应用退出、崩溃恢复和紧急快捷键遇到短暂锁竞争时保留会话文件并重试，不因第二个异常丢失恢复机会。
- 新增锁超时隔离和并发守护回归测试；自动测试通过替代 Win32 边界执行，不隐藏测试机任务栏。

## v0.9.18 - 2026-07-27 18:02 (UTC+8)

- 主壳改为经过返回值验证的 Windows 11 DWM Desktop Acrylic：只有 `DWMSBT_TRANSIENTWINDOW` 与客户区扩展都成功时才启用轻薄 tint，失败、高对比度、关闭透明效果或远程桌面时自动回退为实色。
- 移除根页面重复半透明底板和桌面收纳页重阴影，统一使用系统强调色、10px 卡片和 8px 控件圆角；工具栏改为 Segoe Fluent Icons，并移除会全局隐藏桌面图标的入口。
- 自动整理改为逐项容错的无移动批处理：单个项目失败不再中断整批，公共桌面项目单独请求管理员授权，完成后强制刷新并报告成功与剩余数量。
- 视图选项新增“新增桌面项目自动按类型收纳”开关；自动流程与手动按钮共用同一分类策略、属性收纳事务和并发锁。
- 新增类型映射、逐项失败、授权继续、单层表面与原生 Acrylic 契约测试；自动测试不会操作真实桌面项目。

## v0.9.17 - 2026-07-27 16:46 (UTC+8)

- 紧凑侧栏新增 Windows 11 原生托盘溢出入口；通过可访问性调用 Explorer 的“显示隐藏的图标”按钮，第三方图标继续由原应用和 Explorer 托管，保留左键、右键菜单及状态更新。
- 常驻系统区拆分为输入语言/输入法、网络、音量、时钟日期、通知、显示桌面和设置，不再用一个含糊按钮代替整套任务栏能力。
- 固定应用、运行应用和 FocusPanel 工作区合并到中部可滚动区，底部系统按钮不会再因高 DPI 或窗口高度不足而顶掉设置入口。
- 新增托盘可访问节点选择测试；自动测试仍不隐藏开发机任务栏，也不使用跨进程 Explorer 内存读取。

## v0.9.16 - 2026-07-27 16:30 (UTC+8)

- 修复 0.9.15 守卫定时器每秒无条件调用 `SPI_SETWORKAREA` 和隐藏任务栏，造成 Explorer 工作区在 1032px 与 1080px 之间反复切换、界面持续闪烁的问题；现在只有工作区或任务栏实际偏离目标状态时才写入。
- 任务栏隐藏与恢复增加跨进程命名互斥，避免主进程守卫和独立恢复守护进程同时改写 `Shell_TrayWnd`；恢复顺序改为先恢复工作区与 AppBar 状态、最后显示任务栏，并验证显示结果。
- 新增稳定替代状态回归测试，连续执行守卫不得增加工作区写入或任务栏可见性写入次数；本机现场已使用安全停用标记恢复原工作区和原生任务栏，并临时关闭替代配置。
- 紧凑栏合并网络、音量、电池和输入法入口，减少低分辨率及高 DPI 下的固定按钮占用，为固定应用与运行窗口列表留出高度。
- 设置按钮恢复为独立直达入口，不再被“通知、设置与电源”上下文菜单替代；首次启用页只覆盖工作区，不再遮挡右侧紧凑任务栏。

## v0.9.15 - 2026-07-27 16:10 (UTC+8)

- 修复数据库中安全说明已确认、但 `Shell.ReplacementEnabled=False` 时界面不再解释为何 Windows 任务栏仍显示的问题；替代模式未启用时持续提供明确的安全启用页，仍然必须由用户点击后才隐藏。
- 任务栏接管增加最终状态验证：释放主屏工作区后同时使用 `ShowWindow` 与 `SetWindowPos(SWP_HIDEWINDOW)` 隐藏 `Shell_TrayWnd`，并再次检查窗口可见性；任一步失败都会恢复原工作区和任务栏，并展示具体失败点。
- 重建侧栏系统控制中心：Core Audio 默认设备在每次操作时重新解析，支持面板内音量滑块与静音；网络显示真实活动网卡、连接类型和 IPv4；电池、锁定、睡眠、通知、输入法和显示桌面均可直接操作。
- 网络、通知和输入法快捷键注入失败时不再跳转 `ms-settings:`；界面显示明确错误并提示对应系统快捷键，避免把“打开设置”伪装成任务栏功能。
- 时间入口改为面板内时钟与今日任务摘要，任务、通知中心和显示桌面可直接进入；新增任务栏隐藏失败自动回滚测试和系统控制 XAML 契约测试。

## v0.9.14 - 2026-07-27 16:20 (UTC+8)

- 移除会阻断 Desktop Acrylic 的 GDI `SetWindowRgn` 裁剪链路；主壳现在只由 DWM 同时提供唯一圆角轮廓与 `DWMSBT_TRANSIENTWINDOW` 背景模糊，不再叠加矩形/圆角外壳。
- 文本框模板改为由边框承载内边距，并将垂直内容对齐传递给 `PART_ContentHost`；使用 `MinHeight` 代替固定高度，避免局部较大内边距把文字裁成半截。
- 自动收回改用物理光标与 HWND 边界判断；计时器被鼠标、拖拽或输入焦点阻挡时会继续等待，失去前台后忽略残留输入焦点并收回，消除偶发永久展开。
- 删除伪快捷设置和伪日历浮层；紧凑坞新增真实网络、音量、Windows 通知/日历、输入法、电池与显示桌面入口，状态来自 Core Audio、网络接口和系统电源状态。
- Windows 快捷键注入失败时回退到对应系统设置 URI；显示桌面增加 Shell 回退，避免底部入口点击无效。
- 任务栏替代启用改为事务式：工作区切换失败会立即恢复任务栏及原工作区，不再留下半启用状态；守护进程和紧急恢复快捷键保持不变。

## v0.9.13 - 2026-07-27 15:45 (UTC+8)

- 修复 Windows 将用户桌面与公共桌面合并显示、但拖拽收纳只识别用户桌面的路径判断错误；现在同时扫描并监听两套桌面根目录。
- 收纳入口改为沿用拖入项目的真实完整路径，不再通过文件名拼接用户桌面路径，避免公共桌面快捷方式被误判。
- 公共桌面项目在明确说明“影响本机所有账户”并取得用户确认后，才为该次属性修改请求 UAC；提权助手严格限制为公共桌面根目录的直接子项。
- 拖放结果分别报告非桌面项目、管理员授权取消和属性写入失败，不再用“不是桌面或失败”混合提示。

## v0.9.12 - 2026-07-27 15:30 (UTC+8)

- 修复主窗口同时叠加 DWM 圆角、原生窗口 Region 圆角和 XAML 外壳圆角，导致边缘出现多层轮廓的问题；现在只由原生窗口 Region 定义唯一外形。
- 移除工作区整页的独立圆角底板、透明应用坞的无意义圆角以及全屏引导页的第二层外框，内容区不再与主壳形成“矩形套圆角”的嵌套边界。
- 移除全局隐式 `Border` 圆角，卡片和控件只在明确使用对应设计令牌时绘制圆角，避免普通布局容器被意外圆角化。

## v0.9.11 - 2026-07-27 15:04 (UTC+8)

- 修复 Desktop Acrylic 只设置 backdrop、未把 DWM frame 扩展进 WPF 客户区的问题；透明模式现在调用 `DwmExtendFrameIntoClientArea(-1)`，让背景模糊覆盖整个壳层。
- 顶层 HWND 增加随窗口动画和 DPI 更新的 24px 原生圆角 region，并继续关闭 DWM border，避免透明矩形角和系统描边形成黑边。
- 玻璃表面只保留轻量 tint，关闭透明效果、高对比度或远程桌面时仍自动回退为不透明表面。
- 新增统一圆角令牌：壳层 24px、卡片 16px、控件 12px；同步整理主壳、桌面收纳、任务、OKR 和任务详情的混用圆角。
- 取消收纳成功后清空原分区归属；拖回桌面的项目会立即从面板集合消失，不再以普通分类项残留。

## v0.9.10 - 2026-07-27 14:55 (UTC+8)

- 修复从面板拖回桌面时 `GetClassName` 使用不可封送泛型参数而抛出运行时异常的问题，改用 Win32 支持的 `StringBuilder` 并增加真实 API 回归测试。
- 壳层半透明遮罩由接近不透明调整为可感知的 Desktop Acrylic，移除 DWM 系统黑色边框，外壳仅保留原生圆角与阴影。
- 精简紧凑应用坞：统一 44px 操作尺寸，降低分割线噪声，将低频 OKR、AI、通知、设置和电源入口归入两个分组菜单。
- 右缘热区唤出紧凑坞后立即启动 900ms 进入宽限；未进入面板则自动隐藏，进入后在离开 350ms 后收回，输入焦点和文件拖拽期间仍保持展开。
- 展开工作区移除重复副标题和常驻退出按钮，降低嵌套卡片与描边密度。

## v0.9.9 - 2026-07-27 14:36 (UTC+8)

- 补齐桌面收纳的反向拖拽：已收纳项目从面板拖到 Windows 原生桌面并释放后，会执行取消收纳。
- 拖出判断只识别 `Progman`、`WorkerW`、`SHELLDLL_DefView` 和桌面 `SysListView32`，避免释放到其他应用或资源管理器窗口时误恢复。
- 复用原有幂等恢复事务，精确还原收纳前属性并通知 Explorer 重新枚举，不复制、不移动实体文件。

## v0.9.8 - 2026-07-27 14:24 (UTC+8)

### 桌面图标重新枚举修复

- 修复只发送 `SHCNE_ATTRIBUTES` 后，Explorer 将已收纳项目渲染为半透明图标但不从桌面视图移除的问题。
- 属性变更后追加受支持的 `SHCNE_UPDATEITEM` 与 `SHCNE_UPDATEDIR`，并使用 `SHCNF_FLUSHNOWAIT` 要求 Shell 及时重新枚举桌面目录。
- 不重启 Explorer、不修改“显示隐藏项目”设置，也不恢复旧版跨进程 ListView 操作。

## v0.9.7 - 2026-07-27 14:16 (UTC+8)

### 桌面拖拽收纳入口修复

- 修复外部 `FileDrop` 仍调用旧 `ImportFiles` 分类逻辑，导致从桌面拖进分区后没有执行隐藏属性事务的问题。
- 桌面根目录文件和文件夹拖入分区后，直接复用 `HideFileFromDesktop`，立即应用 `Hidden + System` 并保留原路径。
- 非桌面根目录项目不再移动到桌面；界面明确报告跳过数量，避免“无移动收纳”语义被旧导入逻辑破坏。
- 拖放事件改为等待收纳完成并阻止继续冒泡，新增桌面根目录、子目录和外部目录判定测试。

## v0.9.6 - 2026-07-27 14:07 (UTC+8)

### 无移动桌面收纳修复

- 新收纳项目不改名、不移动，改为保存完整原始属性后追加 `Hidden + System`；取消收纳时逐位恢复原属性。
- 删除通过跨进程读写 Explorer ListView 并发送 `LVM_DELETEITEM` 隐藏单个图标的非公开实现，改用正式文件属性与 Shell 属性刷新通知。
- `DesktopFilePreference` 新增托管路径、原始属性、稳定文件标识、收纳模式和操作状态，支持改名匹配、中断恢复和缺失项目保留。
- 旧 `.FocusPanel` 仓库继续兼容且不自动迁移；分区调整只更新数据库，不再移动收纳文件。
- 设置页新增“显示受保护的系统文件”检测与限制提示，恢复异常项目在收纳页面标记为“需恢复”。
- 新增属性组合、精确恢复、文件改名标识稳定性和 Explorer 私有操作清理测试。

## v0.9.5 - 2026-07-27 13:47 (UTC+8)

### 普通最大化窗口右缘呼出修复

- 修复隐藏 Windows 原生任务栏后，普通最大化窗口因覆盖整个显示器而被误判为无边框全屏，导致右缘指示条和鼠标热区同时停用的问题。
- 全屏策略新增最大化状态与标准窗口边框判断；保留标题栏或可调整边框的普通最大化窗口不再抑制热区，真正无边框、F11 和独占全屏仍遵循原有设置。
- 新增普通最大化覆盖整屏和无边框覆盖整屏的回归测试，并在真实发布版中验证旧固定路径已切换到当前版本。

## v0.9.4 - 2026-07-27 13:02 (UTC+8)

### 全界面资源回归测试与运行验证

- 修复任务详情窗口引用未定义的 `BooleanToVisibilityConverter`、番茄钟引用未定义的 `SecondaryHueMidBrush`；这两项此前只会在进入对应界面时暴露。
- 新增 XAML 资源契约测试，扫描全部主要页面的 `StaticResource`、`DynamicResource` 以及代码后置 `FindResource`/`TryFindResource`，阻止缺失资源再次进入发布包。
- 新增 `--ui-smoke-test` 独立进程自检模式，真实创建、测量和布局桌面收纳、任务、番茄钟、OKR、AI、任务详情、悬浮番茄钟和右缘指示窗口。
- 冷启动实际程序后验证：隐藏状态仅有 3px 点击穿透指示条可见；物理鼠标在主屏右缘停留后，76px 紧凑坞能够显示。

## v0.9.3 - 2026-07-27 12:34 (UTC+8)

### 桌面收纳主题资源修复

- 修复打开桌面收纳页面或执行分区拖拽时，代码后置仍查找已移除的 `OrganizerCardBrush`、`OrganizerCardBorderBrush` 而触发未处理异常的问题。
- 分区拖拽反馈改用现有 `FocusSurfaceSoftBrush`，恢复状态使用 `FocusSurfaceStrongBrush` 与 `FocusStrokeBrush`，与当前 Fluent 主题字典保持一致。
- 审查全部代码后置 `FindResource` 调用，确认剩余资源键均由当前主题或页面资源提供。

## v0.9.2 - 2026-07-27 12:19 (UTC+8)

### 右缘运行提示与热区可靠性

- 修复 `Progman`、`WorkerW` 等 Windows 桌面壳窗口覆盖整屏时被误判为全屏应用，导致右缘热区在桌面场景始终被禁用的问题。
- 新增主屏右缘 `3px` 白色运行指示条；窗口使用 `WS_EX_TRANSPARENT`、`WS_EX_NOACTIVATE` 和工具窗口样式，完全点击穿透且不抢焦点。
- 指示条在侧边栏展开、托盘隐藏或真正的全屏应用前台时自动隐藏，恢复可用后重新显示。
- 新增单实例互斥保护；重复启动 FocusPanel 时不再创建第二套壳层、热区和任务栏守护链路，而是唤出已有实例。
- 新增 Windows 壳窗口、当前进程、真实全屏应用和保留任务栏的最大化窗口判定测试。

## v0.9.1 - 2026-07-27 12:03 (UTC+8)

### 桌面阻塞与右缘呼出修复

- 永久移除会在开机时覆盖桌面并拦截点击的全屏 `DesktopOverlayWindow`；启动、退出和异常恢复仍会恢复 Explorer 原生桌面图标。
- 桌面收纳继续保留在侧边栏中，文件分区、隐藏、恢复、拖拽及现有 SQLite 数据不变。
- 将 `3px` WPF 悬停窗口替换为无窗口物理坐标监测器，以 `30ms` 周期检测主屏右缘最后 `12px`，停留 `100ms` 后呼出紧凑坞。
- 增加 `32px` 复位区域与触发锁，避免重复抖动；显示设置变化后重新计算主屏边界，全屏抑制和全局快捷键行为保持不变。
- 新增边缘区域、停留时间、快速划过、重复锁定、负坐标屏幕和纵向边界测试。
- 发布版本提升至 `0.9.1`，更新安装包和设置页一键更新所需的发布说明。

## v0.9.0 - 2026-07-27 04:20 (UTC+8)

### 安装包与一键更新

- 接入 Velopack 1.2.0，生成 Windows x64 自包含安装器、完整更新包、更新清单和后续差分包。
- 设置页新增“一键检查并安装更新”，支持 GitHub Releases 版本检查、更新说明、下载进度和自动重启安装。
- 安装更新前自动备份业务数据库、恢复主屏 Windows 任务栏和原生桌面图标，避免更新进程直接退出后留下替代状态。
- 开发运行版不会覆盖自身，会明确提示先安装 `Setup.exe`；安装版与便携版支持原地更新。
- 新增可复现的本地打包、GitHub Release 草稿/发布脚本，以及标签触发和手动触发的 Windows 发布工作流。
- 新增更新服务边界和开发运行版测试；自动测试总数增加至 12 项。

## v0.8.0 - 2026-07-27 01:06 (UTC+8)

### Windows 11 侧边任务栏

- 将仅桌面显示的旧抽屉重构为跨应用可用的右侧玻璃壳层，提供 `76px` 紧凑坞、约 `720px` 工作区和独立无激活热区。
- 新增应用目录搜索、固定与拖动排序、运行窗口跟踪、多窗口列表、激活/最小化/正常关闭操作。
- 新增时钟月历、今日任务、音量与静音、网络/电池状态、通知、输入法、显示桌面和电源入口。
- 使用 `SetWinEventHook` 跟踪窗口变化，并在独占或无边框全屏应用前台时默认停用鼠标热区。
- 新增 `Ctrl+Alt+Space` 全局主动唤出快捷键，以及可持久化的主题和全屏热区选项。
- 桌面收纳、任务、番茄钟、OKR、AI 和数据库恢复全部迁入新工作区，原业务数据库内容与语义保持不变。

### 任务栏安全与恢复

- 新增仅作用于主显示器的 `TaskbarController`，隐藏 `Shell_TrayWnd` 并临时释放主屏工作区，不结束 Explorer、不处理副屏任务栏。
- 新增同一可执行文件的 `--taskbar-watchdog` 守护模式；父进程异常退出时恢复任务栏与原工作区。
- 增加 `Ctrl+Alt+Shift+F10` 紧急恢复快捷键；快捷键注册失败时禁止隐藏任务栏。
- 启动、正常退出、未处理异常和数据库恢复重启均执行幂等恢复；Explorer 重启及显示设置变化后重新识别任务栏。
- 移除构造函数中的无条件开机自启动，改为首次安全引导中的明确选项。

### 视觉、数据与测试

- 移除 MaterialDesignThemes 与 MaterialDesignColors 包，改用自定义 Fluent 设计令牌、Segoe Fluent Icons、DWM 圆角和 Desktop Acrylic。
- 支持系统浅色/深色、高对比度及透明效果不可用时的安全降级，并增加 Per-Monitor V2 DPI manifest。
- 新增 `PinnedApps` 表和唯一索引，继续通过 `EnsureSchema()` 手工维护数据库结构。
- 新增可替换的任务栏、窗口、应用目录和系统状态边界，以及统一管理生命周期的 `ShellCoordinator`。
- 新增独立 xUnit 测试项目，覆盖应用去重、固定项排序、任务栏安全前置条件与幂等状态转换；测试不会调用真实的隐藏任务栏 API。

## Unreleased

### Desktop Organizer
- Added a custom desktop overlay so FocusPanel can collect desktop icons into panel partitions while the original files remain in the Desktop folder.
- Added free desktop icon placement with persisted `DesktopX` / `DesktopY` coordinates.
- Added drag support from desktop into the organizer panel and back out to the desktop without triggering Explorer's same-name file prompt.
- Added desktop icon sorting by name, type, date, and size.
- Added blank-desktop double-click to hide or restore the custom desktop icons.
- Expanded native-like desktop context menu actions: open, open with, show in Explorer, cut, copy, paste, rename, delete, refresh, properties, sorting, and desktop folder access.
- Added adjustable desktop icon size from the desktop right-click menu: small, medium, large, and extra large.
- Persisted desktop icon size in app configuration.
- Improved icon rendering quality by loading larger shell icons and using high-quality scaling.

### Desktop Storage Semantics
- Reworked collection behavior away from simply relying on Windows hidden-item visibility as the user-facing model.
- Kept files physically on the desktop while FocusPanel tracks whether they are collected into the panel.
- Updated desktop file scanning to include FocusPanel-managed collected files while still filtering unrelated system-hidden files.
- Added database schema support for desktop icon position and collection state.
- Fixed save failures caused by missing or null desktop file preference fields.

### UI
- Removed the Dashboard navigation entry from the main panel.
- Changed the default startup view to the desktop organizer page.
- Rebuilt the main shell with an Apple-inspired glassmorphism style: translucent surfaces, soft shadows, rounded panels, and lighter navigation.
- Restyled the desktop organizer page with glass cards, polished partition headers, clearer file hover/selection states, and localized Chinese UI copy.
- Improved the organizer toolbar, popups, empty state, rename dialog, and rescue tools presentation.
- Refined the Apple-inspired styling after review to reduce excessive transparency, gradients, and heavy shadows in favor of a cleaner Finder-like panel and organizer layout.
- Reworked the organizer layout after visual QA: removed the nested left action rail, moved organizer actions into a single top toolbar, unified icon sizing/color, and standardized partition card radius, borders, and drag feedback.
- Fixed right-edge drawer chrome: removed right-side rounded corners, removed outer shadow halo, disabled host-window DWM rounding, and made the drawer background fully opaque.

### Desktop-Only Panel Behavior
- Fixed a blank-desktop recovery issue by removing unconditional native desktop icon hiding during overlay load/refresh and synchronizing Explorer icon visibility only after the custom desktop overlay has visible icon data.
- Added startup, tray-hide, app-hide, exit, and unhandled-exception fail-safes that restore Explorer's native desktop icons.
- Hardened desktop ListView discovery by rebuilding the Explorer desktop host before giving up on native icon recovery.
- Fixed desktop-scene detection for minimized or invisible foreground windows and explicitly shows the hidden-startup main window before making the panel visible.
- Fixed Windows Show Desktop recovery by restoring minimized overlay and panel windows when the foreground guard detects the desktop scene.
- Added a minimized-state recovery hook so right-corner Show Desktop can pull FocusPanel back into the desktop scene after Windows minimizes it.
- Fixed a wallpaper-only Show Desktop state by restoring native Explorer icons whenever the custom desktop overlay is minimized.
- Added DWM cloaking detection so native desktop icons are restored if Windows Show Desktop hides the overlay without minimizing it.
- Replaced desktop-scene topmost recovery with non-activating Win32 window restore so Show Desktop can bring FocusPanel back without restoring other application windows.
- Tightened foreground detection and forced non-topmost demotion after desktop recovery so FocusPanel does not linger above newly focused application windows.
- Fixed a blank-desktop fail-safe issue by restoring Explorer's native desktop icons whenever the FocusPanel desktop overlay is hidden.
- Hardened desktop-only window layering so both the desktop overlay and panel stay hidden outside the desktop scene and the panel never uses topmost mode over other applications.
- Improved panel visibility behavior so it can stay available in the desktop scene while avoiding obstruction of normal foreground applications.
- Preserved drag-to-panel behavior while preventing the panel from disappearing during desktop collection workflows.
